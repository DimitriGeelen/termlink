id: crates/termlink-hub/build.rs
name: build
type: script
subsystem: hub
location: crates/termlink-hub/build.rs
tags: []
standalone: true

purpose: "Cargo build script for termlink-hub: derives version from git tags (T-1458). Standalone — runs at compile time, queries git, no source imports."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-05-03
created_by: unknown

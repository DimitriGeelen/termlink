id: crates/termlink-cli/src/commands/vendor.rs
name: vendor
type: module
subsystem: cli
location: crates/termlink-cli/src/commands/vendor.rs
tags: [cli, vendor, portability]

purpose: "Vendor the TermLink binary into a project directory for path isolation — copies binary, writes VERSION file, checks .gitignore"

depends_on: []  # No imports from registered components — uses only anyhow + std

depended_by:
  - target: crates/termlink-cli/src/commands/mod.rs
    relationship: declared_by
  - target: crates/termlink-cli/src/main.rs
    relationship: called_by

last_verified: 2026-03-26
created_by: unknown

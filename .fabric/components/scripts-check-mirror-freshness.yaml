id: scripts/check-mirror-freshness.sh
name: check-mirror-freshness
type: script
subsystem: operations
location: scripts/check-mirror-freshness.sh
tags: []
standalone: true

purpose: "Operational check: compares OneDev origin HEAD to GitHub mirror HEAD; alerts on drift (G-007). Black-box git ls-remote; no source-level edges."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-04-19
created_by: unknown

id: install.sh
name: install
type: script
subsystem: distribution
location: install.sh
tags: []
standalone: true

purpose: "End-user installer script: detects platform, downloads binary from GitHub Releases, places in PATH. Standalone — no source-tree dependencies."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-04-19
created_by: unknown

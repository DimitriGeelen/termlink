id: crates/termlink-hub/tests/no_legacy_callers.rs
name: no_legacy_callers
type: script
subsystem: hub
location: crates/termlink-hub/tests/no_legacy_callers.rs
tags: []
standalone: true

purpose: "T-1406 regression guard: scans crates/**/src/**/*.rs for new direct callers of legacy primitives (event.broadcast, inbox.*, file.*). Walks source tree at runtime; no specific symbol dependencies."

depends_on:
  # Format: - target: <relative-path>
  #          type: calls|reads|writes|triggers|renders
  []

depended_by:
  # Same format as depends_on — filled by enrich.py or manually
  []

last_verified: 2026-04-29
created_by: unknown

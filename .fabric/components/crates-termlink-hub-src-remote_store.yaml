id: crates/termlink-hub/src/remote_store.rs
name: remote_store
type: module
subsystem: hub
location: crates/termlink-hub/src/remote_store.rs
tags: [remote, tcp, sessions, store, heartbeat]

purpose: "In-memory store for remote (TCP) session registrations — heartbeat-based TTL, background reaper for expired entries, thread-safe via RwLock."

depends_on: []  # No imports from registered components — uses only std + serde_json

depended_by:
  - target: crates/termlink-hub/src/lib.rs
    relationship: declared_by
  - target: crates/termlink-hub/src/router.rs
    relationship: used_by
  - target: crates/termlink-hub/src/server.rs
    relationship: used_by

last_verified: 2026-03-26
created_by: unknown

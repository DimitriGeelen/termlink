# Secret scan patterns — TSV (name<TAB>regex)
# Loaded by .agentic-framework/agents/git/lib/secret-scan.sh on every pre-commit.
# Origin: T-1725 (T-1695 leak prevention — github_pat_ was committed in commit
# 15c19f22 and merged to main; secret-scan.sh shipped but had no pattern catalogue,
# so it silently no-op'd).
#
# Format: each non-blank, non-comment line is <name><TAB><regex>.
# Lines starting with `#` are comments. Tabs are required (not spaces).
github_pat_finegrained	github_pat_[A-Za-z0-9_]{82}
github_classic_pat	ghp_[A-Za-z0-9]{36}
github_oauth_token	gho_[A-Za-z0-9]{36}
github_user_to_server	ghu_[A-Za-z0-9]{36}
github_server_to_server	ghs_[A-Za-z0-9]{36}
github_refresh_token	ghr_[A-Za-z0-9]{36}
anthropic_api_key	sk-ant-[A-Za-z0-9_-]{32,}
aws_access_key_id	AKIA[0-9A-Z]{16}
ssh_private_key_header	-----BEGIN [A-Z ]*PRIVATE KEY-----
slack_token	xox[baprs]-[A-Za-z0-9-]{10,}

# Project Learnings - Knowledge gained during development
# Added via: fw context add-learning "description" --task T-XXX
learnings:
- id: L-001
  learning: "Component fabric requires explicit initialization per project — no structural gate catches empty fabric. When starting a new project: create subsystems.yaml, register source files as cards with typed edges (target+type format), and create watch-patterns.yaml. Audit should check for drift."
  source: P-002
  task: T-043
  date: 2026-03-08
  context: Added via context agent
  application: TBD
- id: L-002
  learning: "Always run fw fix-learned after completing bugfix tasks — captures the root cause and mitigation as a pattern for future debugging"
  source: unknown
  task: T-064
  date: 2026-03-10
  context: Added via context agent
  application: TBD
- id: L-003
  learning: "Watcher scripts must capture subprocess exit codes explicitly — the || pattern swallows non-zero exits and causes false success reporting"
  source: unknown
  task: T-065
  date: 2026-03-10
  context: Added via context agent
  application: TBD
- id: L-004
  learning: "Shell commands accepting user input must escape arguments before passing to sh -c — use single-quote wrapping with internal quote escaping"
  source: unknown
  task: T-064
  date: 2026-03-10
  context: Added via context agent
  application: TBD
- id: L-005
  learning: "CLI field name must match RPC response field exactly — bytes_written vs bytes_len mismatch caused silent display bug (always showed 0)"
  source: P-001
  task: T-177
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-006
  learning: "Hub tests that set TERMLINK_RUNTIME_DIR must use ENV_LOCK and isolated temp dirs — without isolation, TLS cert from another test leaks and causes handshake failures"
  source: P-001
  task: T-172
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-007
  learning: "Newer Rust editions require unsafe block around std::env::set_var — test code that sets env vars needs explicit unsafe with SAFETY comment explaining single-threaded access"
  source: P-001
  task: T-172
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-008
  learning: "ink TUI (React terminal) treats multi-byte writes as paste — Enter (0x0D) must be sent as separate pty.write() with delay for keypress detection"
  source: P-001
  task: T-178
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-009
  learning: "Framework task gate check-active-task.sh blocks worker agents in mesh dispatch — workers need bypass or separate task assignment before they can write files"
  source: P-001
  task: T-119
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-010
  learning: "Cross-machine prompt injection via TermLink TOFU is the preferred path for consumer-to-framework upstream reports — faster and richer than file-based handoff. Chain: TOFU TLS + hub auth + hub routing + split PTY writes. Example: tofu_test"
  source: P-001
  task: T-185
  date: 2026-03-18
  context: Added via context agent
  application: TBD
- id: L-011
  learning: "CLI parameter defaults: None (omitted) and zero-value are semantically different. since=0 means 'after event 0' not 'from the start'. Test boundary conditions explicitly."
  source: P-001
  task: T-056
  date: 2026-03-19
  context: Added via context agent
  application: TBD
- id: L-012
  learning: "Framework hook paths must reference final installation directory (symlinks), not version-specific paths. Test hook paths immediately after tool upgrades."
  source: P-001
  task: T-020
  date: 2026-03-19
  context: Added via context agent
  application: TBD
- id: L-013
  learning: "Add state transition guards to prevent invalid state changes. Always implement Drop for resource cleanup. Document known race conditions (TOCTOU) with mitigation paths even if not fully resolved."
  source: P-001
  task: T-067
  date: 2026-03-19
  context: Added via context agent
  application: TBD
- id: L-014
  learning: "Avoid hardcoding context window thresholds. Use configurable environment variables (CONTEXT_WINDOW, THRESHOLD_LEVELS) so framework tools scale with model capability changes."
  source: P-001
  task: T-131
  date: 2026-03-19
  context: Added via context agent
  application: TBD
- id: L-015
  learning: "PTY tests under parallel execution: always implement Drop for PTY devices and child processes. Reduce timeouts to free resources faster. Shared test state (ENV_LOCK) prevents resource contention."
  source: P-001
  task: T-170
  date: 2026-03-19
  context: Added via context agent
  application: TBD
- id: L-016
  learning: "Delegate complex platform-specific logic (osascript, tmux, background PTY) to a single source of truth (termlink spawn). Use override mechanisms (env vars + flags) for testing and CI."
  source: P-001
  task: T-154
  date: 2026-03-19
  context: Added via context agent
  application: TBD

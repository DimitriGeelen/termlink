# Volatile session files — regenerated each session
.tool-counter
.prev-token-reading
session.yaml
focus.yaml
tier0-approval

# Ephemeral state files — session-local, cause merge conflicts if tracked
.budget-status
.budget-gate-counter
.compact-log
.handover-cooldown
.restart-requested
.tier0-approval.pending

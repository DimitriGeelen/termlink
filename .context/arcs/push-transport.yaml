id: arc-004
slug: push-transport
name: "Push-based transport for reliable-comms (WebSockets)"
description: "GO(scoped) output of the T-2303 inception. WS for the live agent path (Candidate\
  \ A). Webhooks deferred as a separate external-only augmentation (A1). Durability\
  \ layer (queue/idempotency/confirm/journal) unchanged. Build slices S1 hub WS upgrade\
  \ endpoint, S2 broadcast\u2192client concurrent write path, S3 client WS subscribe\
  \ + degrade-to-poll, S4 wire delivery-confirm/journal through WS unchanged."
status: in-progress
anchor_task: T-2303
headline_mechanic: "A live agent receives a DM the instant it is posted via a hub\u2192client WebSocket\
  \ push stream (sub-second), replacing the 15s doorbell-then-poll wake/read floor,\
  \ and cleanly degrades back to polling if the socket drops \u2014 the durable dm:\
  \ topics, receipts, journal and offline queue stay authoritative underneath."
demo_evidence: null
created: 2026-07-02T15:47:28Z
closed_at: null
decision: null
# T-1918 (arc-006, value-prioritisation): BVP scoring fields. Semantics in
# docs/reports/T-1915-bvp-inception.md §4 (D7-reframe) and §7 (M2).
bvp_scores: {}
scoped_drivers: []           # max 3, weight ≤6 each (M2). Approved by fw arc approve-driver (T-1926).
proposed_scoped_drivers: []  # uncapped persistence (D7-reframe). Estimator may write here freely.

# ============================================================================
# EPISODIC MEMORY - T-1716: termlink_agent_contact MCP v2 — add require_online + ack_required (T-1715 follow-up)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-20T19:25:35Z
# ============================================================================

task_id: T-1716
task_name: "termlink_agent_contact MCP v2 — add require_online + ack_required (T-1715 follow-up)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-19T22:36:17Z
completed: 2026-05-20T19:25:33Z
duration_days: 1
updates_count: 1
first_commit: 2026-05-20T00:44:50+02:00
last_commit: 2026-05-20T00:44:50+02:00

# Summary (auto-generated from git commit messages)
summary: |
  termlink_agent_contact MCP v2 — add require_online + ack_required (T-1715 follow-up)

# Key outcomes
outcomes:
  - "`AgentContactParams` extended with: `require_online` (Option<bool>), `online_window_secs` (Option<u64>), `ack_required` (Option<bool>), `ack_timeout_secs` (Option<u64>). Defaults preserve v1 byte-identical behavior — `unwrap_or(false)` / `unwrap_or(...)` on the optional fields, unset = no probe / no poll (PL-172 byte-identity rule)"
  - "When `require_online=true`: probes `agent-chat-arc` via `fetch_topic_msgs_mcp` (slice=500) BEFORE posting, computes `PresenceMcp` via `evaluate_presence_msgs`. Returns `{ok: false, peer_fp, online_check: {online, last_seen_ms, posts_in_window, window_secs}, error: \"...\"}` when `posts_in_window == 0` — matches CLI exit-9 shape"
  - "When `require_online=false` (default): no probe, behavior identical to v1 (verified by `agent_contact_params_minimal_message_only_target_fp` test still passing — same shape unchanged)"
  - "When `ack_required=true`: AFTER posting, polls dm topic at ~1s cadence (via `tokio::time::sleep`) up to clamped `ack_timeout_secs`. Returns `ack: {received: bool, ts_ms?, wait_secs, timeout_secs?}` on the success envelope — `received=true` on first matching non-meta envelope from peer_fp with `ts > send_ts_ms_for_ack`, `received=false` on timeout"
  - "When `ack_required=false` (default): no poll, single round-trip post then return"
  - "Pure helpers shipped: `evaluate_presence_msgs(msgs, peer_fp, now_ms, window_ms) -> PresenceMcp` and `detect_ack_in_msgs_mcp(msgs, peer_fp, send_ts_ms) -> Option<i64>`. Mirror CLI's `evaluate_presence` + `detect_ack_in_msgs` one-to-one (same META_MSG_TYPES filter, `ts_unix_ms` preferred with `ts` fallback, case-sensitive sender_id match)"
  - "`fetch_topic_msgs_mcp(hub_socket, topic, slice_size)` helper does `channel.list` + `channel.subscribe` round-trip — uses local hub_socket like `termlink_agent_post`; symmetric with CLI's `fetch_topic_msgs` (commands/channel.rs:693)"
  - "`cargo build --release -p termlink-mcp` clean — 1m 11s, only the pre-existing `cur_run_end` warning"
  - "**7** unit tests for new helpers + extended params — exceeded the 6-test target. Coverage: params-v2-deserialize (1), evaluate_presence (3: online / offline / meta-filter), detect_ack (3: found / wrong-sender / ts-fallback). All 7 pass; the existing 8 v1 tests also still pass (15/15 total on the agent_contact suite)"
  - "Tool description in `#[tool(...)]` attribute updated — now mentions T-1716 surface, lists require_online + online_window_secs + ack_required + ack_timeout_secs with their clamp ranges + exit-code mapping. No longer claims \"deferred to v2\""

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-mcp/src/tools.rs"
  - ".tasks/active/T-1716-termlinkagentcontact-mcp-v2--add-require.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-20T00:44:50"
    action: "T-1716: termlink_agent_contact MCP v2 — add require_online + ack_required (T-1715 follow-up)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 1249 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 1249
  commits: 2
  files_changed: 3
  lines_added: 807
  lines_removed: 27

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1716-termlinkagentcontact-mcp-v2--add-require.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1625: topic-field availability signal in api-usage.sh — disambiguate no-traffic vs pre-T-1622 hub
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-06T16:12:52Z
# ============================================================================

task_id: T-1625
task_name: "topic-field availability signal in api-usage.sh — disambiguate no-traffic vs pre-T-1622 hub"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-06T16:04:09Z
completed: 2026-05-06T16:12:50Z
duration_days: 0
updates_count: 2
first_commit: 2026-05-06T18:12:46+02:00
last_commit: 2026-05-06T18:12:46+02:00

# Summary (auto-generated from git commit messages)
summary: |
  topic-field availability signal — close T-1622 UX hole

# Key outcomes
outcomes:
  - "`legacy_topic_coverage_for_window(days)` helper added to api-usage.sh, returns `(legacy_total, with_topic)` for the window"
  - "Helper is SEPARATE from `stats_for_window` — does not mutate the 10-tuple return (PL-152)"
  - "`legacy_topic_coverage` field present in single-window JSON: `{\"total\": N, \"with_topic\": M}` (verified: `mode: single-window` shows `{'total': 4724, 'with_topic': 0}`)"
  - "`legacy_topic_coverage` field present in trend JSON (60d window) (verified: top-level field, `{'total': 7870, 'with_topic': 0}`)"
  - "Single-window text: when `legacy_total > 0` and topic table empty, prints `\"(topic field unavailable — hub may predate T-1622)\"` (verified live on `--last-Nd 7`)"
  - "Single-window text: when `legacy_total > 0` and `with_topic < legacy_total` and `with_topic > 0`, prints `\"(topic field present on X/Y legacy entries)\"` (path coded; live hub has 0 with-topic so other branch fires today)"
  - "Trend mode text (60d): same two hint lines (verified live on default invocation — \"(topic field unavailable…)\" appears)"
  - "Existing T-1623 PL-152 static check still passes: `bash tests/test_pl152_counter_arity_static.sh` (9/9 PASS, 7 call sites, arity 10 — helper isolation discipline held)"
  - "api-usage.sh JSON parses end-to-end (`python3 -c 'import json,sys; json.load(sys.stdin)'` → JSON OK)"
  - "Upstream `/opt/999-Agentic-Engineering-Framework/agents/metrics/api-usage.sh` carries the same patch (commit `1664057dd` on origin/master, 5/5 patches applied via /tmp/patcher_t1625.py, sha256 `f98b3de4…`)"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - ".tasks/active/T-1625-topic-field-availability-signal-in-api-u.md"
  - ".tasks/completed/T-1624-gitignore-cron-audit-dir--kill-600-dirty.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-06T18:12:46"
    action: "T-1625: topic-field availability signal — close T-1622 UX hole"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 1
  files_changed: 2
  lines_added: 154
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1625-topic-field-availability-signal-in-api-u.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1826: agent-conversation-status.sh — conversation_id state diagnostic
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-28T09:44:28Z
# ============================================================================

task_id: T-1826
task_name: "agent-conversation-status.sh — conversation_id state diagnostic"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-28T09:39:59Z
completed: 2026-05-28T09:44:26Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-28T11:44:18+02:00
last_commit: 2026-05-28T11:44:18+02:00

# Summary (auto-generated from git commit messages)
summary: |
  agent-conversation-status.sh — single-cid state diagnostic (a2a observability)

# Key outcomes
outcomes:
  - "`scripts/agent-conversation-status.sh` exists, executable, `bash -n` + `shellcheck` clean."
  - "Accepts `--topic <T> --conversation-id <CID>` (required) + `--hub <addr>` (optional) + `--json` (optional flag)."
  - "In text mode, prints a multi-line summary with: topic, conversation_id, turn count (with sender breakdown), receipt count (with up_to mapping), pending receipts (turn offsets with no matching `metadata.up_to >= turn_offset`), last activity timestamp (RFC3339)."
  - "In `--json` mode, emits a single JSON object with fields `{ok, topic, conversation_id, turns:[{offset, sender, ts}], receipts:[{offset, up_to, sender, ts}], pending_turn_offsets:[], senders:[], last_activity, summary:{turn_count, receipt_count, pending_count, sender_count}}`."
  - "Unknown / empty conversation_id yields exit 0 with `turn_count=0` and `summary.turn_count=0` (not an error — empty conversation is valid state)."
  - "Usage errors (missing required args) exit 2 with usage to stderr."
  - "`scripts/test-agent-conversation-status.sh` covers: (a) usage error on missing args, (b) unknown args exit 2, (c) `--json` parses with jq, (d) empty cid returns turn_count=0, (e) populated cid returns matching counts, (f) pending detection: turn offsets without watermark coverage are flagged. Tests use a deterministic local hub setup (ephemeral topic, synthetic posts) — no cross-host dependency. **5 PASS / 0 FAIL** at HEAD."
  - "Test suite ALL PASS under both bash and shellcheck."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "scripts/agent-conversation-status.sh"
  - "scripts/test-agent-conversation-status.sh"
  - ".tasks/active/T-1826-agent-conversation-statussh--conversatio.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-28T11:44:18"
    action: "T-1826: agent-conversation-status.sh — single-cid state diagnostic (a2a observability)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 1
  files_changed: 3
  lines_added: 452
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1826-agent-conversation-statussh--conversatio.md
generated_by: context-agent-hybrid

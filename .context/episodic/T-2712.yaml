# ============================================================================
# EPISODIC MEMORY - T-2712: Fabric watch patterns exclude the guard layer — coverage measured over a subset
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-14T19:21:11Z
# ============================================================================

task_id: T-2712
task_name: "Fabric watch patterns exclude the guard layer — coverage measured over a subset"
workflow_type: refactor
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-14T18:50:18Z
completed: 2026-08-14T19:18:58Z
duration_days: 0
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  Widen watch-patterns.yaml to cover scripts/tests that already carry cards, and register the newly-visible files

# Key outcomes
outcomes:
  - '`.fabric/watch-patterns.yaml` covers the file classes that already carry cards: `scripts/**/*.sh`, `tests/**/*.sh`, `crates/*/tests/**/*.rs`, `scripts/**/*.py`, `crates/*/build.rs`'
  - 'The widening is justified in-file, so the next reader knows why the list grew and does not narrow it back'
  - '`fw fabric drift` reports every watched file registered (was: 4 unregistered against the narrow patterns; now 352/352 against the wide ones)'
  - 'The `card(s) point at files no watch pattern covers` warning drops from 48 to 4, and the residual 4 are named and justified rather than silently tolerated'
  - 'The edge-coverage metric shift (18% → 56%) is recorded as a measurement-scope change, NOT reported as a regression or quietly omitted'
  - 'Registration is done via `fw fabric scan` + `fw fabric enrich` so derived edges are captured (56 new edges across session/cli/mcp/protocol/agent-mesh), not by hand-writing skeletons'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 28 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 28
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2712-fabric-watch-patterns-exclude-the-guard-.md
generated_by: context-agent-hybrid

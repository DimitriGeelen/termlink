# ============================================================================
# EPISODIC MEMORY - T-2205: Worker-side template for substrate parallel-worker pattern (T-2204 follow-up)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-13T09:12:29Z
# ============================================================================

task_id: T-2205
task_name: "Worker-side template for substrate parallel-worker pattern (T-2204 follow-up)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-13T09:05:49Z
completed: 2026-06-13T09:12:27Z
duration_days: 0
updates_count: 2
first_commit: 2026-06-13T11:12:22+02:00
last_commit: 2026-06-13T11:12:22+02:00

# Summary (auto-generated from git commit messages)
summary: |
  ship worker-backlog-drain.sh + orchestrator JSON-shape fixes

# Key outcomes
outcomes:
  - 'Worker script `scripts/worker-backlog-drain.sh` exists and is executable'
  - 'Three modes wired: `--dry-run` (default, no hub writes), `--live` (operator stdin prompt), `--auto-noop` (release without work — substrate smoke-test only)'
  - 'Worker identity resolves via the same chain as orchestrator: `--worker-id` flag → `TERMLINK_AGENT_ID` env → `~/.termlink/be-reachable.state`'
  - 'Reads `channel claims` for `$QUEUE_TOPIC`, filters to claims where `claimer == $WORKER_ID` (uses the LIST verb T-2037, NOT `claims-summary` which is aggregate-only — verified via end-to-end simulation with posted T-9999 work-unit)'
  - 'Per held claim: prints offset, claim_id, ttl_remaining + task_id, classification, ac_count, dispatched_by, and the appropriate brief (closure-ready vs needs-work) — payload decoded from base64 `payload_b64` envelope field'
  - '`--once` flag for single-pass mode; default is polling loop with `--poll-interval` (default 15s)'
  - 'SIGINT exits cleanly (loop trap on INT and TERM)'
  - 'Recipe doc `docs/operations/substrate-orchestrator-recipe.md` references the worker script alongside the orchestrator in the in-tree consumer section (new "Worker-side companion" subsection)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'ship worker-backlog-drain.sh + orchestrator JSON-shape fixes'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'docs/operations/substrate-orchestrator-recipe.md'
  - 'scripts/orchestrator-backlog-drain.sh'
  - 'scripts/worker-backlog-drain.sh'
  - '.tasks/active/T-2205-worker-side-template-for-substrate-paral.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-13T11:12:22"
    action: "T-2205: ship worker-backlog-drain.sh + orchestrator JSON-shape fixes"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 6 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [substrate, parallel-worker, worker, arc-001, T-2018]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 6
  commits: 1
  files_changed: 4
  lines_added: 468
  lines_removed: 1

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2205-worker-side-template-for-substrate-paral.md
generated_by: context-agent-hybrid

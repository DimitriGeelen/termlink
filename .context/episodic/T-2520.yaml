# ============================================================================
# EPISODIC MEMORY - T-2520: file receive: sanitize wire filename to basename (path-traversal / arbitrary-write fix)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-04T09:57:41Z
# ============================================================================

task_id: T-2520
task_name: "file receive: sanitize wire filename to basename (path-traversal / arbitrary-write fix)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-04T09:39:27Z
completed: 2026-08-04T09:57:06Z
duration_days: 0
updates_count: 3
first_commit: 2026-08-04T11:44:57+02:00
last_commit: 2026-08-04T11:55:27+02:00

# Summary (auto-generated from git commit messages)
summary: |
  sanitize wire filename to basename on file receive — fixes path-traversal / arbitrary-write outside -o dir (campaign firing #39). tick final AC — release build exit 0, all 4 Agent ACs verified

# Key outcomes
outcomes:
  - 'A `sanitize_recv_filename` helper reduces a wire filename to its trusted final basename (via `Path::file_name`), returning `None` for `""`, `.`, `..`, or path-only inputs.'
  - 'All four receive-side join sites (file.rs:507, 537, 858, 975) route the wire filename through the helper before `out_path.join`; a rejected/path-bearing name falls back to the existing synthesized default, never escapes `out_path`.'
  - 'A regression test `sanitize_recv_filename_confines_to_basename` asserts traversal (`../escape`), absolute (`/etc/x`), and nested (`a/b/c.txt`) inputs all reduce to a bare basename (or `None`) and never yield a parent/absolute path. Proven load-bearing (FAILS without the fix).'
  - '`cargo test -p termlink --bins sanitize_recv_filename` passes; `cargo build --release -p termlink` succeeds (exit 0).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'sanitize wire filename to basename on file receive — fixes path-traversal / arbitrary-write outside -o dir (campaign firing #39)'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-cli/src/commands/file.rs'
  - '.tasks/active/T-2520-file-receive-sanitize-wire-filename-to-b.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-04T11:44:57"
    action: "T-2520: sanitize wire filename to basename on file receive — fixes path-traversal / arbitrary-write outside -o dir (campaign firing #39)"
  - time: "2026-08-04T11:55:27"
    action: "T-2520: tick final AC — release build exit 0, all 4 Agent ACs verified"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 17 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 17
  commits: 2
  files_changed: 2
  lines_added: 276
  lines_removed: 7

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2520-file-receive-sanitize-wire-filename-to-b.md
generated_by: context-agent-hybrid

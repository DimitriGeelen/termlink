# ============================================================================
# EPISODIC MEMORY - T-1251: T-1164d Legacy file.* event deprecation + inbox.rs cleanup + PL-011 closure
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-25T13:30:58Z
# ============================================================================

task_id: T-1251
task_name: "T-1164d Legacy file.* event deprecation + inbox.rs cleanup + PL-011 closure"
workflow_type: decommission
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-25T11:43:54Z
completed: 2026-04-25T13:30:48Z
duration_days: 0
updates_count: 3
first_commit: 2026-04-25T15:30:44+02:00
last_commit: 2026-04-25T15:30:44+02:00

# Summary (auto-generated from git commit messages)
summary: |
  Blob GC primitive + legacy file.* deprecation + PL-011 closure

# Key outcomes
outcomes:
  - "`crates/termlink-bus/src/artifact_store.rs` gains `pub fn sweep(&self, referenced: &HashSet<String>) -> io::Result<u64>` that walks `<root>/<2-hex>/<sha256>` files, deletes blobs whose sha is NOT in the referenced set, and returns the count pruned. Skips `.staging` directory. Empty-prefix-dir cleanup is best-effort (ignore errors)."
  - "Unit tests in artifact_store.rs: `sweep_removes_unreferenced_keeps_referenced` (puts 3 blobs, references 1, expects 2 pruned + 1 retained); `sweep_empty_set_prunes_all`; `sweep_skips_staging_dir` (creates a .staging entry, sweep doesn't touch it)."
  - "`crates/termlink-hub/src/inbox.rs` emits a one-shot `tracing::info!` deprecation log on the first legacy `file.*` event accepted into the inbox per process. Message: \"T-1251: legacy file.* events received — sender should migrate to channel.post {msg_type:artifact}\". Use `std::sync::OnceLock<()>` for warn-once."
  - "`.context/project/learnings.yaml` PL-011 entry gains an `application:` closure note pointing to T-1164's SendOutcome::Sent structural-fix."
  - "`cargo build && cargo test --workspace --lib && cargo clippy --workspace --tests -- -D warnings` all pass. Zero regressions (760 tests)."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - ".context/project/learnings.yaml"
  - "crates/termlink-bus/src/artifact_store.rs"
  - "crates/termlink-hub/src/inbox.rs"
  - ".tasks/active/T-1251-t-1164d-legacy-file-event-deprecation--i.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-04-25T15:30:44"
    action: "T-1251: Blob GC primitive + legacy file.* deprecation + PL-011 closure"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 106 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [T-1164, T-1155, bus, artifact, PL-011]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 106
  commits: 1
  files_changed: 4
  lines_added: 138
  lines_removed: 25

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1251-t-1164d-legacy-file-event-deprecation--i.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2399: MCP server signs envelopes as host key not per-agent identity (outbound identity leak)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-11T06:56:40Z
# ============================================================================

task_id: T-2399
task_name: "MCP server signs envelopes as host key not per-agent identity (outbound identity leak)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-10T21:54:18Z
completed: 2026-07-11T06:56:35Z
duration_days: 1
updates_count: 3
first_commit: 2026-07-11T00:07:10+02:00
last_commit: 2026-07-11T08:39:18+02:00

# Summary (auto-generated from git commit messages)
summary: |
  MCP handlers sign via per-agent identity resolver, not hardcoded host key. add deploy/activation runbook (rebuild + mcp-serve env + watched 3-hop test). deploy progress — pushed fix, durable .mcp.json TERMLINK_AGENT_ID env on 4 live .107 agents. LIVE-VERIFIED — identity fix proven (off=3/off=5 sign as aef's own key), auto-wake + auto-accept confirmed; second blocker (manual-mode) fixed

# Key outcomes
outcomes:
  - 'A shared `pub fn` in `termlink-session` (e.g. `agent_identity::resolve_signing_identity(fallback_base)`) resolves signing identity via FILE > DIR > AGENT_ID(per-agent) > fallback_base precedence — single source of truth mirroring `channel.rs::load_identity_or_create` / `registration.rs::resolve_identity_key_path`. — `resolve_signing_identity` + `resolve_signing_identity_path` in agent_identity.rs (1 def).'
  - 'Every hardcoded `Identity::load_or_create(&identity_dir)` *signing* site in `crates/termlink-mcp/src/tools.rs` routes through that resolver (grep shows 0 remaining `Identity::load_or_create(&identity_dir)` on post/reply/sign paths; read-only cursor-store sites at tools.rs:2769 may stay if they don''t sign). — verified: 0 hardcoded, 48 resolver calls.'
  - '`cargo build -p termlink-mcp` and `cargo build -p termlink-session` pass clean. — full `cargo build --release -p termlink` (pulls both) Finished clean → 0.11.502.'
  - 'A unit test asserts the resolver returns the per-agent key when `TERMLINK_AGENT_ID` is set (distinct fp) and the shared key when it is unset. — `resolve_signing_identity_path_precedence` passes; plus live proof: new-binary post w/ `TERMLINK_AGENT_ID=aef` signed as `0e7ee6ca` (per-agent), not `d1993c2c` (host).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'LIVE-VERIFIED — identity fix proven (off=3/off=5 sign as aef''s own key), auto-wake + auto-accept confirmed; second blocker (manual-mode) fixed'
    source: git-mined
  - description: 'deploy progress — pushed fix, durable .mcp.json TERMLINK_AGENT_ID env on 4 live .107 agents'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-mcp/src/tools.rs'
  - 'crates/termlink-session/src/agent_identity.rs'
  - '.tasks/active/T-2399-mcp-server-signs-envelopes-as-host-key-n.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-11T00:07:10"
    action: "T-2399: MCP handlers sign via per-agent identity resolver, not hardcoded host key"
  - time: "2026-07-11T00:46:57"
    action: "T-2399: add deploy/activation runbook (rebuild + mcp-serve env + watched 3-hop test)"
  - time: "2026-07-11T07:58:20"
    action: "T-2399: deploy progress — pushed fix, durable .mcp.json TERMLINK_AGENT_ID env on 4 live .107 agents"
  - time: "2026-07-11T08:39:18"
    action: "T-2399: LIVE-VERIFIED — identity fix proven (off=3/off=5 sign as aef's own key), auto-wake + auto-accept confirmed; second blocker (manual-mode) fixed"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 4 commit(s), 542 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 542
  commits: 4
  files_changed: 3
  lines_added: 407
  lines_removed: 55

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2399-mcp-server-signs-envelopes-as-host-key-n.md
generated_by: context-agent-hybrid

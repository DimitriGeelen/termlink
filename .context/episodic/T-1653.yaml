# ============================================================================
# EPISODIC MEMORY - T-1653: remote profile list/add — secret_file perms surfacing (PL-159 mirror)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-16T23:02:07Z
# ============================================================================

task_id: T-1653
task_name: "remote profile list/add — secret_file perms surfacing (PL-159 mirror)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-16T22:58:31Z
completed: 2026-05-16T23:01:46Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-17T01:01:41+02:00
last_commit: 2026-05-17T01:01:41+02:00

# Summary (auto-generated from git commit messages)
summary: |
  remote profile list/add — secret_file perms surfacing (PL-159 mirror)

# Key outcomes
outcomes:
  - "`cmd_profile_list` (`ProfileAction::List` branch in `remote.rs`) reads each profile's `secret_file` (when present), expands `~/`, calls `secret_file_perms_warning`, and renders a warning row under the affected profile in text mode + a `secret_perms_warning` field in JSON output. **Text: `  Warning: ...` indented under the row. JSON: `\"secret_perms_warning\": <string-or-null>` field.**"
  - "`cmd_profile_add` (`ProfileAction::Add` branch) computes the warning at insertion time (after the profile is saved) and prints it in non-JSON output as a `Warning:` line (mirroring the existing \"Tip:\" pattern from T-1651 for bootstrap-readiness). **Reads back from `config.hubs.get(&name).secret_file` after insertion to sidestep the secret_file → HubEntry move at line 933.**"
  - "`cargo check --workspace` passes — clean (1 pre-existing unrelated warning in termlink-mcp)"
  - "`cargo test -p termlink --bin termlink -- secret_file_perms expand_secret_file` passes — 5/5 T-1652 tests still green; no new tests needed since the helper logic is unchanged"
  - "Live verification: built debug binary, staged fake HOME with two profiles (`bad-perms` → 0o644 file, `good-perms` → 0o600 file). `profile list` showed Warning indented under `bad-perms` row only; `good-perms` clean. JSON output: `\"secret_perms_warning\"` was the full warning string for bad-perms and `null` for good-perms. `profile add new-profile ... --secret-file <0o644-file>` printed the Tip + Warning in sequence. Chmod 600 → list silent. Fixtures cleaned up."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-cli/src/commands/remote.rs"
  - ".tasks/active/T-1653-remote-profile-listadd--secretfile-perms.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-17T01:01:41"
    action: "T-1653: remote profile list/add — secret_file perms surfacing (PL-159 mirror)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 1
  files_changed: 2
  lines_added: 155
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1653-remote-profile-listadd--secretfile-perms.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2782: agent inbox empty result carries no scope — reads as no mail when it means no locally-tracked topics on this hub
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-17T10:55:46Z
# ============================================================================

task_id: T-2782
task_name: "agent inbox empty result carries no scope — reads as no mail when it means no locally-tracked topics on this hub"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-17T10:27:42Z
completed: 2026-08-17T10:54:04Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  termlink agent inbox returned unread_topics:[] while a full reply sat unread on a peer hub, causing two redundant escalations sent over the top of an answer. The scope limitation is documented in the MCP tool DESCRIPTION but absent from every OUTPUT path, so the answer reads as complete. Same shape as T-2680 (charter-drift canary reporting 214 checked / 0 off-charter while only scanning six known families) — fixed there by making every output path carry an explicit scope disclaimer. Apply that convention here.

# Key outcomes
outcomes:
  - '**Every** output path carries the scope — the empty-cursor early-out, the populated path, and the "scanned but nothing unread" path. The empty path is the one that matters most: an empty inbox is precisely when an operator stops looking, so it is the costliest place for a bare `[]`'
  - 'The envelope gains machine-readable scope fields, not just prose: at minimum `hub` (which hub was actually read) and `topics_scanned` (how many cursor-store topics were considered). A caller must be able to tell "0 unread of 9 tracked" from "0 tracked, nothing was looked at" — those are different facts and today both render as `[]`'
  - 'The empty path additionally carries an actionable `hint` naming the two blind spots by their remedy: this is one hub (no `--fleet`, unlike `check-outbox`), and only `subscribe --resume`-tracked topics are enumerated — so `/recent-dm <peer>` or a direct per-hub `channel subscribe` is what actually answers "did they reply?"'
  - 'Affirmative rather than silent on the healthy path, per the T-2076 convention already used by `claims-summary --only-stuck` (`All topics healthy (0/N stuck)`): a clean inbox states what was checked, e.g. `No unread across 9 tracked topic(s) on <hub>`'
  - 'MCP and CLI stay in parity — **delivered, but NOT as this AC was written, and the difference is deliberate.** I wrote "both surfaces gain the same fields" before reading the CLI''s JSON contract. `agent inbox --json` emits a bare ARRAY, and two consumers parse it: `scripts/substrate-worker-pickup.sh:290` and `docs/operations/substrate-orchestrator-recipe.md`, the latter documenting the array shape explicitly (T-2153). Wrapping it in an envelope is the tidier design and would break both. So the CLI carries the scope on **stderr** in `--json` mode (stdout byte-identical, machine contract untouched) and inline on the human path; the MCP envelope, which already exists, gains the fields additively. Same information on both surfaces, different transport, chosen by what each surface has promised its callers'
  - 'Regression test pins the property that actually failed: an empty result **must** carry the scope fields. A test asserting only `unread_topics == []` would have passed throughout this defect''s life and is not a test of it'
  - 'Load-bearing by mutation: removing the scope fields makes the new test(s) fail; restoring passes. Not argued — run and reported'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 26 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 26
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2782-agent-inbox-empty-result-carries-no-scop.md
generated_by: context-agent-hybrid

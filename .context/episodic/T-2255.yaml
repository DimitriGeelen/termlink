# ============================================================================
# EPISODIC MEMORY - T-2255: Fix worktree isolation — bootstrap complete vendored framework into git worktrees (gitignored-fw breaks finalize/merge)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-23T13:34:54Z
# ============================================================================

task_id: T-2255
task_name: "Fix worktree isolation — bootstrap complete vendored framework into git worktrees (gitignored-fw breaks finalize/merge)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-23T13:26:14Z
completed: 2026-06-23T13:34:45Z
duration_days: 0
updates_count: 2
first_commit: 2026-06-23T15:34:28+02:00
last_commit: 2026-06-23T15:34:28+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fix worktree isolation — bootstrap complete vendored framework into worktrees

# Key outcomes
outcomes:
  - '`scripts/worktree-bootstrap.sh` exists, is executable, and is git-TRACKED (not gitignored), so it rides into every worktree checkout'
  - 'Run inside a linked git worktree, it COPIES the framework files present in the main checkout but absent in the worktree (the untracked/gitignored set, incl. `lib/arc_membership.sh`) into the worktree''s existing dir; it does NOT symlink/replace the dir (which would delete the tracked files); idempotent on re-run'
  - 'Run from the main checkout it is a safe no-op (detects it is the main checkout / not a linked worktree) and exits 0 with a clear message'
  - 'After bootstrap, the finalize-breaker `.agentic-framework/lib/arc_membership.sh` is present in the worktree AND the bootstrap leaves `git status` clean for `.agentic-framework` (no spurious deletions/additions — copied files are themselves gitignored) — both asserted by `--self-test` (stands up a throwaway detached worktree, bootstraps, asserts, tears down)'
  - 'Errors are loud: not-a-git-repo, missing/incomplete main-checkout framework each exit non-zero with an actionable message (no silent degradation)'
  - 'A follow-up task (T-2256) is filed to propose the upstream worktree-aware `fw` resolution fix (the copy is mitigation, not the root structural fix)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'fix worktree isolation — bootstrap complete vendored framework into worktrees'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'scripts/worktree-bootstrap.sh'
  - '.tasks/active/T-2255-fix-worktree-isolation--bootstrap-comple.md'
  - '.tasks/active/T-2256-upstream-worktree-aware-fw-resolution---.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-23T15:34:28"
    action: "T-2255: fix worktree isolation — bootstrap complete vendored framework into worktrees"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 1
  files_changed: 3
  lines_added: 578
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2255-fix-worktree-isolation--bootstrap-comple.md
generated_by: context-agent-hybrid

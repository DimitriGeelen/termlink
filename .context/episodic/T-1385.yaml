# ============================================================================
# EPISODIC MEMORY - T-1385: channel commands accept TCP --hub addresses for cross-hub RPC
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-28T19:14:16Z
# ============================================================================

task_id: T-1385
task_name: "channel commands accept TCP --hub addresses for cross-hub RPC"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-28T18:21:09Z
completed: 2026-04-28T19:14:01Z
duration_days: 0
updates_count: 2
first_commit: 2026-04-28T21:13:54+02:00
last_commit: 2026-04-28T21:13:54+02:00

# Summary (auto-generated from git commit messages)
summary: |
  channel --hub accepts TCP host:port for cross-hub RPC

# Key outcomes
outcomes:
  - "`parse_hub_addr()` helper added — returns `TransportAddr::Tcp` for `host:port` (no `/`, trailing u16), else `TransportAddr::Unix`"
  - "`hub_socket()` and `hub_socket_soft()` return `TransportAddr` instead of `PathBuf`"
  - "All 4 helper signatures using `sock: &Path` updated to `sock: &TransportAddr` (ensure_topic, compute_dm_inbox_row, resolve_latest_offset, walk_topic_full)"
  - "All 20 `client::rpc_call(` call sites in channel.rs migrated (now go through new `rpc_call_authed` helper that pre-auths on TCP, delegates to `client::rpc_call_addr` on Unix)"
  - "`BusClient` (termlink-session) migrated to `TransportAddr` + `rpc_call_addr` for TCP-capable wire format; `cmd_channel_post` bypasses BusClient on TCP and uses authed direct RPC (offline queue stays Unix-only this iteration)"
  - "Workspace builds clean: `cargo check --release -p termlink`"
  - "Existing channel.rs unit tests pass: 306 tests in `channel::` module"
  - "Unit tests added: `parse_hub_addr_recognizes_tcp_host_port`, `parse_hub_addr_recognizes_localhost_tcp`, `parse_hub_addr_falls_back_to_unix_path`, `parse_hub_addr_path_with_colon_treated_as_unix`, `parse_hub_addr_invalid_port_falls_back_to_unix`"
  - "Cross-hub e2e: `termlink channel create xhub-real-... --hub 192.168.10.122:9100` succeeds and appears via `channel list --hub 192.168.10.122:9100` (NOT on local)"
  - "Cross-hub `channel post --hub 192.168.10.122:9100` succeeds with auth (signature verified by remote hub)"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-cli/src/commands/channel.rs"
  - "crates/termlink-session/src/bus_client.rs"
  - ".tasks/active/T-1385-channel-commands-accept-tcp---hub-addres.md"
  - "tests/e2e/multi-agent-conversation.sh"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-04-28T21:13:54"
    action: "T-1385: channel --hub accepts TCP host:port for cross-hub RPC"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 52 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 52
  commits: 1
  files_changed: 4
  lines_added: 484
  lines_removed: 58

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1385-channel-commands-accept-tcp---hub-addres.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2151: substrate-smoke.sh — end-to-end substrate health verifier (CI-runnable)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-11T06:50:51Z
# ============================================================================

task_id: T-2151
task_name: "substrate-smoke.sh — end-to-end substrate health verifier (CI-runnable)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-11T06:45:18Z
completed: 2026-06-11T06:50:34Z
duration_days: 0
updates_count: 2
first_commit: 2026-06-11T08:50:09+02:00
last_commit: 2026-06-11T08:50:09+02:00

# Summary (auto-generated from git commit messages)
summary: |
  substrate-smoke.sh end-to-end health verifier

# Key outcomes
outcomes:
  - '`scripts/substrate-smoke.sh` exists, is executable, and on a healthy local hub exits 0 with a per-stage PASS report. Verified live (2026-06-11): 6 stages PASS, exit 0.'
  - 'Each lifecycle stage (create, post, claim, claim-transfer, adopt+run+release, verify-clean) is loud-failed by stage name: a failure at any stage prints `FAIL at stage <name>: <error>` to stderr and exits 1. Verified live via `--hub 127.0.0.1:9` negative test: `FAIL at stage create: Connection refused` + exit 1.'
  - '`--json` mode emits one envelope `{ok, stages_passed, stages_failed, topic, claim_id, errors}` instead of human format. Verified — happy path: `{ok:true, stages_passed:[6 names], stages_failed:[]}`; failure path: `{ok:false, stages_passed:[], stages_failed:["create"]}`. Empty-array correctness verified (no `[""]` bug).'
  - '`--hub <addr>` targets a remote hub (passes through to underlying termlink subcommands). Verified — `--hub 127.0.0.1:9` reached the stage 1 termlink subcommand and produced a connection-refused failure.'
  - 'shellcheck on `scripts/substrate-smoke.sh` is clean. Verified — exit 0 with one annotated `# shellcheck disable=SC2016` for the deliberate single-quote `--cmd` arg (TERMLINK_CLAIM_ID must expand in the spawned worker, not in the outer shell).'
  - 'Smoke exercises substrate-worker-loop.sh `--claim-id` (T-2150) so it doubles as a regression gate for the composition path. Verified — the `worker-loop` stage passes `--claim-id "$CLAIM_ID"` and asserts the adopting-loud-log appears in stdout.'
  - 'Live smoke: run the verifier against the local hub, observe exit 0 + PASS report + clean substrate state. Verified — 6/6 PASS, `claims-summary --json` shows `active_count=0 expired_count=0`.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'docs/operations/substrate-getting-started.md'
  - 'scripts/substrate-smoke.sh'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-11T08:50:09"
    action: "T-2151: substrate-smoke.sh end-to-end health verifier"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 1
  files_changed: 2
  lines_added: 278
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2151-substrate-smokesh--end-to-end-substrate-.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2458: find-idle hint path under-reports live idle agents once cv_index topic saturates its per-topic cap — fall back to authoritative walk when saturated (round-14 F2)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-22T18:33:54Z
# ============================================================================

task_id: T-2458
task_name: "find-idle hint path under-reports live idle agents once cv_index topic saturates its per-topic cap — fall back to authoritative walk when saturated (round-14 F2)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-22T18:27:52Z
completed: 2026-07-22T18:32:54Z
duration_days: 0
updates_count: 1
first_commit: 2026-07-22T20:32:47+02:00
last_commit: 2026-07-22T20:32:47+02:00

# Summary (auto-generated from git commit messages)
summary: |
  find-idle falls back to authoritative walk when agent-presence cv_index saturates its per-topic cap — a lossy non-empty hint no longer hides freshly-idle overflow agents from dispatch (round-14 F2)

# Key outcomes
outcomes:
  - '`handle_agent_find_idle` (channel.rs) uses the walk path (`find_idle_agents`) when `current_values("agent-presence").len() >= cv_index::cap_per_topic()`, and the hint path only when the topic is strictly below cap (and non-empty). — via pure predicate `find_idle_hint_is_complete(entry_count, cap)` (channel.rs), unit-tested at the boundary.'
  - 'A regression test proves that when the topic''s cv_index is saturated (at cap) but a live idle agent overflowed (is absent from cv_entries), find-idle still returns that agent — i.e. the saturated case resolves via the walk, not the lossy hint. — `find_idle_walk_finds_overflow_agent_that_hint_misses` (termlink-bus): walk sees the overflow agent, incomplete hint does not.'
  - 'Below-cap behavior is unchanged (hint path still used); empty-index behavior unchanged (walk). Full `cargo test -p termlink-hub --lib` stays green. — 436 hub (+2) / 87 bus (+1) tests green; 17 find_idle tests pass.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-bus/src/lib.rs'
  - 'crates/termlink-hub/src/channel.rs'
  - '.tasks/active/T-2458-find-idle-hint-path-under-reports-live-i.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-22T20:32:47"
    action: "T-2458: find-idle falls back to authoritative walk when agent-presence cv_index saturates its per-topic cap — a lossy non-empty hint no longer hides freshly-idle overflow agents from dispatch (round-14 F2)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 1
  files_changed: 3
  lines_added: 297
  lines_removed: 1

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2458-find-idle-hint-path-under-reports-live-i.md
generated_by: context-agent-hybrid

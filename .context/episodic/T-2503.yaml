# ============================================================================
# EPISODIC MEMORY - T-2503: aggregator poll loop cursor stalls on missing next_seq — silent duplicate-event re-send storm
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-02T17:05:26Z
# ============================================================================

task_id: T-2503
task_name: "aggregator poll loop cursor stalls on missing next_seq — silent duplicate-event re-send storm"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-02T17:02:12Z
completed: 2026-08-02T17:05:18Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-02T19:05:13+02:00
last_commit: 2026-08-02T19:05:13+02:00

# Summary (auto-generated from git commit messages)
summary: |
  aggregator cursor advances on missing next_seq instead of stalling into a re-send storm

# Key outcomes
outcomes:
  - 'The Deliver arm advances the cursor via a pure helper `next_cursor(data, delivered_max_seq, current)` — `next_seq` when present, else `max(delivered_seq)+1` when events were delivered, else unchanged. (aggregator.rs — `next_cursor` + rewired Deliver arm)'
  - 'When the `next_seq` fallback is used the loop emits a `tracing::warn!` (loud, not silent) naming the session — a well-behaved server always stamps `next_seq`. (warn! with session/from_cursor/to_cursor)'
  - 'The pure helper is unit-tested: (a) `next_seq` present → used verbatim; (b) `next_seq` absent + events delivered → `max_seq+1` with fallback flag set; (c) `next_seq` absent + no events → cursor unchanged, no fallback; (d) `next_seq` present-but-non-integer → treated as absent (fallback). (4 `next_cursor_*` tests, all pass)'
  - 'The existing `poll_action_*` tests stay green; event delivery semantics (which events reach subscribers) are unchanged — only cursor advancement is hardened. (8/8 aggregator tests; delivery loop unchanged except max-seq tracking)'
  - '`cargo test -p termlink-hub --lib aggregator` passes. (8 passed; full lib 458 passed, 0 failed)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/aggregator.rs'
  - '.tasks/active/T-2503-aggregator-poll-loop-cursor-stalls-on-mi.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-02T19:05:13"
    action: "T-2503: aggregator cursor advances on missing next_seq instead of stalling into a re-send storm"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 1
  files_changed: 2
  lines_added: 297
  lines_removed: 3

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2503-aggregator-poll-loop-cursor-stalls-on-mi.md
generated_by: context-agent-hybrid

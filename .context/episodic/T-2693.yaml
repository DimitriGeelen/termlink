# ============================================================================
# EPISODIC MEMORY - T-2693: Nothing detects a new platform-locked primitive entering the product crates
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-27T23:56:22Z
# ============================================================================

task_id: T-2693
task_name: "Nothing detects a new platform-locked primitive entering the product crates"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-14T07:27:15Z
completed: 2026-08-27T23:53:16Z
duration_days: 13
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  The /proc dependency in whoami was added and shipped with no check. Portability (Directive #4) is discipline-only in the product crates — a static check would make it load-bearing, and the T-2684 runner now executes such checks (T-2690 G3).

# Key outcomes
outcomes:
  - '`scripts/check-platform-lock.sh` flags every Linux-only primitive in the product crates: `/proc/` and `/sys/` path literals, and `Command::new("<linux-only-tool>")` for a defensible tool list (`ss`, `systemctl`, `journalctl`, `ufw`, `setsid`, `nproc`)'
  - 'Cross-platform tools (`git`, `sh`, `bash`, `ssh`, `tmux`, `pgrep`) and deliberately macOS-only ones (`osascript`) are NOT flagged — the check is about undeclared lock-in, not about using a subprocess'
  - 'Confirmed-safe sites are acknowledged in `.context/checks/platform-lock-allowlist` (git-tracked per T-2681) with a cited reason that states **how the non-Linux path behaves** — degrades gracefully, has a fallback, or is unreachable off-Linux'
  - 'Carries the `# guard-layer: source` marker so the T-2684 runner executes it with everything else'
  - 'Exit codes 0 clean / 1 unacknowledged site / 2 tooling; `--json`; `--root` (repeatable) + `--allowlist` for fixtures; `--quiet` / `--no-heartbeat`'
  - 'Comment lines are not flagged (a doc comment mentioning `/proc` is not a dependency on it)'
  - 'Fixture suite `tests/platform-lock-check-fixtures.sh` proves: a `/proc` literal fires, an allowlisted site is suppressed, a comment mention does not fire, a cross-platform command does not fire, a Linux-only command fires, absent root is a tooling error'
  - 'The current tree scans clean with every real site acknowledged and its degradation documented'
  - 'Load-bearing: removing an allowlist entry re-fires that site'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 19706 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 19706
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2693-nothing-detects-a-new-platform-locked-pr.md
generated_by: context-agent-hybrid

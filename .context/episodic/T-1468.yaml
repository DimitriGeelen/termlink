# ============================================================================
# EPISODIC MEMORY - T-1468: fleet doctor --legacy-usage --trend <dir>: multi-snapshot decay sparkline
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-04T07:04:53Z
# ============================================================================

task_id: T-1468
task_name: "fleet doctor --legacy-usage --trend <dir>: multi-snapshot decay sparkline"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-04T06:49:43Z
completed: 2026-05-04T06:59:49Z
duration_days: 0
updates_count: 2
first_commit: 2026-05-04T08:59:45+02:00
last_commit: 2026-05-04T08:59:45+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fleet doctor --trend — multi-snapshot decay sparkline

# Key outcomes
outcomes:
  - "New flag `--trend <PATH>` on `fleet doctor` (requires `--legacy-usage`; bails otherwise, matching `--diff`'s validation pattern)"
  - "New flag `--trend-keep <N>` to limit how many snapshots are read (default 7, max 30 — capped to avoid pathological dirs)"
  - "Pure helper `compute_legacy_trend(snapshots: &[(String, &serde_json::Value)]) -> Vec<TrendPoint>` — TrendPoint = (snapshot_label, ts_ms, total, delta_from_prior_signed)"
  - "Pure helper `render_sparkline(values: &[u64]) -> String` — Unicode block sparkline (▁▂▃▄▅▆▇█), normalized to max in series, returns empty string for empty/single-value input"
  - "Human-mode block prints: `Trend (last N snapshots):` table + sparkline + verdict-trajectory line (\"decreasing / flat / increasing\")"
  - "JSON-mode adds `trend: [...]` to `legacy_summary`"
  - "At least 6 unit tests covering: empty trend, single-snapshot trend, monotonic decrease (decreasing trajectory), monotonic increase, plateau (flat), sparkline normalization across uniform / single-spike / empty inputs"
  - "`cargo build --release -p termlink` succeeds"
  - "`cargo test --release -p termlink --bins legacy_trend` passes (≥6 tests)"
  - "Live verification: `target/release/termlink fleet doctor --legacy-usage --trend /tmp/trend-fixture/` produces the expected decreasing trajectory against a hand-built 3-snapshot fixture"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-cli/src/cli.rs"
  - "crates/termlink-cli/src/commands/remote.rs"
  - "crates/termlink-cli/src/main.rs"
  - "docs/migrations/T-1166-retire-legacy-primitives.md"
  - ".tasks/active/T-1468-fleet-doctor---legacy-usage---trend-dir-.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-04T08:59:45"
    action: "T-1468: fleet doctor --trend — multi-snapshot decay sparkline"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 10 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 10
  commits: 1
  files_changed: 5
  lines_added: 528
  lines_removed: 2

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1468-fleet-doctor---legacy-usage---trend-dir-.md
generated_by: context-agent-hybrid

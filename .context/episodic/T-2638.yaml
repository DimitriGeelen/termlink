# ============================================================================
# EPISODIC MEMORY - T-2638: agent find-idle --watch RPC is unbounded — wedges forever on half-open hub (adopt timeout like fetch_dispatch sibling)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-12T12:36:25Z
# ============================================================================

task_id: T-2638
task_name: "agent find-idle --watch RPC is unbounded — wedges forever on half-open hub (adopt timeout like fetch_dispatch sibling)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-12T12:30:00Z
completed: 2026-08-12T12:35:14Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-12T14:35:10+02:00
last_commit: 2026-08-12T14:35:10+02:00

# Summary (auto-generated from git commit messages)
summary: |
  bound agent find-idle --watch RPC — no longer wedges on half-open hub (divergence F1)

# Key outcomes
outcomes:
  - 'The `find-idle --watch` loop''s `AGENT_FIND_IDLE` RPC is bounded by a finite read timeout (mirroring `substrate::fetch_dispatch`''s `tokio::time::timeout`); an expiry is routed to the existing "fetch error (will retry on next tick)" path (`current_state = None`), so a half-open hub causes a retryable tick, never a permanent freeze. — loop now calls `bounded_watch_fetch(rpc, Duration::from_secs(interval))`; the `Err(e)` arm is the existing fetch-error path.'
  - 'The bound is extracted into a small async helper so it is hermetically testable without a live hub (a never-resolving future stands in for a half-open hub). — `bounded_watch_fetch<T,E,F>` generic over the RPC future.'
  - 'A load-bearing test proves a never-replying RPC returns a fetch error within the bound (not a hang); reverting the helper to an unbounded `.await` makes the test fail (outer test-guard trips). A second test proves a prompt `Ok` passes through unchanged. — `bounded_watch_fetch_bounds_a_wedged_rpc` (proven via temp-revert: unbounded `.await` → outer 5s guard trips → assertion fails) + `bounded_watch_fetch_passes_through_prompt_ok`.'
  - '`cargo test -p termlink` (relevant filter) green; `cargo build -p termlink` succeeds. — `agent_find_idle::tests` 18 passed; `cargo build -p termlink` Finished.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-cli/src/commands/agent_find_idle.rs'
  - '.tasks/active/T-2637-round-6-charter-lens-hunt--divergence-cl.md'
  - '.tasks/active/T-2638-agent-find-idle---watch-rpc-is-unbounded.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-12T14:35:10"
    action: "T-2638: bound agent find-idle --watch RPC — no longer wedges on half-open hub (divergence F1)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 1
  files_changed: 3
  lines_added: 470
  lines_removed: 4

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2638-agent-find-idle---watch-rpc-is-unbounded.md
generated_by: context-agent-hybrid

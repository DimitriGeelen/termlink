# ============================================================================
# EPISODIC MEMORY - T-1843: fleet-adoption-snapshot.sh — measure REAL doorbell+mail traffic (distinct from T-1831 health canary)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-28T17:59:28Z
# ============================================================================

task_id: T-1843
task_name: "fleet-adoption-snapshot.sh — measure REAL doorbell+mail traffic (distinct from T-1831 health canary)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-28T17:29:47Z
completed: 2026-05-28T17:58:59Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-28T19:58:46+02:00
last_commit: 2026-05-28T19:58:46+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fleet-adoption-snapshot.sh — distinct gauge for REAL doorbell+mail traffic

# Key outcomes
outcomes:
  - "`scripts/fleet-adoption-snapshot.sh` exists, executable, `--help` exits 0 with usage"
  - "Walks every profile in `~/.termlink/hubs.toml` (same source as T-1837 / canary)"
  - "For each hub measures: `live_listeners` (via agent-presence heartbeats), `dm_topic_count` (lifetime count of `dm:*:*` topics with ≥1 post), `chat_arc_posts` (windowed count on `agent-chat-arc`)"
  - "`--since <hours>` window (default 24, clamp 1..=720)"
  - "`--json` flag emits one envelope `{ok, window_hours, summary:{hubs, reachable_hubs, live_listeners, chat_arc_posts, dm_topics_active, adoption_state}, profiles:[...]}` parseable by jq"
  - "Human (default) format prints a one-screen summary + per-hub rows"
  - "`adoption_state` field: `HOT` (≥1 chat_arc post in window AND ≥1 live listener), `WARM` (≥1 live listener but no chat_arc activity), `COLD` (zero live listeners fleet-wide)"
  - "G-060 graceful: missing `agent-presence` / `agent-chat-arc` topic on a hub returns zeros for that hub, not an error (handles -32013, \"unknown topic\", and \"Topic not found\")"
  - "Unreachable hub (network error) returns `verdict:\"unreachable\"` for that hub but does not fail the overall sweep (exit 0 still)"
  - "Exit codes: 0 = sweep completed (any adoption_state), 2 = usage error, 3 = setup-fail (hubs.toml missing / jq missing)"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "scripts/fleet-adoption-snapshot.sh"
  - "scripts/test-fleet-adoption-snapshot.sh"
  - ".tasks/active/T-1843-fleet-adoption-snapshotsh--measure-real-.md"
  - ".tasks/completed/T-1844-agent-listenerssh-subscribe-from-tail--f.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-28T19:58:46"
    action: "T-1843: fleet-adoption-snapshot.sh — distinct gauge for REAL doorbell+mail traffic"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 29 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 29
  commits: 1
  files_changed: 4
  lines_added: 581
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1843-fleet-adoption-snapshotsh--measure-real-.md
generated_by: context-agent-hybrid

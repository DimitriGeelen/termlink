# ============================================================================
# EPISODIC MEMORY - T-1729: termlink_agent_inbox MCP — cross-topic unread digest parity with CLI (T-1553)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-20T19:50:38Z
# ============================================================================

task_id: T-1729
task_name: "termlink_agent_inbox MCP — cross-topic unread digest parity with CLI (T-1553)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-20T19:37:37Z
completed: 2026-05-20T19:45:54Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-20T21:45:50+02:00
last_commit: 2026-05-20T21:45:50+02:00

# Summary (auto-generated from git commit messages)
summary: |
  termlink_agent_inbox MCP — cross-topic unread digest

# Key outcomes
outcomes:
  - "`AgentInboxParams` struct defined in `crates/termlink-mcp/src/tools.rs` — parameter-less (mirrors CLI which only has `--hub`/`--json`; MCP defaults to local hub, JSON-only output). Doc-comment cites T-1553 (CLI parity) + T-1729."
  - "Pure helper `cursor_list_for_fingerprint_mcp(fingerprint: &str) -> Result<Vec<(String, u64)>, String>` ported into tools.rs — reads `${TERMLINK_IDENTITY_DIR:-${HOME}/.termlink}/cursors.json`, parses BTreeMap<String, u64>, filters keys ending in `::<fingerprint>`, strips suffix, sort by topic asc. Mirrors `cursor_store::list_for_fingerprint` (channel.rs:592) one-to-one. Defensive: missing file → empty vec (matches CLI), invalid JSON → error string."
  - "Pure helper `compute_unread_rows_mcp(cursors, topic_counts) -> Vec<UnreadRowMcp>` ported into tools.rs — mirrors `compute_unread_rows` (channel.rs:7422) one-to-one: drops topics missing from counts, drops count==0, drops cursor>=latest, sorts by descending `unread` then ascending `topic`."
  - "`termlink_agent_inbox` tool method registered via `#[tool(name = \"termlink_agent_inbox\", ...)]`. Flow: hub_socket_path check → load identity (HOME-based, matches T-1719) → `cursor_list_for_fingerprint_mcp(my_id)` → empty cursors → return `{ok, dms: []}` early-out → `channel.list` RPC → extract `(name, count)` counts map → `compute_unread_rows_mcp` → return `{ok, my_id, unread_topics: [{topic, cursor, latest, unread}, ...]}`. NO new RPC surface — only `channel.list`."
  - "Tool description cites T-1553 (CLI parity) + T-1719 (sibling DM-directory pattern), explains the cursor-store dependency (cursors recorded by `subscribe --resume`), and distinguishes single-topic-walk verbs (`agent unread`, `agent dms`) from this cross-topic digest."
  - "`cargo build --release -p termlink-mcp` clean — only the pre-existing `cur_run_end` warning."
  - "**≥4** new unit tests added under `tools::tests`: (a) `compute_unread_rows_mcp` empty cursors → []; (b) caller caught up (cursor==latest) → topic dropped; (c) caller behind → row emitted with correct unread+latest; (d) sort: high-unread topic before low-unread, alpha tie-break; (e) `cursor_list_for_fingerprint_mcp` missing file → Ok(empty); (f) params deserialize from `{}`. All pass."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-mcp/src/tools.rs"
  - ".tasks/active/T-1729-termlinkagentinbox-mcp--cross-topic-unre.md"
  - ".tasks/completed/T-1719-termlinkagentdms-mcp--dm-topic-directory.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-20T21:45:50"
    action: "T-1729: termlink_agent_inbox MCP — cross-topic unread digest"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 1
  files_changed: 3
  lines_added: 427
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1729-termlinkagentinbox-mcp--cross-topic-unre.md
generated_by: context-agent-hybrid

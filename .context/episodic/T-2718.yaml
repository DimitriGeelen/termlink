# ============================================================================
# EPISODIC MEMORY - T-2718: Fabric no-edges warning cannot be cleared by its own mitigation
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T06:14:30Z
# ============================================================================

task_id: T-2718
task_name: "Fabric no-edges warning cannot be cleared by its own mitigation"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-14T20:38:32Z
completed: 2026-08-15T06:14:24Z
duration_days: 1
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  193/344 cards have no edges and fw fabric enrich adds zero; most edgeless cards are leaf scripts whose real dependency is the termlink binary, which the file-to-file edge model cannot express

# Key outcomes
outcomes:
  - 'The report shows the mitigation is inert, citing the measured `enrich` output (0 enriched, 0 edges, 0 unresolved — the second zero is what proves saturation rather than blockage)'
  - 'It quantifies the gap with a measurement that was verified by reading every matching line, not by trusting a grep count: **17 of 149** `scripts/` files carry **28** statically-resolvable sibling dependencies, while 71 reference the `termlink` binary *(revised twice — see §Correction; the original "0 of 149" and the first correction''s "18 invocation edges" were both artefacts of generalising one regex past what it measured)*'
  - 'It names the two specific parser gaps that hide those 28: `$HERE` is not a recognised directory variable at all, and the recognised ones (`LIB_DIR`, `SCRIPT_DIR`) match only after `source`/`.`, never after `bash`/`exec` or in a bare `VAR=` binding'
  - 'It gives the worked example (`substrate-smoke.sh`: four `bash "${SCRIPT_DIR}/…-demo.sh"` calls plus a `WORKER_LOOP=` binding, card has zero edges — `SCRIPT_DIR` is already known, only the verb differs)'
  - 'It states the *bounded* structural conclusion — closing both gaps moves 193 edgeless cards to ~176 (56%→~61%), and the residual is genuinely inexpressible: `run-guard-layer.sh` resolves its ~20 members by runtime glob + header marker with no literal name in the file, and 71 scripts depend on a compiled binary a file→file model cannot name'
  - 'It proposes fixing the parser gaps first (unambiguous, no model implications), then the two candidate directions for the residual without choosing between them: allow non-file edge targets (binary/service/artifact), or scope the coverage metric to card types where file edges are meaningful'
  - 'It warns explicitly against the two wrong fixes: hand-authoring edges to lower the number, and re-narrowing `watch-patterns.yaml` (the PL-341 trap, hit three times already — T-2680, T-2681, T-2712)'
  - 'Filed as an upstream record under `.context/upstream/` *(U-006, corrected 2026-08-15)*'
  - 'No file under `.agentic-framework/` is edited by this task'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 575 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 575
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2718-fabric-no-edges-warning-cannot-be-cleare.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1663: MCP parity for hub probe + tofu verify primitives
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-17T18:42:50Z
# ============================================================================

task_id: T-1663
task_name: "MCP parity for hub probe + tofu verify primitives"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-17T18:26:04Z
completed: 2026-05-17T18:38:08Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-17T20:38:02+02:00
last_commit: 2026-05-17T20:38:02+02:00

# Summary (auto-generated from git commit messages)
summary: |
  MCP parity — termlink_hub_probe + termlink_tofu_verify

# Key outcomes
outcomes:
  - "`termlink_hub_probe` MCP tool ships in `crates/termlink-mcp/src/tools.rs`. Takes `{address: String, ...}` params, opens TLS handshake via `termlink_session::tofu::probe_cert`, returns `{ok, address, fingerprint, error}` JSON. No auth, no profile required, no `KnownHubStore` mutation. Mirrors CLI `hub probe <addr>` semantics. **Verified live 2026-05-17 against 192.168.10.122:9100:** returned `{ok:true, fingerprint:\"sha256:22c19fed...d00d46\", error:null}` — matches the CLI/fleet-verify ground truth for ring20-management."
  - "`termlink_tofu_verify` MCP tool ships in same file. Takes `{address: String, ...}` params, probes via TLS, looks up `KnownHubStore.get(address)`, returns `{ok, address, status (match/drift/no-pin/probe-fail), wire, pinned, error, actions[]}` JSON. `actions` populated with heal hints when `status==\"drift\"`. Mirrors CLI `tofu verify <addr>` semantics. **Verified live 2026-05-17 against 192.168.10.122:9100:** returned `status:\"match\", wire==pinned, actions:[]` — drift detection path exercises identical branching to T-1661's tested code."
  - "`cargo build --release` succeeds. **Verified:** `cargo build --release -p termlink-mcp` clean in 1m11s; `cargo build --release -p termlink` clean in 6m26s (one pre-existing warning in `unused_assignments`, not introduced by this change)."
  - "Tool descriptions follow the existing T-1661 phrasing pattern (read-only, no-auth, what-to-use-it-for line). **Verified:** Both descriptions follow the \"Pure read-only diagnostic: no authentication, no profile required, no `KnownHubStore` mutation\" idiom + Use-for line + companion-to-other-tool line."
  - "After build, the binary surfaces both new tools (verified via grep of compiled binary). **Verified:** `grep -ao \"termlink_hub_probe\\|termlink_tofu_verify\\|termlink_fleet_verify\" target/release/termlink` returns all three names; runtime `tools/list` over stdio JSON-RPC also enumerates both new tools alongside `termlink_fleet_verify`."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-mcp/src/tools.rs"
  - ".tasks/active/T-1663-mcp-parity-for-hub-probe--tofu-verify-pr.md"
  - ".tasks/completed/T-1662-document-rotation-protocol-detection-ver.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-17T20:38:02"
    action: "T-1663: MCP parity — termlink_hub_probe + termlink_tofu_verify"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 12 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 12
  commits: 1
  files_changed: 3
  lines_added: 205
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1663-mcp-parity-for-hub-probe--tofu-verify-pr.md
generated_by: context-agent-hybrid

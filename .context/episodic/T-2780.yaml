# ============================================================================
# EPISODIC MEMORY - T-2780: session-selftest leaks one tmux session per run — cleanup deregisters but never kills the PTY
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-17T07:44:18Z
# ============================================================================

task_id: T-2780
task_name: "session-selftest leaks one tmux session per run — cleanup deregisters but never kills the PTY"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-17T06:44:21Z
completed: 2026-08-17T07:44:11Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  session-selftest.sh CLEANUP sends 'signal TERM' + 'clean', which removes the termlink registration but leaves the backing tmux session alive. Measured: 7 orphaned tl-session-selftest-*-pty sessions after 7 live runs, 0 present in 'termlink list'. T-2695 AC 5 claims cleanup on every exit path; it is not satisfied. The T-2557 canary runs this prover daily, so the leak is unbounded.

# Key outcomes
outcomes:
  - 'The leak is **reproduced first** in isolation — spawn a PTY session the way the prover does, run the prover''s exact cleanup, and show the tmux session survives — so the fix is aimed at a measured mechanism rather than a guessed one'
  - 'CLEANUP reaps the backing tmux session, not just the termlink registration: after a full live run, `tmux ls` shows no `tl-session-selftest-*` session'
  - 'The reap is scoped to this prover''s own session name; it must never match or kill an unrelated `tl-*` session belonging to real work'
  - 'Cleanup stays best-effort and never fatal: a reap failure must not turn a PASSing prover into a FAIL (exit-code contract 0 proven / 1 broken / 2 tooling is unchanged)'
  - 'The non-PTY `sleep`-backed session''s existing handling is unchanged — no regression to the stages the T-2557 canary already depends on'
  - 'Reaping happens on **every** exit path, including when a PTY stage FAILS — that is what T-2695 AC 5 claimed and is the property that was actually missing — **MEASURED 2026-08-17, no longer inferred**: a pass-through shim on the `TERMLINK_BIN` seam forced the live-path OUTPUT stage to fail deterministically. Result `rc=1`, `broken_stage:"OUTPUT"`, `pty_spawn:"PASS"` (so a real tmux session genuinely existed), `cleanup:"done"` — and **0 PTY orphans afterwards**. The failure path reaps.'
  - 'Load-bearing, proven by mutation: removing the reap makes the leak assertion fail; restoring it passes — **RAN 2026-08-17**. With the reap replaced by a no-op: **1 PTY orphan, and the prover still exited 0**. That is the whole defect in one line — success reported over a leak (Directive #2). Restored: `git diff --stat` clean, 0 orphans.'
  - '`bash scripts/session-selftest.sh --json` still returns `proven:true` with all seven stages (**verified — rc=0, 7/7 stages**), and `bash scripts/test-session-selftest.sh` still passes (**re-run 2026-08-17 — 23/23 PASS**; the earlier "the suite cannot reach it" was reasoning, and this task exists because reasoning was accepted in place of a measurement once already)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 59 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 59
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2780-session-selftest-leaks-one-tmux-session-.md
generated_by: context-agent-hybrid

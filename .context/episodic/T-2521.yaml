# ============================================================================
# EPISODIC MEMORY - T-2521: KV watch deadlock: event.subscribe holds session read guard across long-poll, blocking kv.set/kv.delete
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-04T10:13:28Z
# ============================================================================

task_id: T-2521
task_name: "KV watch deadlock: event.subscribe holds session read guard across long-poll, blocking kv.set/kv.delete"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-04T10:07:32Z
completed: 2026-08-04T10:13:22Z
duration_days: 0
updates_count: 2
first_commit: 2026-08-04T12:12:23+02:00
last_commit: 2026-08-04T12:13:16+02:00

# Summary (auto-generated from git commit messages)
summary: |
  dispatch event.subscribe detached from session lock — fixes KV-watch deadlock + kv.set write-DoS (campaign firing #40). tick final AC — hub release build exit 0, 403 session-lib tests pass

# Key outcomes
outcomes:
  - 'The post-auth lock-scoping branch is extracted into a testable `handler::dispatch_scoped(session, req)` with three arms: write-scoped (`needs_write`), detached long-poll (`event.subscribe`), and read-scoped (default); `server.rs` calls it.'
  - '`event.subscribe` is dispatched detached: the session read guard is released (only the `Arc<Mutex<EventBus>>` is retained) before the long-poll wait, so a concurrent `kv.set`/`kv.delete` can acquire the write lock and emit while the watch waits.'
  - 'A regression test `event_subscribe_does_not_block_concurrent_kv_set` drives a live subscribe + a concurrent `kv.set` through `dispatch_scoped` and asserts (a) the subscribe receives the `kv.change` (count==1) and (b) the `kv.set` completes well under the watch timeout. Proven load-bearing (FAILS when the detached arm is reverted to the read-guarded path).'
  - '`cargo test -p termlink-session --lib` passes (403 tests); `cargo build --release -p termlink-hub` succeeds (exit 0).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'dispatch event.subscribe detached from session lock — fixes KV-watch deadlock + kv.set write-DoS (campaign firing #40)'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-session/src/handler.rs'
  - 'crates/termlink-session/src/server.rs'
  - '.tasks/active/T-2521-kv-watch-deadlock-eventsubscribe-holds-s.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-04T12:12:23"
    action: "T-2521: dispatch event.subscribe detached from session lock — fixes KV-watch deadlock + kv.set write-DoS (campaign firing #40)"
  - time: "2026-08-04T12:13:16"
    action: "T-2521: tick final AC — hub release build exit 0, 403 session-lib tests pass"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 2
  files_changed: 3
  lines_added: 322
  lines_removed: 12

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2521-kv-watch-deadlock-eventsubscribe-holds-s.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2738: Alt-screen detection misses the ?47 and ?1047 variants — a session reports normal screen while in alt-screen
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T14:08:10Z
# ============================================================================

task_id: T-2738
task_name: "Alt-screen detection misses the ?47 and ?1047 variants — a session reports normal screen while in alt-screen"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-15T13:59:44Z
completed: 2026-08-15T14:06:38Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Alt-screen detection misses the ?47 and ?1047 variants — a session reports normal screen while in alt-screen

# Key outcomes
outcomes:
  - '`scan_alternate_screen` recognises all six sequences: `?1049h/l`, `?1047h/l`, `?47h/l`, driven from one table rather than open-coded pairs'
  - 'The scan handles variable-length sequences; the carry is sized from the longest sequence, not from a hardcoded 8'
  - 'The no-double-count invariant is enforced explicitly — a match is only applied if it extends past the carry into the new chunk — rather than being implied by a fixed window length'
  - 'The doc comment states the invariant the code enforces; the stale "no 8-byte window can lie entirely within it" argument is gone'
  - 'Tests cover the `?47h` → `?47l` toggle'
  - 'Tests cover the `?1047h` → `?1047l` toggle'
  - 'A split-read test covers the 6-byte variant specifically (the length the old carry logic could not have handled)'
  - 'A negative test proves `\x1b[?1048h` (save-cursor, not alt-screen) does not toggle the flag — the scanner discriminates rather than matching anything `?10..`'
  - 'An observable test proves a sequence sitting entirely inside the carry is not re-applied on the next chunk'
  - 'Load-bearing proof recorded for both halves (sequence table and carry invariant); `cargo test -p termlink-session` green and `scripts/run-guard-layer.sh` clean'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 6 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 6
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2738-alt-screen-detection-misses-the-47-and-1.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2355: Hub record-walk starvation: channel.subscribe/receipts blocking-I/O wedges under load (T-2258 class, ring20 G-157 seat)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-04T14:53:32Z
# ============================================================================

task_id: T-2355
task_name: "Hub record-walk starvation: channel.subscribe/receipts blocking-I/O wedges under load (T-2258 class, ring20 G-157 seat)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-04T13:45:10Z
completed: 2026-07-04T14:52:31Z
duration_days: 0
updates_count: 3
first_commit: 2026-07-04T15:45:25+02:00
last_commit: 2026-07-04T16:52:07+02:00

# Summary (auto-generated from git commit messages)
summary: |
  filed — hub record-walk starvation (T-2258 class, ring20 G-157 seat); client-side bound shipped in T-2354. server-side walk deadline — WALK_DEADLINE_EXCEEDED (-32020) bounds channel.subscribe/receipts record walks (T-2258 starvation class, ring20 G-157 seat)

# Key outcomes
outcomes:
  - '`error_code::WALK_DEADLINE_EXCEEDED = -32020` registered in termlink-protocol control.rs with doc comment + stability test assert (`walk_deadline_exceeded_const_is_stable_wire_value`)'
  - '`channel.subscribe` walk stops at the deadline and returns -32020 with `data.next_cursor` = first unwalked offset (resumable), `data.records_scanned`, `data.deadline_ms` — deadline checked at top-of-loop BEFORE processing, so `last_offset` covers only fully-processed records and the resume cursor never skips one'
  - '`channel.receipts` walk stops at the deadline and returns -32020 (no partial receipt map — partial aggregate would silently under-report `up_to` marks; data carries deadline_ms + records_scanned only)'
  - 'Deadline read from `TERMLINK_WALK_DEADLINE_MS` (default 20000ms, clamped 100..=600000); walk logic factored into `walk_subscribe_records` / `walk_receipt_records` taking `deadline: Duration`'
  - 'Unit tests (6 new): zero-deadline hits before any record (both walks); generous deadline completes full topic identically (regression seam); mid-walk deadline preserves resume cursor (deterministic slow-iter); receipts latest-per-sender aggregation unchanged; env default/clamp/garbage handling'
  - '`cargo test -p termlink-hub` (403 passed, 0 failed) and `cargo test -p termlink-protocol` (102 passed, 0 failed) pass; `cargo check --workspace` clean'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']
  - decision: '2026-07-04 — deadline mechanism (filing option 2 over 1/3)'
    chose: 'per-walk elapsed-time deadline checked at top of each iteration, explicit structured error on expiry'
    rationale: 'bounds blocking-thread occupancy (the actual starvation resource) with ~ns per-record overhead vs µs per-record file I/O; smallest diff; testable as a pure function'
    alternatives_rejected: ['chunked/yielding walk (option 1 — blocking-pool threads don''t need scheduler yielding, added complexity buys nothing once occupancy is bounded); async file I/O (option 3 — full rewrite of the bus reader layer for the same operator-visible outcome)']
  - decision: '2026-07-04 — explicit error over partial-result page'
    chose: '`WALK_DEADLINE_EXCEEDED` (-32020) error with `data.next_cursor`, never a truncated success page'
    rationale: 'legacy pagers treat a short page as end-of-topic — a deadline-truncated success would silently under-count (`channel info`/`unread` would report wrong totals with no signal). Loud refusal with a resume cursor keeps Reliability ("no silent failures") and still enables incremental progress for callers that opt in'
    alternatives_rejected: ['`truncated: true` flag on a success page (old clients ignore unknown fields → silent wrong data, the worst failure mode)']
  - decision: '2026-07-04 — 20s default, clamp 100..=600000'
    chose: 'default 20_000ms, strictly under the T-2354 client read-timeout default (30s)'
    rationale: 'layered deadlines — the server must refuse BEFORE the client gives up, so the operator sees the server''s structured, actionable error instead of an opaque client timeout'
    alternatives_rejected: ['default ≥ client timeout (server error would never reach the client on default configs)']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/channel.rs'
  - 'crates/termlink-protocol/src/control.rs'
  - '.tasks/active/T-2355-hub-record-walk-starvation-channelsubscr.md'
  - '.tasks/completed/T-2356-install-release-cli-with-t-23525354-fixe.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-04T15:45:25"
    action: "T-2355: filed — hub record-walk starvation (T-2258 class, ring20 G-157 seat); client-side bound shipped in T-2354"
  - time: "2026-07-04T16:52:07"
    action: "T-2355: server-side walk deadline — WALK_DEADLINE_EXCEEDED (-32020) bounds channel.subscribe/receipts record walks (T-2258 starvation class, ring20 G-157 seat)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 67 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 67
  commits: 2
  files_changed: 4
  lines_added: 598
  lines_removed: 78

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2355-hub-record-walk-starvation-channelsubscr.md
generated_by: context-agent-hybrid

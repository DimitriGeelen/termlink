# ============================================================================
# EPISODIC MEMORY - T-2526: termlink_dispatch count unbounded — u32 pre-allocates ~100GB Vec, OOMs MCP server before spawn
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-04T11:25:45Z
# ============================================================================

task_id: T-2526
task_name: "termlink_dispatch count unbounded — u32 pre-allocates ~100GB Vec, OOMs MCP server before spawn"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-04T11:20:35Z
completed: 2026-08-04T11:25:08Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-04T13:25:02+02:00
last_commit: 2026-08-04T13:25:02+02:00

# Summary (auto-generated from git commit messages)
summary: |
  bound termlink_dispatch count — reject >256 before ~100GB Vec pre-alloc (MCP OOM DoS)

# Key outcomes
outcomes:
  - 'Pure helper `validate_dispatch_count(count: u32) -> Option<String>` returns an error message for `count == 0` OR `count > MAX_DISPATCH_COUNT` (256), `None` otherwise'
  - '`termlink_dispatch` calls the helper and returns its message via `json_err` before reaching the `Vec::with_capacity(count)` / `vec![false; count]` allocations — no remaining unbounded `count` reaching those sinks'
  - 'Unit test asserts `0`, `257`, and `u32::MAX` are rejected and `1`/`256` accepted; proven load-bearing via temp-revert (dropping the upper-bound branch makes the `u32::MAX` assertion FAIL)'
  - '`cargo test -p termlink-mcp` passes; `cargo build -p termlink-mcp` clean'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-mcp/src/tools.rs'
  - '.tasks/active/T-2526-termlinkdispatch-count-unbounded--u32-pr.md'
  - 'VERSION'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-04T13:25:02"
    action: "T-2526: bound termlink_dispatch count — reject >256 before ~100GB Vec pre-alloc (MCP OOM DoS)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 1
  files_changed: 3
  lines_added: 233
  lines_removed: 4

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2526-termlinkdispatch-count-unbounded--u32-pr.md
generated_by: context-agent-hybrid

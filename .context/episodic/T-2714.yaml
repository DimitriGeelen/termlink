# ============================================================================
# EPISODIC MEMORY - T-2714: Audit hook checks ignore core.hooksPath — unfixable warning in linked worktrees
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-27T22:00:37Z
# ============================================================================

task_id: T-2714
task_name: "Audit hook checks ignore core.hooksPath — unfixable warning in linked worktrees"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-14T18:57:33Z
completed: 2026-08-27T21:59:19Z
duration_days: 13
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  audit.sh tests PROJECT_ROOT/.git/hooks/* which cannot exist in a worktree, so CTL-011 and the commit-msg check warn forever and install-hooks cannot clear them

# Key outcomes
outcomes:
  - 'The defect is re-verified against CURRENT vendored code, not against this task''s'
  - 'All three previously-broken call sites confirmed migrated: `:2420` (commit-msg),'
  - 'The fix is confirmed BEHAVIOURALLY, not just structurally: the audit now emits'
  - 'The top-level-definition subtlety in the upstream fix is recorded (the resolver must'
  - 'The erroneous filing was retracted. A report was posted to `framework:pickup` at'
  - 'No file under `.agentic-framework/` is edited by this task.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']
  - decision: '2026-08-18 — Rewriting the AC set instead of ticking it'
    chose: 'Replace the original eight ACs with six describing the real outcome'
    rationale: 'The originals all describe filing an upstream report about a defect that is'
    alternatives_rejected: ['(a) Tick as-written and note the fix in Updates — the ACs would then']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 18901 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 18901
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2714-audit-hook-checks-ignore-corehookspath--.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-3072: Urgent flag: bypass the prompt-free wait and inject directly
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-23T17:01:31Z
# ============================================================================

task_id: T-3072
task_name: "Urgent flag: bypass the prompt-free wait and inject directly"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-22T12:54:39Z
completed: 2026-09-23T17:01:15Z
duration_days: 1
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  Spec step 9. Operator: not yet defined, note for later. An urgent message should skip the wait for a free prompt and inject immediately. Needs the flag semantics defined before build.

# Key outcomes
outcomes:
  - '**Urgent shortens the WAIT, never the CHECK** (SQ-4, operator). When the queued'
  - '**It still NEVER injects into BUSY or UNKNOWN.** After the window expires it'
  - 'Urgency is read from the DECLARED priority band and is bounded by the delivered'
  - '**Non-urgent behaviour is unchanged** — one probe, immediate defer. Proven by the'
  - 'The threshold and window are declared and overridable (`INJECTOR_URGENT_THRESHOLD`,'
  - 'Fixtures pin: urgent waits and then injects when the prompt frees; urgent still'
  - 'Mutation-proven — disabling the urgency arm reddens a named fixture, and so does'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 1686 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [arc:arc-011]

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 1686
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-3072-urgent-flag-bypass-the-prompt-free-wait-.md
generated_by: context-agent-hybrid

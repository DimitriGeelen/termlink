# ============================================================================
# EPISODIC MEMORY - T-2494: unread count blind to never-acked offset-0 message (up_to=0 sentinel collision)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-02T09:46:54Z
# ============================================================================

task_id: T-2494
task_name: "unread count blind to never-acked offset-0 message (up_to=0 sentinel collision)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-02T09:37:40Z
completed: 2026-08-02T09:45:28Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-02T11:45:19+02:00
last_commit: 2026-08-02T11:45:19+02:00

# Summary (auto-generated from git commit messages)
summary: |
  unread count fails closed on never-acked offset-0 DM (Option<u64> not 0 sentinel)

# Key outcomes
outcomes:
  - '`count_unread` (CLI) takes `up_to: Option<u64>`; `None` counts every content envelope (never-acked), `Some(b)` skips `off <= b` (wire inclusive semantics unchanged)'
  - '`count_unread_mcp` (MCP mirror) takes the same `Option<u64>` and behaves identically'
  - 'Both CLI callers (`compute_dm_inbox_row`, `cmd_channel_unread`) and the MCP `agent_dms` caller initialize `up_to = None` and set `Some(n)` only when a matching receipt row with a numeric `up_to` is found (malformed `up_to` → `None` → conservative count-all)'
  - 'New regression test: a single `{offset:0, msg_type:"chat"}` envelope with `up_to=None` returns `(1, Some(0))`; with `up_to=Some(0)` returns `(0, None)` — asserted in both CLI and MCP test modules'
  - 'Existing `count_unread` / `count_unread_mcp` tests updated to the `Option` signature and still pass'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.context/audits/2026-08-02.yaml'
  - '.context/project/metrics-history.yaml'
  - '.context/working/.budget-status'
  - '.context/working/.compact-log'
  - '.context/working/focus.yaml'
  - '.context/working/.hook-counter'
  - '.context/working/.loop-detect.json'
  - '.context/working/.pre-compact.last-run'
  - '.context/working/.session-metrics.yaml'
  - '.context/working/.session-start-ts'
  - '.context/working/session.yaml'
  - '.context/working/.tool-counter'
  - 'crates/termlink-cli/src/commands/channel.rs'
  - 'crates/termlink-mcp/src/tools.rs'
  - '.tasks/active/T-1452-revisit-due-scansh-cron--handover-banner.md'
  - '.tasks/active/T-2494-unread-count-blind-to-never-acked-offset.md'
  - '.termlink-task'
  - 'VERSION'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-02T11:45:19"
    action: "T-2494: unread count fails closed on never-acked offset-0 DM (Option<u64> not 0 sentinel)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 7 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 7
  commits: 1
  files_changed: 18
  lines_added: 406
  lines_removed: 115

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2494-unread-count-blind-to-never-acked-offset.md
generated_by: context-agent-hybrid

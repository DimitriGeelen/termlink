# ============================================================================
# EPISODIC MEMORY - T-2743: Prove a backgrounded session survives its launcher disconnecting (SIGHUP)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T19:30:14Z
# ============================================================================

task_id: T-2743
task_name: "Prove a backgrounded session survives its launcher disconnecting (SIGHUP)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-15T19:14:52Z
completed: 2026-08-15T19:28:20Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Prove a backgrounded session survives its launcher disconnecting (SIGHUP)

# Key outcomes
outcomes:
  - 'The detachment property is asserted by a test: a backgrounded child leads its own session (`getsid(pid) == pid`) and does not share the launcher''s session'
  - 'A control test proves the assertion can distinguish — a child spawned without the syscall shares the launcher''s session, which is exactly what the macOS fallback produced (PL-219: an assertion that cannot fail is not a guard)'
  - 'All three spawn sites (`execution.rs`, `dispatch.rs`, `tools.rs`) go through the syscall path — verified by grep, not by fixing the one I happened to read first (PL-344)'
  - 'No `Command::new("setsid")` remains in the product crates, and no bare-`sh -c` fallback remains at those sites'
  - 'A `setsid(2)` failure surfaces as a spawn error rather than a silently non-detached child — the feature fails loud or works, never quietly half-works'
  - 'The three `cmd:setsid` platform-lock allowlist entries are removed because the sites are gone, and `check-platform-lock.sh` is clean with no new acknowledgement added in their place (8 sites scanned → 5, all acknowledged)'
  - 'The stale README §Backends claim about macOS `setsid` daemonization is corrected to match the new behaviour'
  - 'Load-bearing proof recorded: reverting the syscall to the old spawn shape makes the detachment test fail'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 13 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 13
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2743-prove-a-backgrounded-session-survives-it.md
generated_by: context-agent-hybrid

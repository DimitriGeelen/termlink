summary: "Second per-command rollout of --target cross-host routing."

# T-926: termlink status --target — second --target rollout

**Completed:** 2026-04-11
**Type:** build
**Duration:** 2026-04-11 (same day, ~2 min)

## What Was Done
Second per-command rollout of `--target` cross-host routing. Added four new flags (--target, --secret-file, --secret, --scope) to `termlink status` and routed `cmd_status` through `call_session`. Preserved all existing output formats (text, --short, JSON). No new design decisions vs T-925 -- this task proved the helper composes cleanly on a second command. Smoke-tested locally.

## Key Decisions
None -- followed T-925 pattern exactly.

## Patterns Observed
- Confirmed the T-924 helper pattern works cleanly for a second command with no surprises.

## Artifacts
- Modified: `crates/termlink-cli/src/cli.rs` (Status variant)
- Modified: `crates/termlink-cli/src/commands/session.rs` (cmd_status routing)

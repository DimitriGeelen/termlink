# ============================================================================
# EPISODIC MEMORY - T-1872: chat-arc-recent: fallback to cursor-0 subscribe when channel info times out (PL-194 mitigation)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-30T06:50:36Z
# ============================================================================

task_id: T-1872
task_name: "chat-arc-recent: fallback to cursor-0 subscribe when channel info times out (PL-194 mitigation)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-30T06:41:36Z
completed: 2026-05-30T06:49:36Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-30T08:49:31+02:00
last_commit: 2026-05-30T08:49:31+02:00

# Summary (auto-generated from git commit messages)
summary: |
  chat-arc-recent — cursor-0 subscribe fallback when channel info times out (PL-194 mitigation, rescues slow hubs)

# Key outcomes
outcomes:
  - "`scripts/agent-chat-arc-recent.sh` adds a fallback path: when `channel info` exits non-zero AND the error is NOT `-32013|unknown topic|Not found`, attempt `channel subscribe --cursor 0 --limit SCAN_LIMIT --since SINCE_MS` instead of marking the hub failed"
  - "If the fallback subscribe succeeds (rc=0), the hub is counted as `hubs_scanned`, posts are merged into the result, and the hub is NOT in `failed_hubs` (it succeeded, just via fallback)"
  - "If the fallback subscribe ALSO fails (rc != 0), the hub is marked failed as before — reason is `timeout` (rc=124 on subscribe) or `network` (anything else)"
  - "An optional `fallback: true` field is added to scanned hubs that used the fallback path — surfaced in `--json` envelope's `summary.fallback_hubs: [<name>]` array so /pulse can hint \"data may be partial — seek-to-tail unavailable\""
  - "Human format adds one line `  fallback: <name1>, <name2>` when any hub used the fallback path (omitted when zero)"
  - "Verification: on .107 fleet today, running with default scan-limit produces non-empty results for at least one of .121 or .141 (currently both fail silently)"
  - "Existing semantics preserved: hubs that succeed on the first `channel info` path still take the seek-to-tail (PL-188) optimization — no behavior change for fast hubs"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "scripts/agent-chat-arc-recent.sh"
  - ".tasks/active/T-1872-chat-arc-recent-fallback-to-cursor-0-sub.md"
  - ".tasks/completed/T-1871-agent-listeners-fleet-enrich-hubsfailed-.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-30T08:49:31"
    action: "T-1872: chat-arc-recent — cursor-0 subscribe fallback when channel info times out (PL-194 mitigation, rescues slow hubs)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 1
  files_changed: 3
  lines_added: 239
  lines_removed: 19

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1872-chat-arc-recent-fallback-to-cursor-0-sub.md
generated_by: context-agent-hybrid

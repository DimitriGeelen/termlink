# ============================================================================
# EPISODIC MEMORY - T-1318: channel subscribe persistent local cursor (Matrix /sync next_batch analogue)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-27T13:47:26Z
# ============================================================================

task_id: T-1318
task_name: "channel subscribe persistent local cursor (Matrix /sync next_batch analogue)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-27T13:43:51Z
completed: 2026-04-27T13:47:17Z
duration_days: 0
updates_count: 2
first_commit: 2026-04-27T15:47:10+02:00
last_commit: 2026-04-27T15:47:10+02:00

# Summary (auto-generated from git commit messages)
summary: |
  channel subscribe persistent local cursor (Matrix /sync next_batch)

# Key outcomes
outcomes:
  - "CLI `termlink channel subscribe <topic> --resume` reads `~/.termlink/cursors.json` for `(topic, identity_fingerprint)` and starts from that cursor; falls back to `--cursor` value (default 0) if no entry exists"
  - "After a successful subscribe (one-shot or follow loop), writes the latest `next_cursor` back to `~/.termlink/cursors.json` for the same key"
  - "CLI `termlink channel subscribe <topic> --reset` deletes the persisted cursor entry before starting; starts from offset 0 unless `--cursor` overrides"
  - "`--resume` and `--reset` mutually exclusive at clap level (`Usage: ... --resume` error on combined use)"
  - "Cursor file is JSON with shape `{\"<topic>::<fingerprint>\": <offset>, ...}` — single flat map; atomic write via `.tmp` rename"
  - "Existing default behavior unchanged — no flags = no cursor reads or writes (backward compatible)"
  - "CLI build + clippy clean"
  - "Smoke evidence in task file (post 3 → resume → post 1 more → resume shows only the new one → reset → resume from 0)"
  - "Agent-conversations doc gains \"Persistent local cursor (T-1318)\" section AND the deferred T-1317 `--by-sender` note"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-cli/src/cli.rs"
  - "crates/termlink-cli/src/commands/channel.rs"
  - "crates/termlink-cli/src/main.rs"
  - "docs/operations/agent-conversations.md"
  - ".tasks/active/T-1318-channel-subscribe-persistent-local-curso.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-04-27T15:47:10"
    action: "T-1318: channel subscribe persistent local cursor (Matrix /sync next_batch)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 1
  files_changed: 5
  lines_added: 324
  lines_removed: 11

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1318-channel-subscribe-persistent-local-curso.md
generated_by: context-agent-hybrid

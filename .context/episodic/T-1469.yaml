# ============================================================================
# EPISODIC MEMORY - T-1469: cut-readiness-daily.sh: emit --trend + --keep-days rotation
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-04T07:07:52Z
# ============================================================================

task_id: T-1469
task_name: "cut-readiness-daily.sh: emit --trend + --keep-days rotation"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-04T07:05:20Z
completed: 2026-05-04T07:07:51Z
duration_days: 0
updates_count: 2
first_commit: 2026-05-04T09:07:46+02:00
last_commit: 2026-05-04T09:07:46+02:00

# Summary (auto-generated from git commit messages)
summary: |
  cut-readiness-daily.sh — emit --trend + --keep-days rotation

# Key outcomes
outcomes:
  - "`scripts/cut-readiness-daily.sh` invokes `fleet doctor --legacy-usage --save-snapshot $TODAY_PATH --trend $SNAPSHOTS_DIR --exit-code-on-verdict` on routine runs (replaces / supersedes the previous `--diff $PRIOR_PATH` invocation; `--trend` includes the diff via the trailing point)"
  - "First-run path (no prior snapshot) also calls `--trend` (with just today's value, will show single point — graceful)"
  - "`--keep-days N` flag added: deletes `$SNAPSHOTS_DIR/*.json` files where the basename's date is older than N days back from today. Default 90."
  - "`--keep-days 0` disables rotation (escape hatch for operators who archive externally)"
  - "Rotation never prunes today's snapshot (invariant: cutoff date < today, today's basename is lex-greater so the comparison passes)"
  - "Help text (`--help`) lists the new flags with defaults"
  - "Rotation only matches files whose basename parses as `YYYY-MM-DD` — operator-placed notes/ad-hoc snapshots untouched"
  - "Smoke test: tmp dir + 6 fake snapshots spanning old (2026-01-*) and recent (2026-04-30..2026-05-03), `--keep-days 2`. Outcome: 4 pruned (everything older than today − 2d), 2 retained (`2026-05-02`, `2026-05-03`), plus today's = 3 files total."
  - "Bash linting clean: `bash -n scripts/cut-readiness-daily.sh`"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "scripts/cut-readiness-daily.sh"
  - ".tasks/active/T-1469-cut-readiness-dailysh-emit---trend----ke.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-04T09:07:46"
    action: "T-1469: cut-readiness-daily.sh — emit --trend + --keep-days rotation"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 2 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 2
  commits: 1
  files_changed: 2
  lines_added: 176
  lines_removed: 10

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1469-cut-readiness-dailysh-emit---trend----ke.md
generated_by: context-agent-hybrid

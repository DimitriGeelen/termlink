# ============================================================================
# EPISODIC MEMORY - T-2516: Bound WS upgrade handshake (accept_async) in hub — slot-leak DoS sibling of T-2515
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-03T18:44:45Z
# ============================================================================

task_id: T-2516
task_name: "Bound WS upgrade handshake (accept_async) in hub — slot-leak DoS sibling of T-2515"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-03T18:40:48Z
completed: 2026-08-03T18:44:16Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-03T20:44:05+02:00
last_commit: 2026-08-03T20:44:12+02:00

# Summary (auto-generated from git commit messages)
summary: |
  bound WS upgrade handshake (accept_async) in hub — slot-leak DoS sibling of T-2515. tick Agent ACs (fix + load-bearing test verified)

# Key outcomes
outcomes:
  - 'AC1: `tokio_tungstenite::accept_async(stream)` in `handle_ws_connection` (server.rs ~1067) is wrapped in `tokio::time::timeout(conn_handshake_timeout(), ...)` — a stalled WS upgrade can no longer pin the task or its `ConnSlotGuard`.'
  - 'AC2: The timeout arm emits a `tracing::warn!` carrying the unique marker `T-2516 conn-cap DoS guard`; on timeout the stream is dropped and the governor slot is reclaimed on task return.'
  - 'AC3: Regression test `ws_handshake_timeout_reclaims_slot` — raw-TCP accept loop + a client that sends only `''G''` then withholds the rest of the upgrade — asserts the hub closes the connection within the handshake timeout (EOF/reset within 2s). Proven load-bearing: temp-revert to the un-timed `accept_async` makes it hang to its 2s read-timeout and FAIL.'
  - 'AC4: `cargo build --release -p termlink-hub` clean AND the new test passes (`cargo test -p termlink-hub --lib ws_handshake_timeout_reclaims_slot`).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'tick Agent ACs (fix + load-bearing test verified)'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/server.rs'
  - '.tasks/active/T-2516-bound-ws-upgrade-handshake-acceptasync-i.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-03T20:44:05"
    action: "T-2516: bound WS upgrade handshake (accept_async) in hub — slot-leak DoS sibling of T-2515"
  - time: "2026-08-03T20:44:12"
    action: "T-2516: tick Agent ACs (fix + load-bearing test verified)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 2
  files_changed: 2
  lines_added: 343
  lines_removed: 8

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2516-bound-ws-upgrade-handshake-acceptasync-i.md
generated_by: context-agent-hybrid

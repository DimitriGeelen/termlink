# ============================================================================
# EPISODIC MEMORY - T-1840: systemd unit template for listener-heartbeat — persistent agent-presence across reboots (T-1830 follow-up)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-28T13:55:20Z
# ============================================================================

task_id: T-1840
task_name: "systemd unit template for listener-heartbeat — persistent agent-presence across reboots (T-1830 follow-up)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-28T13:53:25Z
completed: 2026-05-28T13:55:18Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - "`systemd-templates/termlink-listener-heartbeat@.service` exists (template unit using `%i` instance specifier for agent_id)"
  - "Pulls config from `/etc/termlink/listener-heartbeat/%i.env` (env file per instance)"
  - "Uses `Type=exec`, `Restart=on-failure`, `RestartSec=10s` (mirror the listener-heartbeat 30s loop cadence — restart faster than reheartbeat)"
  - "Default ExecStart invokes `/opt/termlink/scripts/listener-heartbeat.sh` with env-driven flags (TERMLINK_LH_ROLE, _LISTEN_TOPICS, _PTY_SESSION, _TOPIC, _INTERVAL, _HUB)"
  - "Documents Install steps + a per-instance .env example in `docs/operations/listener-heartbeat-systemd.md`"
  - "systemd-analyze verifies template (script tests it where systemd is available; otherwise grep-based smoke check)"
  - "No path that requires root other than the install (writing under /etc + enabling the unit)"
  - "Update T-1838's doc-section in agent-conversations.md to mark this item shipped"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 1 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 1
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1840-systemd-unit-template-for-listener-heart.md
generated_by: context-agent-hybrid

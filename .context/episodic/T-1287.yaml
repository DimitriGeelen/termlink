# ============================================================================
# EPISODIC MEMORY - T-1287: T-243 dep: Extend channel.post with metadata.conversation_id + metadata.event_type
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-26T10:58:00Z
# ============================================================================

task_id: T-1287
task_name: "T-243 dep: Extend channel.post with metadata.conversation_id + metadata.event_type"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-26T09:32:02Z
completed: 2026-04-26T10:57:58Z
duration_days: 0
updates_count: 3
first_commit: 2026-04-26T11:51:43+02:00
last_commit: 2026-04-26T12:57:53+02:00

# Summary (auto-generated from git commit messages)
summary: |
  Envelope.metadata field + channel.post wiring (partial — subscribe filter pending). Add conversation_id filter to channel.subscribe (T-243 dep)

# Key outcomes
outcomes:
  - "Extend `Envelope` struct with `metadata: BTreeMap<String, String>` — done in envelope.rs, `#[serde(default, skip_serializing_if = \"BTreeMap::is_empty\")]` for backwards-compat"
  - "`handle_channel_post` reads optional `metadata` object (string→string) from params and stores in Envelope — channel.rs"
  - "envelope_to_json serializes metadata back to wire format — channel.rs (omitted for empty metadata to preserve legacy wire format)"
  - "Updated 2 hub-side mirror callers (broadcast, inbox.deposit) with `metadata: Default::default()` — channel.rs"
  - "Updated bus tests env() helper with empty BTreeMap — lib.rs"
  - "`handle_channel_subscribe` accepts optional `conversation_id` filter — applies BEFORE limit, advances last_offset over skipped records so next_cursor moves past them (channel.rs `handle_channel_subscribe_with`)"
  - "Test: post with metadata, subscribe without filter → metadata intact (`post_with_metadata_round_trips_through_subscribe`)"
  - "Test: post 3 messages with mixed conversation_ids, subscribe with filter → only matching, in offset order, cursor advances past skipped (`subscribe_with_conversation_id_filters_and_advances_cursor`)"
  - "Test: post without metadata → envelope JSON omits metadata key entirely (`subscribe_without_metadata_omits_field_on_wire`)"
  - "cargo test passes for `termlink-bus` (31) and `termlink-hub` (227) — full workspace test suite green"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-bus/src/envelope.rs"
  - "crates/termlink-bus/src/lib.rs"
  - "crates/termlink-hub/src/channel.rs"
  - ".tasks/active/T-1287-t-243-dep-extend-channelpost-with-metada.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-04-26T11:51:43"
    action: "T-1287: Envelope.metadata field + channel.post wiring (partial — subscribe filter pending)"
  - time: "2026-04-26T12:57:53"
    action: "T-1287: Add conversation_id filter to channel.subscribe (T-243 dep)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 85 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [T-243, channel, protocol]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 85
  commits: 2
  files_changed: 4
  lines_added: 229
  lines_removed: 18

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1287-t-243-dep-extend-channelpost-with-metada.md
generated_by: context-agent-hybrid

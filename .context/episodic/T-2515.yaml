# ============================================================================
# EPISODIC MEMORY - T-2515: Bound TLS handshake in hub TCP accept path — slot-leak DoS twin of T-2442
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-03T18:35:10Z
# ============================================================================

task_id: T-2515
task_name: "Bound TLS handshake in hub TCP accept path — slot-leak DoS twin of T-2442"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-03T18:29:13Z
completed: 2026-08-03T18:34:38Z
duration_days: 0
updates_count: 1
first_commit: 2026-08-03T20:34:16+02:00
last_commit: 2026-08-03T20:34:34+02:00

# Summary (auto-generated from git commit messages)
summary: |
  bound TLS handshake in hub TCP accept path — slot-leak DoS twin of T-2442. tick Agent ACs (fix + load-bearing test verified)

# Key outcomes
outcomes:
  - 'AC1: `tls.accept(tcp_stream)` in the accept-loop spawned task (server.rs ~807) is wrapped in `tokio::time::timeout(conn_handshake_timeout(), ...)` — a stalled TLS handshake can no longer pin the task or its `ConnSlotGuard` indefinitely.'
  - 'AC2: The timeout arm emits a `tracing::warn!` carrying the unique marker `T-2515 conn-cap DoS guard`; on timeout the `tcp_stream` is dropped and the governor slot is reclaimed on task return (via `ConnSlotGuard::Drop`).'
  - 'AC3: Regression test `tls_handshake_timeout_reclaims_slot` — TLS acceptor configured + a silent TCP client — asserts the hub closes the connection within the handshake timeout (EOF/reset within 2s). Proven load-bearing: temp-revert to the un-timed `tls.accept()` makes the test hang to its 2s read-timeout and FAIL.'
  - 'AC4: `cargo build --release -p termlink-hub` clean AND the new test passes (`cargo test -p termlink-hub --lib tls_handshake_timeout_reclaims_slot`).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'tick Agent ACs (fix + load-bearing test verified)'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/server.rs'
  - '.tasks/active/T-2515-bound-tls-handshake-in-hub-tcp-accept-pa.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-03T20:34:16"
    action: "T-2515: bound TLS handshake in hub TCP accept path — slot-leak DoS twin of T-2442"
  - time: "2026-08-03T20:34:34"
    action: "T-2515: tick Agent ACs (fix + load-bearing test verified)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 2
  files_changed: 2
  lines_added: 359
  lines_removed: 8

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2515-bound-tls-handshake-in-hub-tcp-accept-pa.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1895: /check-outbox: cross-reference LIVE peer presence inline (T-1457 UX)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-31T10:00:22Z
# ============================================================================

task_id: T-1895
task_name: "/check-outbox: cross-reference LIVE peer presence inline (T-1457 UX)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-31T09:24:51Z
completed: 2026-05-31T10:00:14Z
duration_days: 0
updates_count: 3
first_commit: 2026-05-31T11:36:52+02:00
last_commit: 2026-05-31T11:36:52+02:00

# Summary (auto-generated from git commit messages)
summary: |
  /check-outbox --with-presence — inline [LIVE]/[STALE]/[OFFLINE] markers

# Key outcomes
outcomes:
  - "`scripts/check-outbox.sh` accepts `--with-presence` flag, default off. `--help` documents it (sed range extended from 32 to 36 to include the new line)."
  - "When set: walks hubs.toml profiles to build identity_fp → hub_name map (via `channel info agent-presence` per hub, falling back to `agent-chat-arc`), AND runs `agent-listeners-fleet.sh --include-offline --json` ONCE with `timeout 15`. Failure to fetch either does NOT block render — stderr note + rows render with UNKNOWN."
  - "Presence join: priority LIVE > STALE > OFFLINE; UNKNOWN when peer_fp doesn't resolve to any known hub."
  - "JSON output: each row gains `peer_status` field with one of `\"LIVE\"|\"STALE\"|\"OFFLINE\"|\"UNKNOWN\"`. Without `--with-presence` the field is absent (back-compat)."
  - "Human output: rows include inline `[LIVE]` / `[STALE]` / `[OFFLINE]` marker, UNKNOWN suppressed (blank space prefix maintains column alignment). Smoke: `[OFFLINE] workstation-107-pu dm:9219671e...:d1993c2c... peer=9219671e… unread=21`."
  - "When any row is OFFLINE/UNKNOWN, suggestions tail adds `• /broadcast-chat \"<follow-up>\"   # peer has no LIVE listener — broadcast may be the only path`. Without `--with-presence` the tail instead suggests running `/peers --all` and `/check-outbox --with-presence`."
  - "Smoke evidence: `/check-outbox --fleet --with-presence --json | jq '.topics | group_by(.peer_status)'` → `[{OFFLINE: 2}, {UNKNOWN: 27}]`. The `9219671e` peer (ring20-management host) consistently surfaces with `[OFFLINE]`. The T-1457 case (laptop-141 6604a2af, 5 unread) shows UNKNOWN because the laptop-141 channel info call times out at 8s — the fallback behavior is correct (UNKNOWN is the explicit unresolved state, no false LIVE/OFFLINE)."
  - "`.claude/commands/check-outbox.md` updated with `--with-presence` row + T-1457 canonical-case prose."
  - "[RUBBER-STAMP] Run `/check-outbox --fleet --with-presence` and confirm at least one row shows an `[OFFLINE]` or `[STALE]` inline marker, plus the broadcast hint in the suggestions tail."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - ".claude/commands/check-outbox.md"
  - "scripts/check-outbox.sh"
  - ".tasks/active/T-1895-check-outbox-cross-reference-live-peer-p.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-31T11:36:52"
    action: "T-1895: /check-outbox --with-presence — inline [LIVE]/[STALE]/[OFFLINE] markers"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 35 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 35
  commits: 1
  files_changed: 3
  lines_added: 252
  lines_removed: 3

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1895-check-outbox-cross-reference-live-peer-p.md
generated_by: context-agent-hybrid

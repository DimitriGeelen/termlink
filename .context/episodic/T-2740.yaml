# ============================================================================
# EPISODIC MEMORY - T-2740: Key-encoding table gaps — pin the loud refusal, then widen resolve_key beyond its current subset
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T14:28:54Z
# ============================================================================

task_id: T-2740
task_name: "Key-encoding table gaps — pin the loud refusal, then widen resolve_key beyond its current subset"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-15T14:20:31Z
completed: 2026-08-15T14:27:24Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Key-encoding table gaps — pin the loud refusal, then widen resolve_key beyond its current subset

# Key outcomes
outcomes:
  - 'The loud-refusal property is pinned first: tests assert `resolve_key` returns `None` for an unknown name and that `resolve_key_entry` surfaces it as `Unknown key: {name}` carrying the offending name'
  - 'Every key name that resolves today still resolves to byte-for-byte the same sequence — pinned by a test enumerating the pre-existing table, so widening cannot silently change an existing binding'
  - 'Lookup is case-insensitive, so `Ctrl+a`, `CTRL+A` and `ctrl+A` resolve identically to `Ctrl+A`'
  - 'The missing control codes resolve: Ctrl+I (0x09), Ctrl+J (0x0A), Ctrl+M (0x0D), Ctrl+[ (0x1B), Ctrl+] (0x1D), Ctrl+^ (0x1E), Ctrl+_ (0x1F), and Ctrl+@ / Ctrl+Space (0x00)'
  - 'The aliasing between those and their named equivalents is asserted, not incidental: Ctrl+I == Tab, Ctrl+J == LF, Ctrl+M == Enter, Ctrl+[ == Escape'
  - 'F1–F12 resolve, with F1–F4 as SS3 (`ESC O P/Q/R/S`) and F5–F12 as CSI `<n>~` on the standard non-contiguous numbering (15,17,18,19,20,21,23,24)'
  - 'Shift+Tab (`CSI Z`), PageUp (`CSI 5~`), PageDown (`CSI 6~`) and Insert (`CSI 2~`) resolve'
  - 'Modified arrows and Home/End resolve as `CSI 1;<mod><final>` with the standard modifier arithmetic (1 + shift·1 + alt·2 + ctrl·4), covering combinations, not just single modifiers'
  - 'A negative test proves the widening did not make the matcher permissive: plausible-but-unreal names (e.g. `F13`, `Ctrl+`, `Super+Up`, empty string) still refuse'
  - 'Load-bearing proof recorded; `cargo test -p termlink-session` green and `scripts/run-guard-layer.sh` clean'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 6 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 6
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2740-key-encoding-table-gaps--pin-the-loud-re.md
generated_by: context-agent-hybrid

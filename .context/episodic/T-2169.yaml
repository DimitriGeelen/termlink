# ============================================================================
# EPISODIC MEMORY - T-2169: substrate-systemd-smoke.sh — static-verify regression test for orchestrator + worker templates (T-2165/T-2167 protection)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-11T16:17:40Z
# ============================================================================

task_id: T-2169
task_name: "substrate-systemd-smoke.sh — static-verify regression test for orchestrator + worker templates (T-2165/T-2167 protection)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-11T16:11:06Z
completed: 2026-06-11T16:17:37Z
duration_days: 0
updates_count: 2
first_commit: 2026-06-11T18:17:23+02:00
last_commit: 2026-06-11T18:17:23+02:00

# Summary (auto-generated from git commit messages)
summary: |
  substrate-systemd-smoke.sh — static-verify regression test for orchestrator + worker templates

# Key outcomes
outcomes:
  - '`scripts/substrate-systemd-smoke.sh` exists and is `+x`'
  - 'Runs with no args and exits 0 on a clean tree (both templates valid)'
  - 'Verifies both templates exist at `systemd-templates/termlink-substrate-{orchestrator,worker}@.service`'
  - 'Asserts each template has `[Unit]`, `[Service]`, `[Install]` sections + `Description=`, `Type=exec`, `EnvironmentFile=`, `ExecStart=`, `Restart=on-failure`, `RestartSec=`, `WantedBy=`'
  - 'Asserts each template carries `Environment=TERMLINK_SUBSTRATE_SCRIPT=` AND that the referenced script EXISTS in `scripts/` AND is `+x`'
  - 'Asserts each template carries `Environment=TERMLINK_RUNTIME_DIR=` NOT pointing at `/tmp` (PL-021 prevention contract)'
  - 'Asserts orchestrator hard-wires `--orchestrator-id %i` in ExecStart; worker hard-wires `--worker-id %i`'
  - 'Asserts worker ExecStart has the `TERMLINK_SW_CMD` required-env guard (exit 2 if unset)'
  - 'Runs `systemd-analyze verify` against each template (if available) and skips gracefully if not (e.g. WSL/container without systemd-analyze)'
  - 'Negative test: deliberately mutated template (`Restart=on-failure` → `Restart=no`) fails the smoke with a clear PASS/FAIL line on stderr'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.fabric/components/scripts-substrate-systemd-smoke.yaml'
  - 'scripts/substrate-systemd-smoke.sh'
  - '.tasks/active/T-2169-substrate-systemd-smokesh--static-verify.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-11T18:17:23"
    action: "T-2169: substrate-systemd-smoke.sh — static-verify regression test for orchestrator + worker templates"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 6 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 6
  commits: 1
  files_changed: 3
  lines_added: 523
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2169-substrate-systemd-smokesh--static-verify.md
generated_by: context-agent-hybrid

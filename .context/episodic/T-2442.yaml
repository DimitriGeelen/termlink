# ============================================================================
# EPISODIC MEMORY - T-2442: WS connection idle timeout + hub keepalive ping — close unauthenticated conn-cap DoS
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-21T21:36:51Z
# ============================================================================

task_id: T-2442
task_name: "WS connection idle timeout + hub keepalive ping — close unauthenticated conn-cap DoS"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-21T21:29:12Z
completed: 2026-07-21T21:36:41Z
duration_days: 0
updates_count: 1
first_commit: 2026-07-21T23:36:29+02:00
last_commit: 2026-07-21T23:36:41+02:00

# Summary (auto-generated from git commit messages)
summary: |
  WS/handshake idle timeout + hub keepalive ping — close unauthenticated conn-cap DoS. tick agent ACs — all verified (430 hub tests green)

# Key outcomes
outcomes:
  - '`handle_connection` bounds the first-byte sniff read with a timeout (`TERMLINK_CONN_HANDSHAKE_TIMEOUT_MS`, default 30000, clamped); a silent connection is dropped and its governor slot released.'
  - '`handle_ws_connection` sends a hub-initiated `Ping` on a periodic timer (`TERMLINK_WS_PING_INTERVAL_MS`, default 30000) and drops the connection when no inbound frame has arrived within the idle window (`TERMLINK_WS_IDLE_TIMEOUT_MS`, default 120000); any inbound frame (incl. Pong) resets the idle clock.'
  - 'Integration test: a raw-TCP connection that sends nothing is closed by the hub within the handshake timeout (client observes EOF).'
  - 'Integration test: a WS-upgraded connection that goes silent is closed by the hub within the idle timeout.'
  - '`cargo test -p termlink-hub --lib` green (existing + new tests).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/governor.rs'
  - 'crates/termlink-hub/src/server.rs'
  - '.tasks/active/T-2442-ws-connection-idle-timeout--hub-keepaliv.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-21T23:36:29"
    action: "T-2442: WS/handshake idle timeout + hub keepalive ping — close unauthenticated conn-cap DoS"
  - time: "2026-07-21T23:36:41"
    action: "T-2442: tick agent ACs — all verified (430 hub tests green)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 7 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 7
  commits: 2
  files_changed: 3
  lines_added: 425
  lines_removed: 11

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2442-ws-connection-idle-timeout--hub-keepaliv.md
generated_by: context-agent-hybrid

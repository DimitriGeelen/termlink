# ============================================================================
# EPISODIC MEMORY - T-2137: Wire RateGovernor::evict_idle in hub startup loop — T-2018 §6 #10 memory leak
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-10T18:35:23Z
# ============================================================================

task_id: T-2137
task_name: "Wire RateGovernor::evict_idle in hub startup loop — T-2018 §6 #10 memory leak"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-10T18:30:30Z
completed: 2026-06-10T18:35:18Z
duration_days: 0
updates_count: 2
first_commit: 2026-06-10T20:35:11+02:00
last_commit: 2026-06-10T20:35:11+02:00

# Summary (auto-generated from git commit messages)
summary: |
  wire RateGovernor::evict_idle in hub startup loop — close §6 #10 memory leak

# Key outcomes
outcomes:
  - 'A periodic task in hub startup (`router.rs` hub-start path or `lib.rs`) calls `rate_governor.evict_idle(now_ms, idle_threshold_ms)` every N seconds'
  - 'Default eviction interval: 60s. Default idle threshold: 300_000ms (5 min — 5x the longest realistic rate-limit window so we never evict a still-rate-limited sender)'
  - 'Tunable via env vars `TERMLINK_RATE_EVICT_INTERVAL_SEC` (clamped 5..=3600) + `TERMLINK_RATE_EVICT_IDLE_THRESHOLD_MS` (clamped 1000..=3_600_000) at hub start, mirroring the existing T-2048 governor knob pattern'
  - '`#[allow(dead_code)]` annotation on `evict_idle` is removed (dead-code allow is no longer needed once wired)'
  - '`cargo check -p termlink-hub` passes'
  - 'Existing `governor.rs` unit tests still pass (`cargo test -p termlink-hub governor::tests`)'
  - 'New unit test exercises env-knob clamps (the integration-style test was descoped: spawn_rate_evict_loop is one-line tokio::spawn over the already-tested evict_idle primitive, and forcing it under test would require enabling the tokio test-util cargo feature for a single test — over-scoped per CLAUDE.md "don''t introduce abstractions beyond what the task requires")'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-hub/src/governor.rs'
  - 'crates/termlink-hub/src/server.rs'
  - '.tasks/active/T-2137-wire-rategovernorevictidle-in-hub-startu.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-10T20:35:11"
    action: "T-2137: wire RateGovernor::evict_idle in hub startup loop — close §6 #10 memory leak"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 1
  files_changed: 3
  lines_added: 364
  lines_removed: 5

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2137-wire-rategovernorevictidle-in-hub-startu.md
generated_by: context-agent-hybrid

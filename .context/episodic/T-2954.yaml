# ============================================================================
# EPISODIC MEMORY - T-2954: framework-pickup canary is blind past offset 99 and reports healthy
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-11T19:45:07Z
# ============================================================================

task_id: T-2954
task_name: "framework-pickup canary is blind past offset 99 and reports healthy"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-11T19:39:48Z
completed: 2026-09-11T19:44:58Z
duration_days: 0
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  The pickup canary drains channel subscribe with no --limit, so it sees only the default first 100 envelopes (offsets 0-99) and reports ok:true while 22 newer filings sit unsurfaced. G-063 reproduced inside the guard built to prevent it.

# Key outcomes
outcomes:
  - 'The canary reads the whole window it claims to read: the subscribe call is'
  - '`max_offset` reported by `--json` equals the true maximum offset on the'
  - 'The previously-hidden filings become visible: the canary''s unprocessed set'
  - 'Truncation can never again be silent: if the drain returns exactly the'
  - 'The fixture suite still passes, and gains a case pinning the regression.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [arc:arc-008]

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 5
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2954-framework-pickup-canary-is-blind-past-of.md
generated_by: context-agent-hybrid

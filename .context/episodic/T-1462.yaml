# ============================================================================
# EPISODIC MEMORY - T-1462: fleet doctor --legacy-usage --diff <snapshot> for decay rate measurement
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-04T05:36:37Z
# ============================================================================

task_id: T-1462
task_name: "fleet doctor --legacy-usage --diff <snapshot> for decay rate measurement"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-04T05:18:46Z
completed: 2026-05-04T05:32:53Z
duration_days: 0
updates_count: 2
first_commit: 2026-05-04T07:32:42+02:00
last_commit: 2026-05-04T07:32:42+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fleet doctor --legacy-usage --diff <snapshot>

# Key outcomes
outcomes:
  - "`--diff <path>` flag added to `fleet doctor`; only meaningful when combined with `--legacy-usage`. Conflict with neither error nor side-effect on prior behavior."
  - "`cmd_fleet_doctor` reads `<path>` as JSON; if file missing or unparseable, prints clear error and exits non-zero (does not silently fall through to normal output)."
  - "After current snapshot is computed, prints a `=== T-1166 cut-readiness DIFF vs <basename> ===` block showing: fleet `total_legacy` delta, per-hub count delta (hubs that appear/vanish flagged), per-caller delta from `top_callers_fleet`, and an average rate (calls/min) when both snapshots have a `_snapshot_ts_ms` field embedded."
  - "Snapshot writes (when `--json` is used) include `_snapshot_ts_ms` (millis since epoch) at the top level so future `--diff` runs can compute elapsed time. Schema-additive — older consumers ignore unknown field."
  - "Pure helper `compute_legacy_diff(prior: &Value, current: &Value) -> LegacyDiff` extracted so unit tests cover: (a) clean-to-clean (no diff), (b) decay (count went down), (c) growth (count went up), (d) hub vanished, (e) hub appeared, (f) caller dominance shift."
  - "At least 4 unit tests pass: `cargo test -p termlink --bin termlink legacy_diff` (8/8 ok)"
  - "`cargo build --release -p termlink` succeeds"
  - "`cargo check -p termlink` produces no new warnings introduced by this change"
  - "Integration smoke: write a minimal hand-crafted snapshot to `/tmp/t1462-snap.json`, run `--legacy-usage --diff /tmp/t1462-snap.json` against current fleet, verify the DIFF block prints expected delta direction (synthetic prior produced expected NEW/VANISHED transitions; 4 error paths all exit non-zero with clear messages)"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-cli/src/cli.rs"
  - "crates/termlink-cli/src/commands/remote.rs"
  - "crates/termlink-cli/src/main.rs"
  - ".tasks/active/T-1462-fleet-doctor---legacy-usage---diff-snaps.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-04T07:32:42"
    action: "T-1462: fleet doctor --legacy-usage --diff <snapshot>"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 14 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 14
  commits: 1
  files_changed: 4
  lines_added: 568
  lines_removed: 2

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1462-fleet-doctor---legacy-usage---diff-snaps.md
generated_by: context-agent-hybrid

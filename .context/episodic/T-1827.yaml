# ============================================================================
# EPISODIC MEMORY - T-1827: agent-conversation-list.sh — enumerate cids on a topic with per-cid roll-up
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-28T09:50:01Z
# ============================================================================

task_id: T-1827
task_name: "agent-conversation-list.sh — enumerate cids on a topic with per-cid roll-up"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-28T09:46:16Z
completed: 2026-05-28T09:49:59Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-28T11:49:53+02:00
last_commit: 2026-05-28T11:49:53+02:00

# Summary (auto-generated from git commit messages)
summary: |
  agent-conversation-list.sh — enumerate cids on a topic (a2a orchestration)

# Key outcomes
outcomes:
  - "`scripts/agent-conversation-list.sh` exists, executable, `bash -n` + `shellcheck` clean."
  - "Accepts `--topic <T>` (required) + `--hub <addr>` (optional) + `--limit <N>` (optional, default 500) + `--include-no-cid` (optional flag, default OFF) + `--json` (optional flag) + `--sort <field>` (optional, one of: `last_activity` (default) / `turn_count` / `cid`)."
  - "In text mode, prints a table with one row per distinct `conversation_id`: `cid | turns | receipts | senders | last_activity`. Header row + one row per cid."
  - "In `--json` mode, emits a single JSON object: `{ok, topic, conversation_count, conversations: [{conversation_id, turn_count, receipt_count, sender_count, senders: [...], last_activity, first_activity}], summary: {total_envelopes_scanned, with_cid, without_cid}}`. Sorted per `--sort`."
  - "`--include-no-cid` enables a sentinel entry with `conversation_id=\"(no-cid)\"`; default OFF."
  - "Usage errors (missing `--topic`, unknown args, invalid `--sort`) exit 2 with usage to stderr."
  - "Empty topic / no matching envelopes yields exit 0 with `conversation_count=0`."
  - "`scripts/test-agent-conversation-list.sh` covers all 7 paths (T1..T7). **7 PASS / 0 FAIL / 0 SKIP** at HEAD."
  - "Test suite ALL PASS; `bash -n` + `shellcheck` clean on both files."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "scripts/agent-conversation-list.sh"
  - "scripts/test-agent-conversation-list.sh"
  - ".tasks/active/T-1827-agent-conversation-listsh--enumerate-cid.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-28T11:49:53"
    action: "T-1827: agent-conversation-list.sh — enumerate cids on a topic (a2a orchestration)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 1
  files_changed: 3
  lines_added: 484
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1827-agent-conversation-listsh--enumerate-cid.md
generated_by: context-agent-hybrid

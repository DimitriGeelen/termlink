# ============================================================================
# EPISODIC MEMORY - T-2363: Fix remote send-file legacy fallback uses wrong RPC (event.emit not event.emit_to), skips inbox.queued
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-07-06T13:06:58Z
# ============================================================================

task_id: T-2363
task_name: "Fix remote send-file legacy fallback uses wrong RPC (event.emit not event.emit_to), skips inbox.queued"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-07-05T09:56:01Z
completed: 2026-07-06T13:06:39Z
duration_days: 1
updates_count: 2
first_commit: 2026-07-06T15:06:23+02:00
last_commit: 2026-07-06T15:06:23+02:00

# Summary (auto-generated from git commit messages)
summary: |
  remote send-file legacy fallback uses event.emit_to (was event.emit) so offline targets spool to inbox + fire inbox.queued

# Key outcomes
outcomes:
  - 'The three legacy 3-phase send calls in `cmd_remote_send_file_inner` (file.init, file.chunk, file.complete) call RPC `event.emit_to` (unicast) instead of `event.emit`, keeping the `{target, topic, payload}` param shape used by `file.rs` DeliveryRoute::Hub. — remote.rs:1750/1785/1820 now `event.emit_to` (3 sites; grep: 3 emit_to, 0 broadcast emit).'
  - 'No `client.call("event.emit", …)` (broadcast) call carrying a `target` param remains in the legacy send-file fallback in remote.rs. — `grep -q ''client.call("event.emit"''` returns none.'
  - '`cargo check -p termlink` passes (the CLI crate''s package name is `termlink`; dir is crates/termlink-cli). — clean, 17.55s.'
  - 'Affected-crate tests pass (`cargo test -p termlink`), and the hub-side unicast→inbox path retains coverage (existing `mirror_inbox_deposit`/`inbox_queued`/`event.emit_to` tests in crates/termlink-hub still green). — CLI 0 failed (4 ignored live-PTY); hub `emit_to` 4/4, `inbox_queued` 3/3 passed.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'crates/termlink-cli/src/commands/remote.rs'
  - '.tasks/active/T-2363-fix-remote-send-file-legacy-fallback-use.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-07-06T15:06:23"
    action: "T-2363: remote send-file legacy fallback uses event.emit_to (was event.emit) so offline targets spool to inbox + fire inbox.queued"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 1630 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [bug, inbox-queued, T-2409]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 1630
  commits: 1
  files_changed: 2
  lines_added: 53
  lines_removed: 21

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2363-fix-remote-send-file-legacy-fallback-use.md
generated_by: context-agent-hybrid

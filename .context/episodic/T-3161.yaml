# ============================================================================
# EPISODIC MEMORY - T-3161: SQ-1 tranche 3: convert further mechanically-checkable Human ACs
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-26T00:10:30Z
# ============================================================================

task_id: T-3161
task_name: "SQ-1 tranche 3: convert further mechanically-checkable Human ACs"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-26T00:07:51Z
completed: 2026-09-26T00:10:22Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  SQ-1 tranche 3: convert further mechanically-checkable Human ACs

# Key outcomes
outcomes:
  - 'Each candidate''s full `### Human` AC block is **read before conversion** — T-3154 excluded T-2878 on exactly this ground, and converting an AC I have not read is the failure this work exists to avoid.'
  - 'Each converted AC moves to `### Agent` as `[REVIEWER]`, **unticked**, with a real command added to that task''s `## Verification` (CLAUDE.md T-1811/T-1878).'
  - '**Every command written is executed and its verdict recorded before commit.** A converted AC whose command has never run replaces a human gate with an unverified one.'
  - 'Any candidate whose Expected clause turns out **not** to be settleable by a command, or whose Steps carry a genuine human judgement, is **left as a Human AC** with the reason recorded. T-3153 measured ~57% as genuinely human; a pass that converts everything contradicts its own evidence.'
  - '**No AC is ticked and no task is closed**, verified mechanically (no `+- [x]`, no `+owner:` under `.tasks/active` outside this task''s own file).'
  - 'The queue delta is **measured** with `fw review-queue`, so the tranche''s effect is a number rather than a claim.'
  - 'The uncommitted T-3160 frontmatter delta left by the previous run''s park/unpark cycle is picked up in this task''s commit rather than left dangling.'
  - '`## Decisions` left EMPTY — the vendored auto-capture mitigation, now 4/4.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 2 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 2
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-3161-sq-1-tranche-3-convert-further-mechanica.md
generated_by: context-agent-hybrid

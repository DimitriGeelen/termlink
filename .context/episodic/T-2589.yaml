# ============================================================================
# EPISODIC MEMORY - T-2589: fix check-outbox outbound_unread over-count (peer receipt makes it always >=1)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-10T19:22:07Z
# ============================================================================

task_id: T-2589
task_name: "fix check-outbox outbound_unread over-count (peer receipt makes it always >=1)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-09T22:38:21Z
completed: 2026-08-10T19:22:02Z
duration_days: 1
updates_count: 2
first_commit: 2026-08-10T21:21:54+02:00
last_commit: 2026-08-10T21:21:54+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fix check-outbox outbound_unread always->=1 over-count (peer receipt inflates whole-topic count)

# Key outcomes
outcomes:
  - '`scripts/check-outbox.sh` no longer computes `outbound_unread` as `count - 1 - peer_acked` (the whole-topic subtraction that counts the peer''s own posts + the peer''s own receipt envelope).'
  - '`outbound_unread` is computed by a per-envelope tail scan: subscribe from `cursor = peer_acked + 1` and count only envelopes where `sender_id == self_fp` AND `msg_type != "receipt"` AND `offset > peer_acked` (the semantic "DMs I sent that the peer has not acked").'
  - 'The peer''s latest-receipt-always->=1 false positive is eliminated: a topic where the peer has acked every self-authored post now reports `outbound_unread = 0` and drops out of the report (all-caught-up reachable).'
  - 'A load-bearing fixture test (`tests/check-outbox-fixtures.sh`) drives the whole script through a mock `termlink` (`TERMLINK_BIN`) and asserts the new count. It FAILS against the old `count-1-peer_acked` formula (proven by temp-revert) and PASSES against the fix.'
  - 'The header comment (lines ~228-233) is updated to document the exact semantic instead of the "approximation, close enough" note.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: 'fix check-outbox outbound_unread always->=1 over-count (peer receipt inflates whole-topic count)'
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - 'scripts/check-outbox.sh'
  - '.tasks/active/T-2589-fix-check-outbox-outboundunread-over-cou.md'
  - 'tests/check-outbox-fixtures.sh'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-08-10T21:21:54"
    action: "T-2589: fix check-outbox outbound_unread always->=1 over-count (peer receipt inflates whole-topic count)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 1243 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [bug]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 1243
  commits: 1
  files_changed: 3
  lines_added: 180
  lines_removed: 12

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2589-fix-check-outbox-outboundunread-over-cou.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2767: hub start does not detect a LIVE hub when the pidfile is stale — allows a second hub to steal hub.sock
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-16T15:38:17Z
# ============================================================================

task_id: T-2767
task_name: "hub start does not detect a LIVE hub when the pidfile is stale — allows a second hub to steal hub.sock"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-16T15:01:33Z
completed: 2026-08-16T15:22:49Z
duration_days: 0
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  Measured in T-2766: PID 3869961 started a second hub against /var/lib/termlink at 16:52 while supervised PID 3093442 was already serving, and took over hub.pid and hub.sock. The already-running guard keys on the PIDFILE, so when the pidfile is missing or names a dead pid the check passes even though a hub is demonstrably bound to hub.sock and serving. Result is a split-brain: unix-socket clients reach one instance, TCP clients another, with the same topic names resolving differently on ONE host. Fix direction: probe the socket for liveness (or bind-exclusively) rather than trusting the pidfile alone, and refuse loudly per Directive 2.

# Key outcomes
outcomes:
  - 'The current already-running check in `hub start` is located and its predicate'
  - '`hub start` refuses when a hub is LIVE on the target `runtime_dir` even if'
  - 'The refusal is LOUD and actionable per Directive #2: it names the socket it'
  - 'A stale pidfile with NO live socket still starts normally — the fix must not'
  - 'Regression test pins BOTH directions: live-socket-and-stale-pidfile refuses,'
  - 'The test fails against the pre-fix code path (load-bearing, not merely green) —'
  - '`cargo test --workspace` green — CONFIRMED post-change: 3593 passed, 0 failed'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 21 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 21
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2767-hub-start-does-not-detect-a-live-hub-whe.md
generated_by: context-agent-hybrid

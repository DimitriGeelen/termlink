# ============================================================================
# EPISODIC MEMORY - T-1730: termlink_agent_mentions MCP — find @-mentions of a user on agent-chat-arc (T-1513 parity)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-20T20:04:15Z
# ============================================================================

task_id: T-1730
task_name: "termlink_agent_mentions MCP — find @-mentions of a user on agent-chat-arc (T-1513 parity)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-20T19:52:14Z
completed: 2026-05-20T19:59:48Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-20T21:59:44+02:00
last_commit: 2026-05-20T21:59:44+02:00

# Summary (auto-generated from git commit messages)
summary: |
  termlink_agent_mentions MCP — find @-tags on agent-chat-arc

# Key outcomes
outcomes:
  - "`AgentMentionsParams` struct defined in tools.rs with single field `user: String` (the mention target — agent identity FP or peer name; `\"*\"` matches any non-empty CSV per T-1333 rules). Doc-comment cites T-1513 (CLI parity) + T-1730."
  - "Pure helpers ported into tools.rs:"
  - "`termlink_agent_mentions` tool method registered. Flow: hub_socket_path check → walk `agent-chat-arc` via existing `walk_topic_full_mcp` → `compute_mentions_of_mcp(&envelopes, &p.user)` → return `{ok, topic: \"agent-chat-arc\", user, mentions: [{mention_offset, sender_id, payload, mentions_csv, ts_ms}, ...]}`. Empty user input → JSON error. Empty result → `mentions: []`, still `ok: true`. NO new RPC surface — only `channel.subscribe` via `walk_topic_full_mcp`."
  - "Tool description cites T-1513 (CLI parity), explains the `metadata.mentions` filter is structured (distinct from `termlink_agent_search`'s substring match), documents the `*` glob, names the hardcoded topic `agent-chat-arc`."
  - "`cargo build --release -p termlink-mcp` clean — only the pre-existing `cur_run_end` warning."
  - "**≥5** new unit tests under `tools::tests`: (a) `mentions_match_mcp` literal hit; (b) `mentions_match_mcp` `*` target matches any non-empty CSV; (c) `mentions_match_mcp` `*` in CSV matches every specific target; (d) `extract_mentions_mcp` returns None on missing metadata; (e) `compute_mentions_of_mcp` returns descending-offset rows; (f) `compute_mentions_of_mcp` skips redacted offsets; (g) `compute_mentions_of_mcp` skips META_MSG_TYPES envelopes; (h) `AgentMentionsParams` deserializes. All pass."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-mcp/src/tools.rs"
  - ".tasks/active/T-1730-termlinkagentmentions-mcp--find--mention.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-20T21:59:44"
    action: "T-1730: termlink_agent_mentions MCP — find @-tags on agent-chat-arc"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 7 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 7
  commits: 1
  files_changed: 2
  lines_added: 453
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1730-termlinkagentmentions-mcp--find--mention.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1175: Fix G-012 — teach fabric enrich.py to parse Rust mod/use
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-20T23:31:47Z
# ============================================================================

task_id: T-1175
task_name: "Fix G-012 — teach fabric enrich.py to parse Rust mod/use"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-20T23:28:37Z
completed: 2026-04-20T23:31:46Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Fix G-012 — teach fabric enrich.py to parse Rust mod/use

# Key outcomes
outcomes:
  - "Add `detect_rust_deps` function in `.agentic-framework/agents/fabric/lib/enrich.py` that emits `(target_location, \"calls\")` tuples for `mod <name>;` (sibling file or `mod.rs` subdir) and `use <crate_name>::` → `crates/<kebab>/src/lib.rs`"
  - "Add `is_rust` branch in `compute_forward_edges` that dispatches to the new detector for `.rs` files"
  - "Skip list covers std + common third-party crates used in this workspace (`std`, `core`, `alloc`, `crate`, `self`, `super`, `tokio`, `serde`, `serde_json`, `anyhow`, `thiserror`, `tracing`, `rusqlite`, `ed25519_dalek`, `rand_core`, `base64`, `hex`, `sha2`, `chrono`, `uuid`, `clap`, `futures`, `async_trait`, `reqwest`, `jsonrpsee`, `tempfile`, `once_cell`, etc.)"
  - "Unit-test-style dry run on the current workspace: at least 50 new Rust edges detected against the 16 edgeless Rust cards (mod declarations in lib.rs files + cross-crate `use termlink_*` edges across cli/hub/mcp/session)"
  - "After applying, audit `Fabric: X/104 cards have no edges` drops by ≥10 (from 19 edgeless to ≤9)"
  - "No regressions: existing bash/python/html/ts edges still produced — count before and after the change on the same snapshot, verify zero decrease"
  - "`python3 -c \"import ast; ast.parse(open('.agentic-framework/agents/fabric/lib/enrich.py').read())\"` parses clean"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 3
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1175-fix-g-012--teach-fabric-enrichpy-to-pars.md
generated_by: context-agent-hybrid

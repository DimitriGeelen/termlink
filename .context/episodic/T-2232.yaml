# ============================================================================
# EPISODIC MEMORY - T-2232: Pickup: update-task.sh ships-reachable gate crashes on inception work-completed: __file__ is - under python3 stdin, breaks lib.inception_decisions import (from termlink)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-16T16:15:51Z
# ============================================================================

task_id: T-2232
task_name: "Pickup: update-task.sh ships-reachable gate crashes on inception work-completed: __file__ is - under python3 stdin, breaks lib.inception_decisions import (from termlink)"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-06-21T10:39:02Z
completed: 2026-09-16T16:15:44Z
duration_days: 87
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Auto-created from pickup envelope. Source: termlink, task T-2229. Type: bug-report.

# Key outcomes
outcomes:
  - 'The fix is present in the vendored tree and verified by reading it: `update-task.sh:668` passes `FRAMEWORK_ROOT` via `sys.argv[3]` with an env fallback, so the `__file__ is "-"` failure the envelope reports cannot occur. Not inferred from the register alone.'
  - 'Cross-checked against `.vendor-divergence.yaml`: the same fix is registered as T-2304, `status: landed-upstream`, with upstream carrying it by a **better** mechanism than the one filed (argv survives an env-stripping wrapper; the filed env-threading did not).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 125616 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [pickup, bug-report]

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 125616
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2232-pickup-update-tasksh-ships-reachable-gat.md
generated_by: context-agent-hybrid

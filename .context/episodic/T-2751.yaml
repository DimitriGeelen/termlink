# ============================================================================
# EPISODIC MEMORY - T-2751: install.sh artifact names drift from release.yml — 9th static check (herdr rank 20 real finding)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T21:32:01Z
# ============================================================================

task_id: T-2751
task_name: "install.sh artifact names drift from release.yml — 9th static check (herdr rank 20 real finding)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-15T21:22:27Z
completed: 2026-08-15T21:30:31Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  install.sh artifact names drift from release.yml — 9th static check (herdr rank 20 real finding)

# Key outcomes
outcomes:
  - '`scripts/check-release-artifact-drift.sh` exists, carries the `# guard-layer: source`'
  - 'It parses the artifact set from `install.sh` case arms and from `release.yml`''s publish'
  - 'The Homebrew formula is checked as a SUBSET, not an equality: every artifact the formula'
  - 'Exit codes follow the layer contract: 0 clean, 1 firing, 2 tooling error — and an'
  - '`tests/release-artifact-drift-fixtures.sh` exists, is hermetic (fixture dirs via test'
  - 'Load-bearing proof recorded in Decisions: a temporary rename on ONE side makes the check'
  - 'Current tree scans CLEAN (the three lists agree today) with the census stated in output'
  - 'CLAUDE.md documents the check alongside the other eight static checks, stating its scope'
  - 'Herdr backlog banner updated: rank 20 closed as ALREADY-IMPLEMENTED with the corrected'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-08-15 — rank 20 closed as ALREADY-IMPLEMENTED rather than built'
    chose: 'Do not build a `curl | sh` installer. Close rank 20 by correcting its premise,'
    rationale: '`install.sh` has existed since T-1134 and already has every element the item'
    alternatives_rejected: ['*Change the default prefix to `$HOME/.local`* — a real behaviour change to']
    alternatives_rejected: ['*Close rank 20 with no artifact* — defensible, but it would have left the']
  - decision: '2026-08-15 — bidirectional install↔release, but formula as subset only'
    chose: 'Check `install.sh` ↔ `release.yml` in **both** directions with distinct'

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 8 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 8
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2751-installsh-artifact-names-drift-from-rele.md
generated_by: context-agent-hybrid

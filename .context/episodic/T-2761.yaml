# ============================================================================
# EPISODIC MEMORY - T-2761: test-agent-respond.sh writes into the production woken-but-silent canary log
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-16T12:10:24Z
# ============================================================================

task_id: T-2761
task_name: "test-agent-respond.sh writes into the production woken-but-silent canary log"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-16T11:59:02Z
completed: 2026-08-16T12:08:41Z
duration_days: 0
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  test-agent-respond.sh invokes the real agent-send.sh without exporting TERMLINK_WOKEN_SILENT_LOG, so its by-design give-up path appends to the real .woken-but-silent-canary.log. Sibling test-agent-send.sh got that redirect in T-2402 Stage 5; this one was not migrated. Because empty-log = healthy is a one-bit channel, test residue leaves the canary permanently FIRING and therefore deaf to genuine events (T-2685 harm). Observed: entry cid=cidB-962839 topic=agent-respond-test-962839 dated 2026-08-15T22:19:24Z.

# Key outcomes
outcomes:
  - '`test-agent-respond.sh` exports `TERMLINK_WOKEN_SILENT_LOG` into its own temp dir before invoking `agent-send.sh`, mirroring `test-agent-send.sh`'
  - 'Running `test-agent-respond.sh` leaves the real `.context/working/.woken-but-silent-canary.log` byte-identical'
  - 'The existing test-fixture residue is cleared from the production log, so the canary is readable again'
  - 'A check fires if any `tests/` or `scripts/test-*` script invokes `agent-send.sh` without redirecting the canary log — so the next sibling cannot regress the same way'
  - 'The new check is a `# guard-layer: source` member, so `run-guard-layer.sh` executes it (a check nothing runs is the T-2683 defect)'
  - 'The check is load-bearing: removing the export from `test-agent-respond.sh` makes it fire; restoring returns it to clean'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 9 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 9
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2761-test-agent-respondsh-writes-into-the-pro.md
generated_by: context-agent-hybrid

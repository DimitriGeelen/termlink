# ============================================================================
# EPISODIC MEMORY - T-2778: Make BVP estimator produce real signal, then rank and work top HV items
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-16T23:30:41Z
# ============================================================================

task_id: T-2778
task_name: "Make BVP estimator produce real signal, then rank and work top HV items"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-16T22:18:22Z
completed: 2026-08-16T23:29:33Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Operator asked for the BVP estimator to be run regularly. T-2776 measured that value scoring works but cost scoring emits no-signal defaults for every task, so no quadrant can be computed. Find why cost is no-signal, fix what is fixable in-repo, run the estimator across the actionable backlog, and work the top-ranked HV/LC and HV/HC items.

# Key outcomes
outcomes:
  - 'The two causes above are separated with evidence: state whether `blast_radius=0` is a MISSING-RUBRIC problem, a MISSING-INPUT-DATA problem, or both — quoting the relevant `score_blast_radius` branch — **DONE: missing input data (`components: []`), not the rubric; see Decisions. The estimator computes correctly and needs no fix.**'
  - 'Value estimation is run across the actionable backlog and produces **differentiated** scores (assert: more than one distinct `bvp_raw` value across the scored set — a single repeated value means it is defaulting, not measuring) — **DONE: 224 tasks scored, 47 distinct `bvp_raw`, range 0.0–107.0, 0 errored. Assertion authored and RUN, not written blind.**'
  - '**[AMENDED — mechanism changed by AC 6, substance unchanged]** A populated ranking is produced and the top HV entries are recorded in `## Decisions` — **DONE via `fw bvp estimate --dry-run --json` across all 224 active tasks, NOT via `fw bvp --include-proposed`.** The original wording named `--include-proposed`, which reads persisted `bvp_scores_proposed:` and therefore requires the bulk write that AC 6 below explicitly instructs to reconsider — and which was rejected. `fw bvp --include-proposed` currently returns 3 rows (all `COST: -`, `QUAD: -`), so the literal mechanism cannot satisfy the intent without the write. The dry-run path yields the identical numbers with zero writes. The assertion was not weakened; only the read path changed. Recorded so the amendment is visible rather than silent.'
  - 'Cost is either (a) made to produce real signal, with the same differentiation assertion applied, or (b) explicitly left unscored with the reason — **no no-signal defaults are committed to task frontmatter under any circumstances** — **DONE: option (b). Cost left unscored; reason is unpopulated `components:`, established with evidence in `## Decisions`. Zero cost values written to any task.**'
  - 'If cost stays unscored, the quadrant claim is not made: the ranking is reported as value-only and labelled as such, never as HV/LC vs HV/HC — **DONE: the ranking is labelled value-only in `## Decisions` and in the session report.**'
  - 'The estimator''s frontmatter churn is assessed before any bulk write (T-2776 measured 2951 insertions across 165 files, including `description:` reflow and `date_finished: null` → empty); if it still reflows unrelated fields, that is reported and the bulk write is reconsidered — **DONE: it still reflows. Measured 23+/3- for ONE task, only 15 lines being the wanted payload (~35% unrelated churn: `description:` reflow, `date_finished: null` → bare, `last_update:` re-quoted). Bulk write REJECTED. Cross-references existing T-2222.**'
  - 'At least one top-ranked HV item is worked to completion under its own task ID — **DONE: T-2644 is `status: work-completed`, `owner: human` (partial-complete — its one Human AC is a raw-mode terminal-render check that is not agent-verifiable, so it correctly stays unchecked). Passed its own P-011 gate 11/11. Fix: the attach loop''s fire-and-forget `command.inject` now classifies its outcome; two pure helpers, 8 tests, load-bearing proven by mutation.**'
  - '`bash scripts/run-guard-layer.sh` passes — **DONE: 45/45 members clean.**'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']
  - decision: '2026-08-17 — the cause is MISSING INPUT DATA, and "no-signal" was the wrong diagnosis'

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 71 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 71
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2778-make-bvp-estimator-produce-real-signal-t.md
generated_by: context-agent-hybrid

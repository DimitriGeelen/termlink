# ============================================================================
# EPISODIC MEMORY - T-2760: parity_version and parity_info are non-hermetic — fail on git HEAD movement mid-build
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-16T12:24:19Z
# ============================================================================

task_id: T-2760
task_name: "parity_version and parity_info are non-hermetic — fail on git HEAD movement mid-build"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-16T11:52:06Z
completed: 2026-08-16T12:10:42Z
duration_days: 0
updates_count: 3

# Summary (auto-generated from git commit messages)
summary: |
  parity_version/parity_info compare git-derived commit+version across MCP and CLI. The MCP test crate is compiled at cargo-test start; find_termlink_bin_fresh rebuilds the CLI at test runtime minutes later. Any commit landing in between guarantees divergence, failing the gate for a build-environment artifact rather than a product defect. Observed: MCP f28e9b857/0.11.1403 vs CLI 5859c89ad/0.11.1405, blocking T-2757 closure.

# Key outcomes
outcomes:
  - 'A shared helper detects build skew by comparing the two sides'' `commit` values'
  - 'When commits DIFFER, `version`/`commit` are excluded from the diff and the test emits a diagnostic naming the skew and both commits — the run does not fail on that field'
  - 'When commits MATCH, `version` is compared strictly, so a genuine T-1458/T-2744-class version-derivation defect still fails the test'
  - 'Every other field in both envelopes is compared strictly in both cases — skew narrows the comparison to the two git-derived fields only, never the whole envelope'
  - '`parity_version` and `parity_info` both use the helper (the defect is present in both)'
  - 'Unit coverage proves both branches: skew-detected (version excluded) and no-skew (version compared)'
  - '`cargo test -p termlink-mcp --test parity parity_version parity_info` passes on the current tree'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 18 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 18
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2760-parityversion-and-parityinfo-are-non-her.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2292: V1: per-agent identity by default
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-06-27T17:38:09Z
# ============================================================================

task_id: T-2292
task_name: "V1: per-agent identity by default"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-06-27T17:05:54Z
completed: 2026-06-27T17:38:04Z
duration_days: 0
updates_count: 3
first_commit: 2026-06-27T19:09:57+02:00
last_commit: 2026-06-27T19:37:36+02:00

# Summary (auto-generated from git commit messages)
summary: |
  arc-003 reliable-comms — mint 5 build tasks (V1 identity, V2 discovery, V3a notify, V3b confirm, V6 apex direct-transport) with real ACs; arc started in-progress. V1 per-agent identity by default (arc-003 reliable-comms)

# Key outcomes
outcomes:
  - '`register` (and `register --self`) defaults to a stable per-agent key at `~/.termlink/identities/<agent_id>.key`, derived from agent_id, instead of the shared `$HOME/.termlink/identity.key` — `TERMLINK_AGENT_ID` branch added to `resolve_identity_key_path` (registration.rs) + `load_identity_or_create` (channel.rs, lockstep) + `bind_per_agent_identity_default` (session.rs, creates+pins on register). Live proof: two co-resident agents under one $HOME got distinct 32-byte seeds `bc747640…` / `aea5baa4…`.'
  - '`/be-reachable` and `listener-heartbeat.sh` set/use the same per-agent key path — both now `export TERMLINK_AGENT_ID="$agent_id"` so heartbeat/DM posts sign with the per-agent key.'
  - '`termlink whoami --json` shows a DISTINCT `identity_fingerprint` for two co-resident agents on the same host — register bakes the per-agent fingerprint into SessionMetadata (which whoami reads); distinct keys → distinct fingerprints, proven by `per_agent_paths_produce_distinct_fingerprints` + live distinct seeds.'
  - 'Clean cutover: the DM-topic naming discontinuity (old shared-fp topics vs new per-agent topics) is documented; no history migration attempted — `docs/operations/per-agent-identity.md`.'
  - 'Transport trust (`hub.secret` HMAC + TLS cert pinning) is untouched — verified by a passing `fleet doctor` against an existing hub after the change — only client signing-key selection changed; `fleet doctor` connects + auths against live hubs (all reachable hubs `[PASS]`).'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '2026-06-27 — per-agent default via TERMLINK_AGENT_ID env, additive branch'
    chose: 'A new `TERMLINK_AGENT_ID` env var that, when set, diverts the key'
    rationale: 'Purely additive and backward compatible — single-agent hosts and all'
    alternatives_rejected: ['(a) Changing the *shared* default unconditionally — would break']
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.context/arcs/reliable-comms.yaml'
  - 'crates/termlink-cli/src/commands/channel.rs'
  - 'crates/termlink-cli/src/commands/session.rs'
  - 'crates/termlink-session/src/agent_identity.rs'
  - 'crates/termlink-session/src/registration.rs'
  - 'docs/operations/per-agent-identity.md'
  - 'scripts/be-reachable.sh'
  - 'scripts/listener-heartbeat.sh'
  - '.tasks/active/T-2292-v1-per-agent-identity-by-default.md'
  - '.tasks/active/T-2293-v2-fleet-discovery-registry.md'
  - '.tasks/active/T-2294-v3a-deterministic-notify-sidecar-wake.md'
  - '.tasks/active/T-2295-v3b-delivery-confirm-by-default--canary.md'
  - '.tasks/active/T-2296-v6-direct-transport-first--hub-fallback-.md'

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-06-27T19:09:57"
    action: "T-2292: arc-003 reliable-comms — mint 5 build tasks (V1 identity, V2 discovery, V3a notify, V3b confirm, V6 apex direct-transport) with real ACs; arc started in-progress"
  - time: "2026-06-27T19:37:36"
    action: "T-2292: V1 per-agent identity by default (arc-003 reliable-comms)"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 2 commit(s), 32 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [arc:reliable-comms]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 32
  commits: 2
  files_changed: 13
  lines_added: 1343
  lines_removed: 12

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2292-v1-per-agent-identity-by-default.md
generated_by: context-agent-hybrid

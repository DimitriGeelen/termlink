summary: "Documented and executed the one-shot runtime directory migration from /tmp/termlink-0/ (tmpfs, wiped on reboot) to /var/lib/termlink/ (persistent, systemd StateDirectory)."

# T-935: Runtime dir migration — /tmp/termlink-0 to /var/lib/termlink

**Completed:** 2026-04-11
**Type:** build
**Duration:** 2026-04-11 (same day, ~35 min)

## What Was Done
Documented and executed the one-shot runtime directory migration from `/tmp/termlink-0/` (tmpfs, wiped on reboot) to `/var/lib/termlink/` (persistent, systemd StateDirectory). Wrote operations guide, removed stale `/tmp/termlink-0/` on .107, and verified the systemd hub remained active (PID unchanged) with persistent secret byte-identical before/after. No code changes needed -- documentation-only plus operational cleanup.

## Key Decisions
None -- approach (b) from task description (documentation + delete stale dir) was the obvious choice since T-931's systemd unit already pointed at the new location.

## Patterns Observed
None.

## Artifacts
- New: `docs/operations/termlink-hub-runtime-migration.md`
- Operational: removed stale `/tmp/termlink-0/` on .107

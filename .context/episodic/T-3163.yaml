# ============================================================================
# EPISODIC MEMORY - T-3163: SQ-1 tranche 4: continue Human AC conversions (operator raised context target to 1000K)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-09-26T00:39:53Z
# ============================================================================

task_id: T-3163
task_name: "SQ-1 tranche 4: continue Human AC conversions (operator raised context target to 1000K)"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-09-26T00:36:20Z
completed: 2026-09-26T00:39:45Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  SQ-1 tranche 4: continue Human AC conversions (operator raised context target to 1000K)

# Key outcomes
outcomes:
  - 'The operator''s ruling that **the context target is 1000K, not ~300K**, is recorded in the live handover so the next session inherits it rather than re-raising the question a fifth time.'
  - 'Each candidate''s full `### Human` AC block is **read before conversion** — T-2870 was refused in tranche 3 on exactly this ground, so it is read first here.'
  - 'Each converted AC moves to `### Agent` as `[REVIEWER]`, **unticked**, with a real command added to that task''s `## Verification` (T-1811/T-1878).'
  - '**Every command written is executed and its verdict recorded before commit.** A converted AC whose command has never run replaces a human gate with an unverified one.'
  - 'Any candidate whose Expected cannot be settled by a command, or whose Steps carry genuine human judgement, is **left as a Human AC** with the reason recorded.'
  - '**No AC is ticked and no task is closed**, verified mechanically (no `+- [x]`, no `+owner:` under `.tasks/active` outside this task''s own file).'
  - 'The queue delta is **measured** with `fw review-queue`.'
  - '`## Decisions` left EMPTY — the vendored auto-capture mitigation, now 5/5.'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 3 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 3
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-3163-sq-1-tranche-4-continue-human-ac-convers.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1683: fleet doctor --auto-heal one-shot — heal without --watch (T-1680 ergonomic extension)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-18T06:15:47Z
# ============================================================================

task_id: T-1683
task_name: "fleet doctor --auto-heal one-shot — heal without --watch (T-1680 ergonomic extension)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-18T06:10:18Z
completed: 2026-05-18T06:15:37Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-18T08:15:32+02:00
last_commit: 2026-05-18T08:15:32+02:00

# Summary (auto-generated from git commit messages)
summary: |
  fleet doctor --auto-heal one-shot — heal without --watch

# Key outcomes
outcomes:
  - "`--auto-heal` accepted without `--watch` in clap — `requires = \"watch\"` removed; doc-comment updated to document both modes"
  - "Single-shot path: post-loop iteration over `hub_results` fires heal for any hub in `pin=drift` OR `conn=auth-mismatch` AND with declared `bootstrap_from`; reuses existing `fire_auto_heal` + `derive_watch_conn` helpers"
  - "Profiles without declared `bootstrap_from` printed to stderr — `[SKIP] <name>: no bootstrap_from declared (R2 ...)` lines after the \"Auto-heal: fired N (one-shot, T-1683)\" header"
  - "`--auto-heal` without `--include-pin-check`: info hint printed at command start — `[info] --auto-heal without --include-pin-check: only conn=auth-mismatch heals will fire. Pass --include-pin-check to also heal on cert drift.` — verified live"
  - "`cargo check -p termlink` passes (clean)"
  - "Smoke: `fleet doctor --auto-heal --include-pin-check` ran against 5-hub local fleet — pin=match=3, drift=0, no-pin=1, probe-fail=1; no heals fired (correct — nothing to heal); no panics"
  - "Smoke: `fleet doctor --auto-heal` (no pin-check) — info hint shown at top of output as designed"
  - "CLAUDE.md: detection-verbs row added for one-shot mode; recipe section now documents both continuous and one-shot forms"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "CLAUDE.md"
  - "crates/termlink-cli/src/cli.rs"
  - "crates/termlink-cli/src/commands/remote.rs"
  - ".tasks/active/T-1683-fleet-doctor---auto-heal-one-shot--heal-.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-18T08:15:32"
    action: "T-1683: fleet doctor --auto-heal one-shot — heal without --watch"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 5 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 5
  commits: 1
  files_changed: 4
  lines_added: 256
  lines_removed: 23

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1683-fleet-doctor---auto-heal-one-shot--heal-.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-1131: Wire protocol_version enforcement at hub — structured error instead of opaque serde parse failure (from T-1071 GO)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-04-23T19:13:48Z
# ============================================================================

task_id: T-1131
task_name: "Wire protocol_version enforcement at hub — structured error instead of opaque serde parse failure (from T-1071 GO)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-04-18T22:59:37Z
completed: 2026-04-19T14:06:47Z
duration_days: 1
updates_count: 3
first_commit: 2026-04-19T16:06:41+02:00
last_commit: 2026-04-19T16:06:41+02:00

# Summary (auto-generated from git commit messages)
summary: |
  Scaffold protocol_version enforcement — error code, helper, storage

# Key outcomes
outcomes:
  - "New error code `PROTOCOL_VERSION_TOO_OLD = -32011` defined in `termlink-protocol::control::error_code`"
  - "`Capabilities` (control.rs) gains `#[serde(default = \"default_protocol_version\")]` so missing fields default to 1 (backward compatible)"
  - "New helper `check_protocol_version(id, declared, required, method) -> Option<ErrorResponse>` returns `Some(PROTOCOL_VERSION_TOO_OLD)` structured error with `{declared, required, method}` data when declared < required, else `None`"
  - "`RemoteEntry` (remote_store.rs) records `protocol_version: u8` captured at registration time (default 1)"
  - "`handle_register_remote` parses optional `protocol_version` from params and forwards it to the store"
  - "Unit test: helper returns structured error with correct shape when `declared < required`"
  - "Unit test: helper returns None when `declared >= required` (two variants: equal and greater)"
  - "Unit test: `Capabilities` deserializes with missing `protocol_version` field (backward-compat guarantee)"
  - "`cargo test -p termlink-protocol -p termlink-hub` passes (96 + 198 tests)"
  - "[RUBBER-STAMP] Confirm the scope fence is acceptable — follow-up task can plumb the check into each Tier-B handler — ticked by user direction 2026-04-23. Evidence: User direction 2026-04-23 — scope fence accepted."

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  - description: "Scaffold protocol_version enforcement — error code, helper, storage"
    source: git-mined

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "crates/termlink-hub/src/remote_store.rs"
  - "crates/termlink-hub/src/router.rs"
  - "crates/termlink-protocol/src/control.rs"
  - ".tasks/active/T-1131-wire-protocolversion-enforcement-at-hub-.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-04-19T16:06:41"
    action: "T-1131: Scaffold protocol_version enforcement — error code, helper, storage"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 907 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: [protocol, termlink, version-skew, T-1071]

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 907
  commits: 1
  files_changed: 4
  lines_added: 151
  lines_removed: 26

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1131-wire-protocolversion-enforcement-at-hub-.md
generated_by: context-agent-hybrid

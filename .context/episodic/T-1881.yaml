# ============================================================================
# EPISODIC MEMORY - T-1881: recent-dm: surface conversation_id in render output
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-30T16:16:19Z
# ============================================================================

task_id: T-1881
task_name: "recent-dm: surface conversation_id in render output"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-30T16:10:50Z
completed: 2026-05-30T16:15:24Z
duration_days: 0
updates_count: 1

# Summary (auto-generated from git commit messages)
summary: |
  >

# Key outcomes
outcomes:
  - "`scripts/agent-chat-arc-recent.sh` per-post JSON object includes a `conversation_id` field (null when envelope has no metadata.conversation_id)"
  - "`scripts/recent-dm.sh` text mode renders a `CID` column between SENDER and PREVIEW (first 22 chars or `-` when null)"
  - "`scripts/recent-dm.sh --json` output preserves `conversation_id` on each post (additive field, no shape change to other fields)"
  - "Live smoke against `dm:d1993c2c3ec44c94:d1993c2c3ec44c94` shows the cid column populated for envelopes that carry one (e.g. T-1880 smoke entry with cid `reply-20260530T152332Z`)"
  - "Backward compat: chat-arc posts (no `metadata.conversation_id`) flow through with `conversation_id: null` in JSON without breaking `/recent-chat` text render"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1881-recent-dm-surface-conversationid-in-rend.md
generated_by: context-agent-hybrid

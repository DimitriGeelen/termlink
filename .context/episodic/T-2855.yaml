# ============================================================================
# EPISODIC MEMORY - T-2855: Template-planted required field makes the inception gate incapable of failing
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-29T14:23:58Z
# ============================================================================

task_id: T-2855
task_name: "Template-planted required field makes the inception gate incapable of failing"
workflow_type: build
enrichment_status: auto-complete  # Mechanical task — AC checked, no decisions to record

# Timeline
created: 2026-08-29T14:14:16Z
completed: 2026-08-29T14:23:47Z
duration_days: 0
updates_count: 0
first_commit: 2026-08-29T16:23:33+02:00
last_commit: 2026-08-29T16:23:33+02:00

# Summary (auto-generated from git commit messages)
summary: |
  detect template-planted fields that make a gate incapable of failing

# Key outcomes
outcomes:
  - '`scripts/check-planted-default-gate.sh` exists, carries a `# guard-layer:` marker, and implements the exit contract 0=clean / 1=firing / 2=tooling, fail-closed (a missing template dir or empty task corpus exits 2, never a vacuous clean)'
  - 'Against the REAL tree with an empty ledger it fires on both `voi_score` and `target_blast_radius`, naming the domination ratio — load-bearing against real state, not only against fixtures'
  - '`tests/planted-default-gate-fixtures.sh` passes, and includes at least one fixture shape that does NOT occur in our corpus (577 @766 / 832 @769: red-then-green on real state proves the discriminator handles the instance, never the class)'
  - 'A mutation that weakens the discriminator is caught BY THE FIXTURES — recorded in this task with the observed failure text, not asserted'
  - '`.context/checks/planted-default-gate-allowlist` is git-tracked (T-2681) and each entry carries a cited reason; the two exposed fields are acknowledged pending the upstream fix so the check runs clean while staying a visible ledger'
  - 'The vendored defects are filed at `framework:pickup` (G-062) with the measurement, and the offset is recorded here — **filed at offset 71**, `metadata.from_project=010-termlink` so the T-2816 self-filter keeps it from firing our own pickup canary'
  - '`bash scripts/run-guard-layer.sh` reports the new member PASS and no new ERROR'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  # No decisions recorded (mechanical task or old template)

# Artifacts (auto-mined from git --name-only)
artifacts:
  - '.context/checks/planted-default-gate-allowlist'
  - 'scripts/check-planted-default-gate.sh'
  - '.tasks/active/T-2855-template-planted-required-field-makes-th.md'
  - 'tests/planted-default-gate-fixtures.sh'

# Timeline (auto-mined from git log)
git_timeline:
  - time: '2026-08-29T16:23:33'
    action: 'T-2855: detect template-planted fields that make a gate incapable of failing'

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 9 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  # git_mining: ok = the four counters below are measurements.
  #             skipped = git could not be reached from PROJECT_ROOT; the
  #             counters are null (absent measurement), NOT zero (T-3129).
  git_mining: ok
  wall_clock_minutes: 9
  commits: 1
  files_changed: 4
  lines_added: 587
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.tasks/completed/T-2855-template-planted-required-field-makes-th.md
generated_by: context-agent-hybrid

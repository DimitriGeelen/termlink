# ============================================================================
# EPISODIC MEMORY - T-1811: agent-send.sh --await-reply: synchronous conversational round-trip (T-1800 follow-up)
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-05-25T20:40:00Z
# ============================================================================

task_id: T-1811
task_name: "agent-send.sh --await-reply: synchronous conversational round-trip (T-1800 follow-up)"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-05-25T20:35:30Z
completed: 2026-05-25T20:39:42Z
duration_days: 0
updates_count: 1
first_commit: 2026-05-25T22:39:36+02:00
last_commit: 2026-05-25T22:39:36+02:00

# Summary (auto-generated from git commit messages)
summary: |
  agent-send.sh --await-reply — synchronous conversational round-trip

# Key outcomes
outcomes:
  - "`agent-send.sh --help` documents `--await-reply <secs>` and the new exit code 4 (delivered-but-no-reply)"
  - "`--await-reply` validates its argument as a positive integer; non-numeric or `<1` exits 2 (usage) — verified: `foo`→2, `0`→2"
  - "With `--await-reply <secs>`, after a receipt is seen the script polls the dm topic for the first `msg_type=turn` with `offset > post_offset` (the peer's reply), prints its payload, and exits 0 — verified Path D (PONG_D_REPLY printed, rc=0)"
  - "When delivered but no reply arrives within the `--await-reply` window, the script exits 4 with a clear \"delivered, no reply\" message (delivery still succeeded — distinct from exit 3 not-acked) — verified Path E (rc=4)"
  - "Without `--await-reply`, behavior is unchanged (exit 0 delivered / 3 not-acked / 2 usage) — Paths A/B/C still pass"
  - "`test-agent-send.sh` gains Path D (delivered + reply → exit 0, reply payload printed) and Path E (delivered, no reply → exit 4); full suite passes — ALL PASS (A–E)"
  - "`bash -n` clean on both scripts — plus shellcheck CLEAN"

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: "[date] — [topic]"
    chose: "[what was decided]"
    rationale: "[rationale]"
    alternatives_rejected: ["[alternatives and why not]"]

# Artifacts (auto-mined from git --name-only)
artifacts:
  - "docs/operations/injectable-listener-spawn-recipe.md"
  - "scripts/agent-send.sh"
  - "scripts/test-agent-send.sh"
  - ".tasks/active/T-1811-agent-sendsh---await-reply-synchronous-c.md"

# Timeline (auto-mined from git log)
git_timeline:
  - time: "2026-05-25T22:39:36"
    action: "T-1811: agent-send.sh --await-reply — synchronous conversational round-trip"

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 1 commit(s), 4 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 4
  commits: 1
  files_changed: 4
  lines_added: 248
  lines_removed: 5

# Metadata
source_file: /opt/termlink/.tasks/completed/T-1811-agent-sendsh---await-reply-synchronous-c.md
generated_by: context-agent-hybrid

# ============================================================================
# EPISODIC MEMORY - T-2744: Session metadata records a frozen termlink_version, not the build version
# ============================================================================
# STATUS: AUTO-GENERATED
# Hybrid episodic: git-mined timeline/metrics + task-file decisions/AC.
# Generated: 2026-08-15T19:46:28Z
# ============================================================================

task_id: T-2744
task_name: "Session metadata records a frozen termlink_version, not the build version"
workflow_type: build
enrichment_status: complete  # AC checked + decisions recorded

# Timeline
created: 2026-08-15T19:30:28Z
completed: 2026-08-15T19:43:02Z
duration_days: 0
updates_count: 2

# Summary (auto-generated from git commit messages)
summary: |
  Session metadata records a frozen termlink_version, not the build version

# Key outcomes
outcomes:
  - '`termlink-session` derives its version from git the way `termlink-cli` and `termlink-mcp` already do, so `metadata.termlink_version` records the build that registered the session'
  - 'A live `termlink list --json` shows a newly-registered session carrying the same version `termlink --version` reports — measured, not inferred from the code'
  - 'The build.rs re-run triggers match the CLI''s, so the version does not freeze after the first build (the T-1057 bug this project already paid for once)'
  - 'A test pins that the recorded version is not the stale `0.9.0` crate constant, so the defect cannot silently return'
  - 'Load-bearing proof recorded: removing the git derivation makes that test fail'
  - 'The follow-up detector task is filed with the corrected premise, so rank 12 is not picked up again on the false one (T-2745)'

# Challenges (auto-detected from git: commits with fix/revert/bug/error)
challenges:
  # No challenges detected in commit messages

# Decisions (from task file Decisions section)
decisions:
  - decision: '[date] — [topic]'
    chose: '[what was decided]'
    rationale: '[rationale]'
    alternatives_rejected: ['[alternatives and why not]']

# Artifacts (auto-mined from git --name-only)
artifacts:
  # No artifacts found in git

# Timeline (auto-mined from git log)
git_timeline:
  # No git timeline available

# What worked well (requires judgment — [TODO] if enrichment_status is pending)
successes:
  # Completed successfully in 0 commit(s), 12 min

# Related tasks
related_tasks:
  blocked: []
  absorbed: []
  spawned: []

# Tags for retrieval
tags: []

# Passive metrics (derived automatically — do not edit)
metrics:
  wall_clock_minutes: 12
  commits: 0
  files_changed: 0
  lines_added: 0
  lines_removed: 0

# Metadata
source_file: /opt/termlink/.claude/worktrees/charter-review-2026-0814/.tasks/completed/T-2744-stale-binary-session-detector--termlink-.md
generated_by: context-agent-hybrid

# T-2359 — declared per-hub version floors for check-fleet-binary-freshness.sh
# (G-069 prevention). Format: <hub-name-from-hubs.toml> <min-version | ->
#   <min-version>  fire when the hub SERVES below this (in-memory process, not disk)
#   -              exempt: foreign build lineage or expected-transient host
# Optional `* <min-version>` row applies to undeclared hubs.
#
# BUMP THE FLOOR WHEN HUB-SIDE RAILS SHIP: that is the operator's declaration
# that "shipped" must mean "capability-live" — the canary then names lagging
# hubs daily until they are restarted onto the new binary. Patch numbers are
# commits-since-tag and are NOT comparable across build lineages — never set
# a floor for a hub whose binary you don't build.

# Our lineage. T-2466 (2026-07-27): upgraded the local .107 systemd hub
# (serves both these profiles — one process on /var/lib/termlink/hub.secret)
# 0.11.635 → 0.11.679 via `systemctl restart termlink-hub` after installing the
# fresh musl binary to /root/.cargo/bin/termlink. Floor bumped to the running
# version so a restart onto an older binary fires the canary.
# (0.11.324 previously carried the T-2355 walk-deadline rails.)
local-test 0.11.679
workstation-107-public 0.11.679

# T-2465 (2026-07-27): deployed fresh musl 0.11.679 to .122 via remote-exec
# (base64-over-remote-exec, PL-096) — was 279 commits stale at 0.11.400, which
# still passed the old 0.11.324 floor (canary read it healthy while dark). Floor
# bumped to the deployed version so any restart onto an older binary fires again.
# (Prior: T-2350 deployed 0.11.296; restart onto >=0.11.324 closed the G-157 seat.)
ring20-management 0.11.679

# EXEMPT — NO UPGRADE FOOTHOLD (T-2467, 2026-07-28): fresh probes SUPERSEDE the
# old T-2377 fork/stale-lineage reading (a large patch number read as a possible
# ~1050-commit fork). .121 now serves **0.11.588**
# (fleet doctor status:ok, 43ms) — our OWN lineage, ~96 commits behind HEAD
# (0.11.684), NOT a divergent fork. It is doorbell-CAPABLE: `channel cv-keys
# agent-presence --hub 192.168.10.121:9100` returns a live `ring20-dashboard-agent`
# entry, so the earlier G-084 "rejects cv_keys" premise no longer holds either
# (.121 was evidently redeployed onto 0.11.588 since those were written). The ONLY
# reason it stays exempt: no execution foothold from here — 0 registered remote-exec
# sessions AND SSH root is publickey-denied, so it cannot be upgraded from .107
# (unlike .122, which had ready sessions). Flip to a real floor (e.g. 0.11.684)
# once a foothold exists and it is upgraded. Do NOT re-assert "fork" from a patch
# number (PL-243).
ring20-dashboard -

# Expected-transient WSL laptop (PL-219): usually unreachable; when up it
# runs its own install cadence.
laptop-141 -

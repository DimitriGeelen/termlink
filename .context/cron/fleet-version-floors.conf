# T-2359 — declared per-hub version floors for check-fleet-binary-freshness.sh
# (G-069 prevention). Format: <hub-name-from-hubs.toml> <min-version | ->
#   <min-version>  fire when the hub SERVES below this (in-memory process, not disk)
#   -              exempt: foreign build lineage or expected-transient host
# Optional `* <min-version>` row applies to undeclared hubs.
#
# BUMP THE FLOOR WHEN HUB-SIDE RAILS SHIP: that is the operator's declaration
# that "shipped" must mean "capability-live" — the canary then names lagging
# hubs daily until they are restarted onto the new binary. Patch numbers are
# commits-since-tag and are NOT comparable across build lineages — never set
# a floor for a hub whose binary you don't build.

# Our lineage (0.11.324 carries the T-2355 walk-deadline rails):
local-test 0.11.324
workstation-107-public 0.11.324

# T-2350 deployed our 0.11.296 to .122; their restart onto >=0.11.324 closes
# the ring20 G-157 walk-wedge seat. Firing here is the G-069 signal working.
ring20-management 0.11.324

# EXEMPT PENDING CONFIRMATION (T-2377): reports 0.11.806, but that is a
# `git describe` tag-base artifact (pre-v0.11.2-tag mainline build counts from
# v0.11.1 → 0.11.80x), NOT proof of a fork. T-2377 found .121 is very likely a
# ~1050-commit-STALE build of our OWN mainline (the T-1166/T-1235 holdout), not
# a divergent lineage. Kept exempt because it is NOT shell-confirmed (.121 has no
# registered session → its git sha is unreachable from here; confirming + any
# upgrade is ring20-manager scope). Do NOT re-assert "own fork" from the larger
# patch number. Flip to a real floor only once a session on .121 confirms lineage.
ring20-dashboard -

# Expected-transient WSL laptop (PL-219): usually unreachable; when up it
# runs its own install cadence.
laptop-141 -

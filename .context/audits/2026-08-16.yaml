# Audit Results - 2026-08-16-1626
timestamp: 2026-08-16T14:26:12Z
sections: "structure"
summary:
  pass: 17
  warn: 4
  fail: 0
findings:
  - level: PASS
    check: "Tasks directory exists"
  - level: PASS
    check: "Tasks/active directory exists"
  - level: PASS
    check: "Tasks/completed directory exists"
  - level: PASS
    check: "Tasks/templates directory exists"
  - level: PASS
    check: "Task template exists"
  - level: PASS
    check: "No duplicate task IDs across active/ and completed/"
  - level: PASS
    check: "All 15 project YAML files parse correctly"
  - level: PASS
    check: "All 6 arc anchor_task references resolve to existing tasks"
  - level: WARN
    check: "Arc 'arc-substrate-fitness' has no task commits in the last 30 days (8 task(s) in arc)"
    mitigation: "Either close/abandon the arc via 'fw arc close' or run 'fw task update T-XXX --last-update $(date -u +%FT%TZ)' on a relevant task. Configurable via FW_STALE_ARC_DAYS (default 30)."
  - level: PASS
    check: "No inline arc:<slug> tag-only scans outside canonical lib (T-1881)"
  - level: PASS
    check: "No PROJECT_ROOT resolution of framework-owned assets in web/ + lib/ Python (T-2648, OBS-097)"
  - level: PASS
    check: "No stale-slice-references (L-417)"
  - level: PASS
    check: "No GO-scope-not-propagated inception(s) (sibling to L-417)"
  - level: PASS
    check: "Fabric: 370 registered card(s)"
  - level: WARN
    check: "Fabric: 207/358 cards have no edges"
    mitigation: "Run: fw fabric enrich"
  - level: WARN
    check: "Fabric drift: 3 source file(s) have no fabric card"
    mitigation: "Run: fw fabric scan"
  - level: WARN
    check: "Fabric: 1 card(s) point at files no watch pattern covers"
    mitigation: "Widen .fabric/watch-patterns.yaml, or remove the cards if they are not components"
  - level: INFO
    check: "Cron drift checks skipped — linked worktree (cron is host-level, managed from the main checkout)"
  - level: INFO
    check: "Cron-misload lint skipped — linked worktree (cron install is host-level, managed from the main checkout)"
  - level: PASS
    check: "Hook threshold: no hooks failing over threshold"
  - level: PASS
    check: "Secret scan: tracked tree clean"

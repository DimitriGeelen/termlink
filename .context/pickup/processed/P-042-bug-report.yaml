pickup_id: P-042
version: 1
type: bug-report
source:
  project: "999-Agentic-Engineering-Framework"
  task_id: "T-1637"
  agent: "claude-code"
  timestamp: "2026-05-16T07:00:33Z"
payload:
  summary: "T-1820 rerun: inbox.queued emit on fix-shipped hub 0.9.2110 (ebe05294) not observable from any session-targeted event poll/subscribe — wiring-asymmetry vs fw peer subscribe"
  detail: "Rerun executed 2026-05-16T06:47Z against local hub. termlink doctor confirms version 0.9.2110 (ebe05294) and PID 2382342 — matches the hub_pid termlink-agent reported in fix.shipped envelope (framework:pickup offset 18 responding to P-041).

Triggers (3 attempts):
  termlink channel post inbox:tl-design-smoke-target --msg-type file.init --payload '{"transfer_id":"t1820-rerun-00N-2026-05-16",...}' --json
  → delivered cleanly: offsets 5/6/7 on topic inbox:tl-design-smoke-target.

Probes after each trigger:
  - termlink event poll tl-design-smoke-target --topic inbox.queued --since 0 --json → count=0, next_seq=0 (session bus empty)
  - termlink event poll framework-agent --topic inbox.queued --since 0 --json → count=0, next_seq=384 (no advance)
  - mcp__termlink_event_subscribe target=tl-design-smoke-target topic=inbox.queued since=0 timeout_ms=3000 → count=0, events=[]
  - termlink event topics (all 10 registered sessions) → ZERO sessions list inbox.queued under their emitted topics

Framework subscriber wire path: lib/peer.py::poll_once runs 'termlink event poll <target_session> --topic inbox.queued --since <cursor>' — per-session bus only.

QUESTION (for termlink-agent / T-1637 owner):
What RPC / subscription path did the live-smoke at commit ebe05294 actually call when it reported 'hub-level event.subscribe (topic=inbox.queued) returned count=1'? Did the new emit attach to <addressee_session_id>'s own event bus, or only to a hub-internal aggregator/cross-session stream that requires a different subscribe call from a CLI client?

Resolution shapes that would unblock T-1820 headline mechanic from the framework side:
  (A) Attach emit to <addressee_session_id>'s session-bus → 'event poll <id> --topic inbox.queued' returns the event. Framework subscriber needs zero changes.
  (B) Hub-aggregator stream is the only sink → ship a CLI/MCP verb that subscribes to it (e.g. 'termlink event aggregator-subscribe --topic inbox.queued' or hub-level event.subscribe via remote_call). Framework subscriber switches verb.

Framework will keep T-1820 in PARTIAL-SHIP pending answer. Demo artefact + Evolution log carry full rerun trail. Framework commit: d5cb7c4d (T-1820 rerun against fix-shipped hub 0.9.2110)."
  priority: high
  tags: [cross-repo, joint-smoke, T-1820, T-1637, T-1636, wiring-asymmetry]

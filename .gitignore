# Rust
/target/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Telemetry (raw session data, not committed)
.context/telemetry/

# Framework symlink (machine-specific)
.agentic-framework

# Vendored TermLink binary (machine-specific, per-project isolation)
.termlink/bin/

# Claude Code backup
.claude/settings.json.bak

# Playwright MCP dev artifacts (L-028)
.playwright-mcp/

# Root-level screenshots (dev scratch)
/*.png

# Ephemeral session state (L-028)
.context/working/.tool-counter
.context/working/.edit-counter
.context/working/.new-file-counter
.context/working/.budget-status
.context/working/.budget-gate-counter
.context/working/.prev-token-reading
.context/working/.session-turn-offset
.context/working/.session-start-ts
.context/working/.compact-log
.context/working/.fleet-failure-state.json

# Task locks (runtime, per-session)
.context/locks/

# Review markers (runtime scratch)
.context/working/.reviewed-*
.context/working/.tier0-approval*
.context/working/.fleet-learning-*
.context/working/.learnings-exchange-cursor.yaml
.context/working/.fw-secret-key

# Framework agent symlinks (machine-specific, point to Homebrew install)
agents/audit
agents/context
agents/git
agents/task-create
agents/handover
agents/healing
agents/session-capture
agents/resume
agents/fabric

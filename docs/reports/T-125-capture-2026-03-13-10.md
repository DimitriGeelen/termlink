# T-125: Session Retrospective Capture — 2026-03-13 10:06

> Captured via /capture | Turns: 14 (tail) | Mode: topic-boundary
> Session tasks: T-120, T-121, T-122, T-123, T-124, T-125, T-126, T-127, T-128

## Topic / Problem Statement

Session focused on implementing 3 build tasks (T-120 EventBus gap detection, T-121 PTY mode detection, T-122 Transport trait) in parallel using worktree isolation. An inception (T-123) was conducted first to determine the right isolation strategy for concurrent mesh builds. dispatch.sh --isolate was built (T-124). Retrospective tasks created (T-126-128).

## Key Insights

- File partitioning is not viable when tasks overlap on shared files (session/server.rs, hub/router.rs)
- Git worktrees are lightweight (~1-2MB shared .git/objects) and provide structural filesystem isolation
- CARGO_TARGET_DIR per worktree eliminates build contention
- Agents dispatched via Claude Code Agent tool don't auto-commit — manual commit + rebase needed
- Verification commands using `tail -1 | grep` are fragile — use `grep -q "test result: ok"` instead
- 3 parallel agents completed in ~10min wall clock vs ~30+ sequential

## Decisions Made

1. T-123 GO: Worktree isolation for mesh concurrent builds
2. Feedback memory: always use TermLink mesh, not Claude Code Agent tool
3. Retrospective improvement tasks: T-126 (auto-commit), T-127 (merge orchestration), T-128 (prompt template)

## Open Questions

1. Verification gate exit 101 when cargo test passes in same shell
2. T-121 human AC: verify pty.mode during vim session

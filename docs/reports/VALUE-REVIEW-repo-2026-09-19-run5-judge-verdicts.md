# Value Review — Judge Verdicts (run5)

Judged solely against `docs/reports/VALUE-REVIEW-repo-2026-09-19-run5-evidence.md`.
No source code, tasks, or other files were read. Confidence penalty for
gatherer/judge separation is not applied per this run's instructions.

---

## Summary

**Counts per class:** ADD = 6 · REFACTOR = 3 · INVESTIGATE = 14 · DELETE = 0 · KEEP = 13 (named list below)

**Top 3 by value-per-cost (fix these first):**
1. `canary-status.sh` FIRING→HEALTHY masking bug (R5-01) — one logic fix restores trust in all 30 canaries at once; currently four+ real fires read green.
2. Push-wake rail dark, 36 firings, verb-2 degraded to poll-floor fleet-wide (R5-03) — cheap operator remediation (relaunch through documented launcher), high blast radius if left.
3. Hook-counter corruption feeding a decay-alarm denominator with false silence (R5-04) — a measurement bug that is itself hiding other measurement bugs.

**Top 3 by cost (highest ongoing burden):**
1. `tools.rs` — 46,458 lines, 390 commits/180d, #1 churn file, 9.2% parity-test coverage (R5-07).
2. `cli/src/commands/channel.rs` — 20,435 lines, 166 commits/180d (R5-08).
3. Guard-layer runner not completing in >600s wall, 0-byte JSON, no partial verdicts recoverable (R5-06) — turns a "seconds" CI gate into an unknown-duration black box.

**Top 3 by risk (worst outcome if misjudged):**
1. Deleting/gutting any of the 28 off-charter analytics tools (R5-13) before the human GO recorded in T-2548 is actually confirmed — the task's own `## Decision` section is empty.
2. Trusting `/canaries` green as an all-clear (R5-01) while genuine fires (binary staleness, waker-dark, pickup backlog) are masked underneath it.
3. Refactoring `tools.rs`/`channel.rs` (R5-07/08) before parity-test coverage is raised past 9.2% — high risk of silent behavioural drift during the very refactor meant to reduce risk.

---

## Findings

### R5-01 | `canary-status.sh` FIRING→HEALTHY masking bug | `scripts/canary-status.sh:20-21` | **ADD(REPAIR)** | none (this is a tooling defect, not a usage question) | Evidence (E-22, E-23): mechanism is `FIRING = log non-empty AND latest log mtime >= latest heartbeat mtime`; an ad-hoc run refreshes the heartbeat without appending to the log, flipping FIRING→HEALTHY. Measured: `.substrate-preflight-canary.log` mtime 09-19 05:23 vs. heartbeat 09-19 20:46:12; same pattern on framework-pickup/waker-liveness/stale-waker-code heartbeats at 20:13. `/canaries` reported `{"firing":0}` (E-22) same day two of those logs were appended to. | None found — E-23 explicitly timestamps these touches as predating this review session by 9-42 minutes, ruling out this review's own footprint as the cause. | **HIGH** — measured mtimes on both sides of the comparison, plus the exact predicate read from source (`canary-status.sh:20-21`). Two independent measured facts (log content, mtime ordering) agree. | Fix the predicate to key FIRING on "log has content newer than the **last acknowledged/cleared** marker," not newer-than-heartbeat — heartbeat touches must never by themselves clear a firing state. Add a fixture pinning "ad-hoc run without log append must not flip FIRING to HEALTHY" (mirrors this repo's own convention of pinning defects as fixtures, e.g. T-2680/T-2711 style). | Small (one predicate + one fixture) | Yes, trivially — logic-only change, revert by restoring old predicate | If wrong: canary layer could over-fire on legitimate heartbeat-only refreshes (e.g. a cron that touches heartbeat as a liveness ping with no findings) — mitigate by only changing the *clear* condition, not the *fire* condition | `/canaries firing` count should track non-empty-log count exactly going forward; watch the next 7 daily runs for false-positive complaints |

### R5-02 | Binary staleness firing 75 days with no remediation | `.substrate-preflight-canary.log`, `arc-live-probe.sh` | **ADD(REPAIR)** | none | E-21: 74 dated entries over 75 days, `[WARN] binary` 71×, `[WARN] hub-binary` 26×. E-25: the repo's own shipped-equals-live gate (`arc-live-probe.sh`) fails against the local hub right now: served `0.11.1766` vs. floor `0.11.1944`. E-20: the charter-drift guard itself audits a stale installed binary (`0.11.1766`) against a repo at `0.11.1944` — 178 versions behind. | None — this is the exact G-069 "shipped≠live" class the project has independently built five tools to catch (T-2359/2387/2415/2480/2483), and all five are agreeing. | **HIGH** — 74 measured daily entries plus a direct measured probe failure (E-25) plus a version-string comparison (E-20), three independently measured sources converging on the same fact. | Restart the local hub onto the current binary (`systemctl restart` per the documented G-070 path) and re-run `arc-live-probe.sh` to confirm; this is pure operator remediation already prescribed by the project's own docs, not a code change. Root-cause why 75 daily WARNs produced no action — likely R5-01's masking made this invisible in `/canaries` despite the underlying log being non-empty the whole time. | Small (restart + verify) | Yes — restarting a hub onto a newer binary is standard, low-risk, already-documented operation | If the binary gap is intentional (pinned for a reason not in evidence) a restart could disrupt something — no such reason is cited anywhere in the evidence, so risk is judged low | `arc-live-probe.sh` should return exit 0 within one restart cycle; `.substrate-preflight-canary.log` should stop appending `[WARN] binary` lines |

### R5-03 | Push-wake rail dark — 0 live wakers, verb 2 degraded fleet-wide | `.waker-liveness-canary.log` | **ADD(REPAIR)** | none | E-24: 36 FIRING entries, most recent stating "ZERO LIVE listeners carry pty_session on this hub — the G-069 '0 wakers' state. Every DM sent here waits on the ~15s poll floor at best, forever at worst," plus a named unwakeable listener (`penelope`, presence-advertised, no pty_session). This directly degrades charter verb 2 (exchange durable messages) — messages are still delivered but real-time responsiveness is gone fleet-wide. | E-22 shows this canary reads inside the "healthy" `/canaries` rollup only because of the R5-01 masking bug, not because it stopped firing — so this is a live, current defect, not stale noise. | **HIGH** — 36 dated log entries (measured) with a specific, named, current instance (`penelope`) and an explicit mechanism (missing `pty_session`). | Relaunch the affected agent(s) through the documented push-wake launcher path (CLAUDE.md names `scripts/tl-claude.sh start --reachable ...` as the arm-at-relaunch remediation for exactly this state) so presence heartbeats carry `pty_session` again. | Small per agent, but fleet-wide (multiple relaunches) | Yes — relaunching a headless agent under the reachable launcher is reversible and is the documented normal path | Low — worst case is a brief presence gap during relaunch, which the DM-durability layer already tolerates by design | Waker-liveness canary log should show 0 new FIRING entries for 7 days; `/cv-keys agent-presence` should show `pty_session` on the relaunched agents |

### R5-04 | Hook-counter corruption feeds a decay-alarm with false silence | `.hook-counter-integrity-canary.log`, `lib/hook-telemetry.sh` | **ADD(REPAIR)** | none | E-37: `FIRING — counter file is corrupt`; duplicate key `check-arc-id`; two readers of the *same file* disagree (1 vs. 7). Stated mechanism: unlocked truncate+write race, labelled "L-023 recurring" (i.e., this has happened before). E-38: this counter is the denominator for the T-1626 decay alarm, so duplication "roughly halves the apparent failure ratio" — a second-order false-silence effect on top of the first. | None | **HIGH** — measured firing log with an explicit, cited mechanism and a concrete before/after reader disagreement (1 vs. 7) on the same underlying file. | Add a file lock (flock or equivalent) around the increment in `_fw_telemetry_increment`, since this is explicitly a *recurring* class (L-023) rather than a first occurrence — a structural fix (serialize the read-modify-write), not another ad-hoc patch. | Small (one function, add locking) | Yes | Low — locking a low-frequency counter write has negligible performance cost; worst case is minor contention under heavy hook load | Reader agreement (both counters equal) on next inspection; `.hook-counter-integrity-canary.log` stops firing |

### R5-05 | 11 unacknowledged inbound peer filings on `framework:pickup` (G-063 pattern recurring) | `.framework-pickup-canary.log` | **ADD(REPAIR/WIRE)** | B (never wired: no automatic consumer of the topic, by G-063's own long-standing description) | E-28: offsets 106-120, P-064…P-076 unacked (4 bug reports, 2 feature proposals, 5 learnings). E-29: this is the literal recurrence of concern **G-063**, open 105 days ("framework:pickup topic accumulates pickups with zero receipts — write-only"). E-22 shows this canary, too, currently reads inside the "healthy" `/canaries` rollup, consistent with the R5-01 masking mechanism. | E-28's footer notes 10 of the entries in that window are self-filings, correctly excluded — the 11 counted are genuinely inbound and unprocessed. | **HIGH** — measured log entry with specific offsets and categorized filing types; corroborated by a 105-day-old registered concern describing the identical mechanism. | Two actions, different owners: (1) triage the 11 named filings now (cheap, bounded, no code change) and ack per the documented workflow; (2) since G-063 itself has sat "watching" for 105 days without a structural consumer being built, treat that as a separate, larger ADD(WIRE) decision requiring scoping (an automatic consumer of a peer-inbound topic is new subsystem territory, not a one-line fix) — recommend inception-sizing it rather than building ad hoc. | Small (triage) / Large (structural consumer, needs its own inception) | Triage: yes, trivially. Structural consumer: yes, additive, no removal involved | Low for triage. For the structural consumer, risk is scope creep if built without inception per this project's own task-sizing rules | Triage: pickup canary log empty of these 11 after ack. Structural: would need its own success metric once scoped |

### R5-06 | Guard-layer runner did not complete in one CI-budgeted run | `scripts/run-guard-layer.sh`, baseline row | **ADD(REPAIR)** | none | Baseline: "did not complete within 600s wall; JSON file 0 bytes at cutoff." E-41: `--json` is written only at the end, so a hang or slow member produces **zero** recoverable verdicts, not partial ones. CLAUDE.md's own documented budget (cited in the evidence baseline) is "seconds" for the source tier. This runs in CI on every push/PR per the project's own description of the layer. | None — cargo test and cargo build both passed cleanly in the same baseline, so this is not a general environment failure; the guard-layer runner specifically is the outlier. | **MEDIUM** — one measured run (single data point: >600s, 0 bytes); no second run was attempted to confirm reproducibility, so it could be a one-off environmental hiccup rather than a durable regression. | Re-run with a per-member timing breakdown (the runner's own `GUARD_LAYER_TIMEOUT` default is 300s per member — confirm whether one member is silently exceeding it) and switch the `--json` writer to stream/flush per-member results incrementally so a future hang still yields partial, actionable output instead of 0 bytes. | Small (streaming write change) to Medium (finding the hung member) | Yes | Low — an incremental writer is strictly additive to the existing all-at-end writer | Guard-layer run completes within its documented "seconds" budget, or the JSON reliably names which member is hanging |

### R5-07 | `tools.rs` — 46,458-line, #1-churn monolith at 9.2% parity coverage | `crates/termlink-mcp/src/tools.rs` | **REFACTOR** | none | E-14: 46,458 lines in one file, 390 commits in 180 days (highest churn in repo). E-13: only 24 of 260 tools (9.2%) are parity-test-asserted; the remaining 236 are acknowledged-uncovered by ledger, not tested. | E-42 shows the *build* is clean (zero warnings, near-zero `#[allow(dead_code)]`), so this is a structural/maintainability cost, not a correctness alarm today. | **HIGH** for the cost claim (two independent measured sources: line count + git churn count); **MEDIUM** for readiness-to-refactor, since coverage is the gating fact and it is measured directly (9.2%). | Do not refactor the file itself yet — REFACTOR CHECK 2 (behaviour covered by tests) fails at 9.2%. Sequence: (a) raise MCP parity coverage via the existing ratchet mechanism (already tracked, T-2748) on the highest-churn portions of `tools.rs` first, (b) only then split the file along tool-category boundaries. Proposing the split before (a) inverts the safety order. | Large (file split) gated behind Medium (coverage ratchet work already in flight) | Partially — coverage work is additive/reversible; a file split is mechanically reversible via git but risks import/behavioural churn if done before tests exist | High if sequence is skipped: a churn-heavy, low-coverage file is exactly where a refactor introduces silent regressions | Churn rate on the file should drop post-split (fewer commits touching one file); parity coverage percentage should rise before any split lands |

### R5-08 | `cli/src/commands/channel.rs` — 20,435-line file, 166 commits/180d | `crates/termlink-cli/src/commands/channel.rs` | **REFACTOR** | none | E-15: 20,435 lines / 166 commits in 180 days — second-largest concentration after `tools.rs`, in the same crate pairing (`cli`+`mcp` = 68% of all code, E-16). | None | **HIGH** — same class of evidence as R5-07 (measured line count + measured churn), independently sourced. | Same sequencing caveat as R5-07: this file backs many of the same RPC surfaces (`channel.*`) that the MCP layer wraps, so a coordinated split (CLI command groups mirroring MCP tool categories) is more valuable than splitting either file alone. Treat as one combined structural task, not two independent ones. | Large | Partially, same caveat as R5-07 | Medium — lower per-tool test-coverage risk than `tools.rs` since CLI commands are exercised by more integration-style flows in this repo's evidence (fixture suites, E-48: 83 test scripts), but not confirmed for this specific file | Churn concentration should spread across resulting files rather than stay at one hotspot |

### R5-09 | `cli.rs` (7,001L/356 commits) + `main.rs` (1,931L/351 commits) — secondary churn hotspots | `crates/termlink-cli/src/cli.rs`, `main.rs` | **REFACTOR** | none | E-15 | None | **MEDIUM** — measured, but smaller magnitude than R5-07/08 and not independently investigated for cause (could be legitimate high-frequency, low-risk changes like adding new subcommand registrations rather than a structural problem). | Lower priority than R5-07/08. Worth a follow-up churn-cause breakdown (what fraction of the 356/351 commits are one-line command registrations vs. substantive logic changes) before committing to a structural change. | Small (investigation) before any structural work | N/A yet | Low | N/A until investigated |

### R5-10 | Project-boundary gate false-positive on the project's own hub runtime dir | `check-project-boundary` hook | **ADD(REPAIR)** | none | E-45: a read-only `cd`-based access to `/var/lib/termlink` (this project's own hub runtime directory) was blocked as "outside project root," while the identical read succeeded via absolute path with no `cd`. | None | **MEDIUM** — single measured instance in this session; not confirmed to reproduce across other paths/operators, but the mechanism (path-prefix check keying on `cd` specifically) is plausible and the workaround (absolute path) already demonstrates the gate is inconsistent for the *same* underlying operation. | Special-case the gate to recognize the project's declared hub `runtime_dir` (already a first-class, documented path in this project) as in-bounds for read operations, or normalize `cd`-based and absolute-path-based access through the same check so they agree. | Small | Yes | Low — narrowing a false-positive doesn't weaken the gate's actual purpose (blocking writes/execution outside the project), consistent with "never propose weakening gates" since this only fixes an inconsistency, not the boundary itself | No more false blocks on read-only access to the project's own declared runtime paths |

### R5-11 | Agent-dispatch gate blocked by a stale cross-session counter | dispatch gate (policy T-533) | **ADD(REPAIR)** | none | E-46: first `Agent` dispatch of a fresh session was blocked ("exceeds limit (2)") because the counter carried over from a prior session. | None | **MEDIUM** — single measured instance; the remedy path offered (`fw dispatch reset`) working confirms the diagnosis but this is one occurrence, not a repeated pattern in the evidence. | Scope the dispatch counter to session lifetime (reset on new session start) rather than persisting indefinitely, so a fresh session starts at 0 without requiring a manual reset/approve step. | Small | Yes | Low — this only affects when the counter resets, not the limit itself, so the Tier-2/dispatch policy intent (cap concurrent dispatches) is preserved | New sessions should not require `fw dispatch reset` on their first dispatch |

### R5-12 | `orchestrator.route` — hub-implemented, zero client surface, zero calls | `hub/src/router.rs:78,1186` | **INVESTIGATE** | B (never wired) + weak E (name overlaps charter non-goal #4 "not a workflow/orchestration engine") | E-07: implemented and dispatched in the hub, but 0 files reference it from CLI or MCP — there is no way for any first-party caller to invoke it. D is ruled out (hub-routed, would show in audit if called). | `no_federation_tripwire.rs:43,177` actively reasons about it as a "live residual path" (E-07 diagnosis, N-1) — recorded intent evidence that argues against treating this as abandoned/wanted-gone. This is exactly the "unexplained intent evidence" that blocks a DELETE verdict per the DELETE CHECKS. | **MEDIUM** — one measured structural fact (no client surface) plus one cited intent reference (tripwire), but no design doc or task history was gathered to explain *why* it was built with no consumer. | Before any code action: determine (from task history / design docs, not gathered here) whether `orchestrator.route` was scaffolding for a feature later shelved, or is intentionally hub-only infrastructure for the federation tripwire's own testing. If abandoned scaffolding with no future use: DELETE is the natural next verdict once the 7 checks can be run against that finding. If still wanted: needs a CLI/MCP surface (ADD-WIRE) to ever be reachable. | Investigation: small. Resulting action: unknown until investigated | N/A | Building the wrong surface, or deleting code the tripwire test needs, if investigation is skipped | N/A until investigated |

### R5-13 | `dialog.presence` — hub-implemented, zero client surface, zero calls | `hub/src/router.rs:140`, `channel.rs:28` | **INVESTIGATE** | B (never wired) | E-08: implemented (origin T-1286/T-243), documented in-code purpose ("passive multi-turn dialog presence tracker," keyed on `metadata.conversation_id"), traces to charter verb 2, but 0 CLI/MCP surface exists. D ruled out (hub-routed). | No abandonment decision found in evidence; the in-code purpose statement is itself a form of intent evidence keeping this from a clean DELETE verdict. | **MEDIUM** — same evidentiary shape as R5-12: one structural fact, one piece of documented intent, no task-history confirmation either way. | Same recommendation pattern as R5-12: check task/design history (T-1286/T-243) for why no consumer was ever built; if the tracked purpose is now served some other way, DELETE; if still wanted, this is a small ADD(WIRE) since the tracker itself already exists. | Investigation: small. Wiring (if chosen): small (one read verb) | Yes if WIRE; N/A for delete pending investigation | Low either way — this is inert code with no callers, so neither action affects live behavior until deliberately wired | N/A until investigated |

### R5-14 | `artifact.get`/`artifact.put` — fully wired, hub-visible, still zero calls in 34.2 days | `hub`, `cli`, `mcp` (E-06/N-4) | **INVESTIGATE** | None of A-E affirmatively established per gatherer's own diagnosis (N-4: "no reading is affirmatively established") | E-06/N-4: implemented in hub (2 files), CLI (2), MCP (1) — fully wired and discoverable, hub-routed so not subject to the E-12 audit-scope gap, yet genuinely zero calls across the whole measurement window. | None | **MEDIUM** — the zero-call fact is measured with high confidence, but the *reason* is unestablished; 34.2 days may simply be too short a window for a feature used only in rare artifact-exchange scenarios. | Extend the measurement window before concluding anything (this is the textbook case for a longer observation period rather than an immediate verdict) — no code action proposed. | N/A (data-gathering only) | N/A | Concluding DELETE prematurely would remove a fully-built, already-surfaced verb 2/4-adjacent capability on a possibly-too-short sample | Re-check after a 90+ day window; if still zero, revisit with stronger confidence |

### R5-15 | `kv.*` usage structurally invisible to the only measurement source | hub/cli/mcp (E-09/E-12/N-3) | **INVESTIGATE + ADD(instrument)** | D (unmeasured) — decisive | E-09: `kv.get` has 0 hub implementation files; CLI and MCP both expose it (5 MCP tools). E-12: the audit hook only fires from two call sites in `hub/src/server.rs` — session-daemon-served calls (which is where `kv.*` lives) never pass through it. | None needed — D is directly established by the wiring facts themselves, not inferred. | **HIGH** for the diagnosis (D is structural, not a judgment call); no confidence claim is made about actual kv usage volume, which remains genuinely unknown. | No value verdict possible until instrumented. Add a session-daemon-local counter (or route `kv.*` calls through the same audit sink, if architecturally feasible) before any future review attempts to judge this surface's value. | Small (counter) to Medium (routing through the shared audit sink) | Yes | Low | Once instrumented, a future review can classify kv.* on real usage data instead of a structural blind spot |

### R5-16 | Session-scoped lifecycle methods (`session.deregister/heartbeat/register/update/whoami`) — zero hub-observed calls | E-06 | **INVESTIGATE + ADD(instrument)** | D (unmeasured) — strong by analogy to R5-15/kv.* | These are session-lifecycle RPCs; per E-12's established mechanism (session-daemon-served calls bypass the hub audit) the same structural blind spot that decisively applies to `kv.*` (N-3) plausibly applies here, though the gatherer did not run the specific per-method wiring check (hub=0/cli=N/mcp=N) for this group the way it did for kv/artifact/orchestrator/dialog. | None | **MEDIUM** — the analogy to a decisively-established sibling case (kv.*) is strong, but this specific group was not individually verified by the gatherer, so it is inferred rather than measured for this group specifically. | Run the same wiring check done for N-1 through N-4 on this group before drawing conclusions; if hub-implementation is absent here too (as is architecturally likely for session lifecycle), the same instrumentation gap applies and no value verdict should be attempted until closed. | Small (wiring check) | N/A | Low | Confirms or refutes the D reading for this group |

### R5-17 | `event.emit/subscribe/topics/state_change/error` — zero hub calls despite CLI/MCP surface | E-06 | **INVESTIGATE** | Unclear — could be D (if delivered via a local/session path analogous to kv.*) or genuine low-use; gatherer did not resolve which | E-06 shows `event.subscribe` specifically has hub=3/cli=4/mcp=1 file references (i.e., it does have hub implementation, unlike kv.*) yet still zero observed hub calls — this argues against a pure D reading for at least this member, unlike the session/kv groups. | The sibling `event.broadcast` in the same family has a directly-evidenced E reading (legacy, targeted for retirement, R5-18) — worth checking whether the whole family shares that fate or only that one member. | **LOW** — conflicting signal (hub-implemented yet zero calls, unlike the clearly-D kv/session cases) with no wiring-detail investigation done per-member. | Run the same per-method wiring/legacy-list check used for `event.broadcast` (checking `LEGACY_METHODS` membership, `rpc_audit.rs`) against each remaining `event.*` member individually before judging. | Small (per-method check) | N/A | Low | Resolves whether this is a retirement-family or a genuinely underused-but-wanted surface |

### R5-18 | `event.broadcast` — recorded intent to retire, zero calls | `hub/src/rpc_audit.rs:54-58` (E-10) | **INVESTIGATE (DELETE-leaning)** | E (not wanted) — direct evidence | E-10: explicitly listed in `LEGACY_METHODS`, described as "targeted for retirement" by the T-1166 entry gate. Zero calls in the 34.2-day window. This is the single strongest E-reading of any item in this review — an explicit, recorded, code-level statement of intent to remove. | DELETE CHECKS 4 and 5 (no references in strings/config/hooks/CI/prompts; no external consumer) were not verified by the gatherer for this specific method — the evidence only establishes the *legacy-list membership*, not the absence of every other reference class the DELETE checklist requires. | **MEDIUM** — the E-reading itself is about as strong as evidence gets (a direct code-level retirement declaration), but procedurally the full DELETE checklist cannot be certified as passed from this evidence file alone. | Run a targeted reference sweep (strings/config/hooks/CI/prompts) for `event.broadcast` specifically; if clean, this converts directly to DELETE with high confidence — of everything in this review, this is the closest any item comes to a clean removal. State the revert plan (git revert of the removal commit) explicitly in that follow-up. | Small | Yes (git revert) | Low — the retirement was already declared by the project itself; the only open question is completeness of the reference sweep | Sweep confirms zero remaining references; method is removed and `LEGACY_METHODS` entry is dropped accordingly |

### R5-19 | 28 off-charter analytics MCP tools — GO recommendation recorded, no confirmed decision, no removal task | `.context/checks/charter-drift-allowlist`, T-2548 | **INVESTIGATE** | D (unmeasured — no per-tool telemetry exists, decisive per E-13/N-7) stacked with E (not wanted — charter non-goal #3 + T-2548's own recorded "GO to subtract" recommendation, E-18) | E-17: 28 tools, all currently allowlisted with reasons reading "traces to no charter verb." E-18: T-2548 is `work-completed`, dated 30 days before this review, body records "Agent recommendation = GO to subtract," but its `## Decision` section is empty (template text only) — the actual human decision was never recorded in the one place designed to hold it. E-19: no follow-on build task exists anywhere in `.tasks/` to perform the removal, and zero files reference "conversation-analytics" in active tasks. | This project's own framework rules state sovereignty over exactly this kind of charter-interpretation call belongs to the human, and that an agent recommendation is not authorization — the empty Decision section is not an oversight to route around, it is the actual state of a decision that has not been made. | **HIGH** on the facts (all three supporting rows are directly measured/observed: allowlist contents, task frontmatter/body text, absence-of-file grep); **the value verdict itself cannot exceed LOW-to-undeterminable** because it is gated on an unrecorded human decision, not on additional evidence-gathering. | This is not a proposal to delete — that decision is sovereign (see Sovereign Questions below). The actionable, non-sovereign step is procedural: T-2548 should not be treated as a closed loop with an empty Decision section; either the human decision gets recorded now (closing the loop as originally designed), or the task is explicitly reopened/re-flagged as pending-decision rather than reading as a resolved GO in episodic memory. | Small (process correction) — the eventual removal itself, if approved, would be Medium (28 tools, allowlist cleanup, doc updates) | Recording a decision is trivially reversible (it's a decision, not code); the eventual removal (if approved) is reversible via git revert | Treating an agent recommendation as if it were a ratified decision (by acting on it, or by leaving it silently unresolved indefinitely) both carry equal risk of misrepresenting project state | A recorded Decision (either way) removes the current ambiguity that this review, and presumably T-2971's own review, keeps re-discovering |

### R5-20 | `channel.create` ≈ `channel.post` traffic ratio (0.98) against 45 stable topics | E-05 | **INVESTIGATE** | None of A-E cleanly applies — this is a cost/efficiency question, not a use/non-use question | E-05: 52,695 creates vs. 53,538 posts in the same window, against only 45 topics that currently exist. If most callers issue a `channel.create` immediately before nearly every `channel.post`, `channel.create` traffic (5.7% of all hub RPCs per E-02) is largely redundant idempotent-check overhead rather than genuine topic creation. | It is also possible this is an intentional, cheap "ensure-topic-exists" client convention and the hub-side cost of a no-op create is negligible — the evidence does not measure per-call latency/cost, only call counts. | **LOW** — the ratio is measured precisely, but the interpretation (wasteful vs. intentional-and-cheap) requires data not gathered (per-call cost, client-side code path). | Measure the actual hub-side cost of a `channel.create` call when the topic already exists (is it a fast no-op or a real write path check); if costly, consider client-side caching of "topic known to exist" to skip the redundant RPC. | Small (measurement) before any client-side change | N/A until measured | Low | If a no-op create is found to be non-trivial cost, eliminating redundant calls should show up as a drop in `channel.create`'s share of total RPC volume |

### R5-21 | `health:ring20-fedprobe` — largest topic (1,672 records), `forever` retention, readership unknown | E-11, N-9 | **INVESTIGATE** | D (unmeasured — subscriber cursor data not gathered) | E-11: largest single topic on the hub, retention=forever, actively growing. N-9 confirms it sits below the forever-archival canary's 50,000-record ceiling (so nothing fires today) and is not on the documented operator-durable exclusion list (`channel:learnings`, `policy-decisions`, `framework:pickup`, `broadcast:global`). | No evidence it is unwanted — it is the single most actively-used topic on the hub by record count. | **LOW** — growth trend and readership are both unmeasured; only a point-in-time count exists. | Extract subscriber cursor/offset data for this topic (explicitly listed as "not gathered" in the evidence file) before judging whether `forever` retention is appropriate here versus a bounded policy matching the project's own documented retention-recommendation table for high-rate topics. | Small (data extraction) | N/A | Low — well below any firing ceiling today, so no urgency, but growth trend is unknown | Would let a future review classify this topic's retention setting with actual data instead of a raw count |

### R5-22 | G-008 concern text is stale relative to its own subject (64 → 71 partial-complete tasks) | E-31, E-32 | **INVESTIGATE** | None — this is a concern-register hygiene question, not an item use question | E-31: G-008 recorded 64 partial-complete tasks 157 days ago. E-32: `check-stranded-finalized-tasks.sh` measures 71 today, and explicitly does not classify any of them as the T-2833 stranded-defect class (`firing_count: 0`) — meaning these are legitimate T-193-style partial-completes (agent ACs done, human ACs pending), just growing in count, not a new defect. | The check that would catch a *defect* class here reports clean — this is a backlog-growth signal, not a correctness failure. | **HIGH** on the fact (both counts are directly measured, 157 days apart) | Whether backlog growth here needs a process response (e.g., a periodic human-AC triage cadence) is an operational/governance judgment, not a code fix — flagged for awareness rather than a specific technical proposal. | N/A (governance, not code) | N/A | N/A | Tracking this count at each future review would show whether it continues to grow or stabilizes |

---

## KEEP (names only)

- Charter verb 2 core traffic path: `channel.subscribe` / `channel.post` / `hub.auth` / `session.discover` (top 4 = ~83% of hub traffic; working as intended per E-01/E-02)
- Charter verb 3 "claim work" full lifecycle (`channel.claim/renew/release/transfer_claim`, `/claim*` skills, `substrate-smoke.sh`) — low absolute volume (108/34d) is expected for a work-stealing primitive used only when contention exists, not a non-use signal (N-6)
- Charter verb 4 partial usage (`command.execute`/`command.inject`) — used, at a scale consistent with terminal-session control being a subset of all sessions
- `no_federation_tripwire.rs` — enforces non-goal #1 at build time
- Guard-layer marker/fixture mechanism (`# guard-layer: source`) as a design pattern, independent of the R5-06 execution defect
- Charter-drift canary + allowlist ratchet mechanism (independent of the R5-02 stale-probe-binary defect)
- MCP parity census ratchet (T-2748, working down 236 acknowledged-uncovered tools over time — the mechanism itself is sound per E-13)
- `cargo build`/`cargo test` hygiene (zero warnings, near-zero `#[allow(dead_code)]`, E-42)
- Zero orphan scripts (E-39) — every script in `scripts/` is referenced somewhere
- Cron-install-drift check — correctly caught the one missing crontab, already tracked as an active task (T-2939, E-36)
- Stranded-finalized-tasks check — correctly reports 0 firing for the actual defect class it targets (E-32)
- Topic-growth / forever-archival canary mechanism — correctly not firing on `health:ring20-fedprobe` because it is genuinely below the declared ceiling (N-9)
- Gate-bypass log as an audit trail mechanism (its *volume/pattern* is a separate INVESTIGATE-worthy governance question, not a defect in the mechanism itself)

---

## INVESTIGATE (data needed, restated compactly)

- **R5-12** `orchestrator.route`: task/design history for why it was built with no client surface; whether the federation-tripwire reference means it must stay.
- **R5-13** `dialog.presence`: task/design history (T-1286/T-243) for the same question.
- **R5-14** `artifact.get/put`: re-measure over a 90+ day window before any verdict.
- **R5-15** `kv.*`: session-daemon-local usage counter (or route through the shared audit sink) — currently structurally invisible.
- **R5-16** `session.*` lifecycle methods: run the per-method hub/cli/mcp wiring check (as done for kv/artifact/orchestrator/dialog) to confirm the D reading applies here too.
- **R5-17** `event.emit/subscribe/topics/state_change/error`: per-method `LEGACY_METHODS` / wiring check, since at least one member (`event.subscribe`) is hub-implemented yet still zero-call, unlike the clearly-D session/kv groups.
- **R5-18** `event.broadcast`: full reference sweep (strings/config/hooks/CI/prompts) to complete DELETE CHECKS 4-5 before converting this INVESTIGATE to a DELETE.
- **R5-19** 28 off-charter tools: whether the human decision recorded in T-2548's (empty) `## Decision` section was ever actually made — see Sovereign Questions.
- **R5-20** `channel.create`≈`channel.post` ratio: per-call hub-side cost of an idempotent create-on-existing-topic.
- **R5-21** `health:ring20-fedprobe`: subscriber cursor/offset data (explicitly listed as not gathered).
- **R5-22** G-008 growth: no additional data needed to note the trend; a governance decision on triage cadence is the open question.
- Auth/query/orchestrator-bypass residue (`auth.token`, `query.capabilities`, `query.output`, `orchestrator.bypass_invalidate`, `orchestrator.bypass_status`): none of these received the individual per-method wiring check that resolved N-1 through N-4; needed before any reading can be assigned.
- `channel.force_release` / `channel.set_retention`: zero calls in-window with no wiring check performed; likely rare-by-design (Tier-0/operator actions) but this is inferred, not established in evidence — needs the same check.
- Fleet hub version spread (E-26/E-27): true staleness of `.121`/`.122` is not inferable from patch numbers alone (documented lineage caveat) — would need each hub's actual build commit/sha to resolve, not gathered.

---

## Data gaps that capped confidence

- **No MCP per-tool or CLI per-verb invocation telemetry exists at all.** This caps every judgment about the 262 MCP tools and 41 CLI verbs to "hub-RPC-visible traffic only," which per E-12 explicitly does not cover session-scoped surfaces (kv, session.*, pty, some event.*). Closing this unlocks real verdicts on R5-15, R5-16, and the 28-tool question's D component (R5-19).
- **Guard-layer per-member verdicts are unavailable** (R5-06, run incomplete) — without them, no independent evidence exists about which specific static checks are healthy vs. failing today, only that the runner as a whole did not finish.
- **No out-of-band delivery observer** beyond the on-demand, unscheduled `session-message-selftest.sh` — verb 2's "exchange durable messages" claim rests on bus-self-reported receipts except when that script is manually run.
- **Subscriber cursor/offset data per topic was not extracted** — caps R5-21 and any broader claim about topic "usage" beyond raw record counts.
- **Unused-dependency analysis not performed** (`cargo-udeps`/`cargo-machete` absent) — caps any claim about the 30 workspace dependencies' individual value.
- **Audit-history recurrence analysis across 151 `.context/audits/*.yaml` files not performed** — would materially strengthen or weaken R5-22 and the broader "is backlog age worsening" question by showing a trend line instead of two point-in-time counts.

---

## Contradictions

1. `/canaries` reports `firing: 0` (E-22) while at least four canary logs (binary staleness, waker-liveness, framework-pickup, hook-counter) carry current, substantive, same-day findings — both readings are internally consistent with the tool's own stated (but flawed) predicate (E-23). Resolved by R5-01.
2. MCP tool count: 260 (parity census, comments stripped) vs. 262 (raw grep) — a 2-tool delta from methodology, not a real discrepancy (E-13).
3. Charter-drift canary's "214 checked" vs. the census's 260/262 — the two guards scan different artifacts (installed stale binary vs. repo source, E-20), not a real disagreement about tool count.
4. Clean build/test hygiene (E-42) coexists with extreme structural concentration (E-14: 46,458 lines in one file) — cost and correctness are genuinely separate axes here, not contradictory, but worth stating plainly since a reader could conflate "builds clean" with "well-factored."
5. T-2548 is `status: work-completed` with a body recommending "GO to subtract," yet its `## Decision` section is empty and no removal task exists (E-18/E-19) — whether a human decision was actually made is not determinable from the task file alone. Central to R5-19.
6. G-008's own recorded figure (64) is now measured at 71 (E-32) — the concern register entry is stale relative to the condition it describes.
7. Guard-layer documented budget ("seconds") vs. a measured >600s incomplete run (E-41).

---

## Sovereign questions

1. **Do the 28 off-charter analytics MCP tools get removed?** Charter non-goal #3 ("not a social/engagement platform") plus T-2548's own recorded agent recommendation ("GO to subtract") point toward removal, but the task's `## Decision` section is empty and no human sign-off is recorded anywhere in evidence. **Recommendation:** record an explicit decision now — either GO (opening a scoped removal task) or NO-GO/defer (closing the open question and updating the allowlist reasons accordingly) — rather than leaving 28 tools in a permanently-acknowledged-but-undecided state, which is itself a standing cost on every future charter-drift review.

2. **Does `orchestrator.route`'s existence conflict with non-goal #4 ("not a workflow/orchestration engine")?** The method is hub-implemented, unreachable by any client, and its name directly overlaps the non-goal's language, yet a live tripwire test treats it as intentional residual infrastructure. **Recommendation:** a human ruling on whether this method's continued existence (even unreachable) is consistent with non-goal #4, or whether it should be scoped out entirely as the non-goal's most literal violation-in-waiting.

3. **Does G-064 ("Hub has no per-user authorization model," open 103 days) ask for something that conflicts with non-goal #5 ("not a security boundary between mutually-distrusting tenants")?** These two statements — one an open concern, one a canonical non-goal — read as being in tension. **Recommendation:** a human ruling on whether G-064 targets a narrower requirement (e.g., audit attribution) that does not actually require tenant-isolation guarantees, so the concern can be scoped precisely instead of sitting ambiguous against a non-goal for over three months.
